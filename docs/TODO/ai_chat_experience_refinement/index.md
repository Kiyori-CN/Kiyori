---
status: in_progress
---

# AI 对话全界面与交互优化

本专项覆盖软件首页右侧 AI 对话及其可达子页面、消息弹层、输入组件、历史选择器、悬浮小窗和语音界面。目标是修复有源码或实测依据的问题，并在既有 Kiyori 视觉体系中完善布局、反馈和操作连续性。本文是本专项唯一进度入口；长期所有权见 [AI 执行契约](../../doc-src/contracts/ai_execution.md) 与 [产品壳契约](../../doc-src/contracts/product_shell.md)。

## 目标与边界

- 从用户的打开、输入、发送、等待、停止、修改、历史切换、语音和退出流程逐项审查；从 Agent 的工具执行、权限等待、流式输出、失败和恢复流程反向验证。
- 保持固定浅深主题、局部 AI 外观设置、唯一 ChatServiceCore/ChatViewModel 与现有语音及浮窗生命周期所有者。
- 不新增静默重试、模型/网络回退、第二套状态存储，不改兼容协议、持久化格式或无关浏览器、播放器与终端架构。
- 当前授权包含本轮优化、本地验证及最终提交推送。安装、清数据、操作设备和真实付费请求不包含在本地验证中。
- 不能以“无已知本地问题”证明不存在未知缺陷；触控、IME、TalkBack、真实语音、OEM 悬浮窗与网络验收分别保留 `verification_pending`。

## 观察基线与来源

### 当前续审基线（2026-09-09 13:45 +08:00）

本轮为上一交付的继续审查，整体仍为 `in_progress`。父仓库 `main` / `4b1be98686075019bba32e392bd4e9af4612ca77`，terminal `main` / `53a8ea5b1bb747bb2caa4dcc6c447631aa650480`；两个工作区均清洁，`git ls-remote origin refs/heads/main` 分别与本地 HEAD、跟踪 ref 一致。可选夜间构建子模块未初始化，不属于本轮构建依赖。

本节及[续审矩阵](01_surface_inventory.md#当前续审矩阵)负责当前状态；下方旧批次中的“待修”“正在构建”“尚未提交”等表述只描述当时观察，不能直接推定当前缺陷或完成。历史 APK 和测试数量不作为本轮源码验证证据。

| 阶段 | 当前真实进度 | 下一验收点 |
| --- | --- | --- |
| A / P0 | 规则、正式契约、日记续段、双仓远端与入口恢复；活动矩阵已校准，定位组继续逐叶展开 | 文档与阶段构建已通过；进入B |
| B / P1–P5 | 上轮首批实现存在；剩余菜单、输入、历史、浮窗与语音消费者尚未逐叶复核 | 按用户动作与共享 owner 逐项审查，必要修复、定向测试、APK |
| C / P6a | 已核实绑定/解绑为等待服务 async 结果，DAO 有原绑定比较；其余旧结论须逐项核实 | 工作区创建/冲突/恢复，终端设置/日志/会话叶子 |
| D / P6b | 正式审计契约与当前入口恢复；上轮修订/导出不等于本轮完整反审 | 三 Tab、payload、修订、批注、统计、导出与敏感边界 |
| E / P6c | 脚本发布改造存在；disablePackage 仍在锁外读取和分别写入关联名单 | 发布状态与引擎释放顺序设计，再闭合各扩展与市场叶子 |
| F / P6d | 当前目录明确注册 16 个原生工具箱入口，另有动态 ToolPkg 项 | 按注册项而非文件名逐页确认；SQL Canvas 等残余范围 |
| G / P6e | 列表已有读取代次；详情 loadWorkflow 无目标代次，写入仍需审查 | 详情/日志身份、写入互斥与草稿、节点/连接、执行、导入导出 |
| H / P6f | 搜索有局部代次；loadMemoryGraph 与搜索仍可互相覆盖，改查询不撤销旧请求 | 统一现有 ViewModel 的图谱发布资格，再审文档/文件夹/链接/索引 |
| I / P6g | 模型、提示词、角色、语音、备份页面和既有配置 owner 已定位 | 参数与保存边界、每个选择/编辑弹层及持久化失败 |
| J / P7 | 上轮交付 refs 已核实；本轮尚未形成候选提交 | 全矩阵对账、正式检查、最新 APK、精确双仓审计与 main 推送 |

执行优先级：先 B 收口主链，再依次 C–I；每个增量完成相关测试、`git diff --check`、文档检查、串行 `:app:assembleDebug` 和 APK 元数据核验才进入下一实现增量。最终执行正式准备检查；候选提交形成后检查链接、仓库卫生、本地化及 fresh clone。terminal 有变更时先独立测试、审计、提交并推送，再记录父 gitlink。没有源码变更的领域以实际阅读符号和无变更理由闭合，不制造修改。

已确认高风险点：registry 关联状态丢更新及引擎释放锁顺序；工作流详情迟到发布；记忆搜索与文件夹图谱竞争。工作区文件系统与 SQL、市场文件与元数据无共同事务是设计边界，需验证部分失败反馈，不能承诺原子回滚。回滚点为上述已推送双仓基线及每个增量精确差异；不重置工作区、不覆盖并发用户修改。设备、IME、TalkBack、SAF、真实语音、SSH、OEM 浮窗与市场服务均未操作，继续 `verification_pending`。

### 本轮阶段证据

本轮阶段 A 验证：文档检查501文件0问题，`git diff --check`通过；`:app:assembleDebug --no-daemon --console=plain`于2026-09-09通过（59s，238任务中20执行、218 up-to-date），launcher及两项runtime packaging验证通过。此阶段只修改文档，APK复用构建缓存、未重新打包；实际产物486090601 bytes，时间13:26:17 +08:00，SHA-256 `1EA82785CFE5BCF9E227605E847C4B98C724FBE5AAD41C90F0764DAA6FA7A3D4`，aapt核实`com.kiyori / 45 / 0.1.0 / arm64-v8a`。不能把此产物用于后续代码增量验收。

阶段 B 首增量：历史标题/角色绑定弹窗原先分别派发异步写入并立即关闭，失败丢草稿且可能产生半次保存。现改为`ChatMetadataSnapshot/mergeChatMetadataEdit → ChatHistoryManager.editChatMetadata → ChatHistoryDelegate → ChatViewModel → ChatHistorySelector`，同一聊天锁与Room事务内比较原值、合并实际修改、成功后关闭；失败保留正文，显示安全错误，取消继续传播，未保存关闭需确认。原列表Flow负责投影，保留其他兼容入口。`:app:testDebugUnitTest --tests '*ChatMetadataEditTest' --no-daemon --console=plain`通过（2m5s），7项执行、0失败/错误/跳过；文档501/0及diff检查通过。`:app:assembleDebug --no-daemon --console=plain`通过（2m21s），新APK时间2026-09-09 14:07:19 +08:00，494437403 bytes，SHA-256 `E935E2478548ECD422D3E489ECAE0647FB62C3F98D241369F172172763A58A85`，aapt核实`com.kiyori/45/0.1.0/arm64-v8a`，launcher/runtime packaging通过。Room事务故障与实际IME仍待设备验证。历史分组/删除/重排与消息正文编辑的异步关闭仍在下一增量队列，不计为关闭。

阶段 B 消息编辑增量：`ChatScreenContent/MessageEditor`保存委托原审计修订并检查原文，失败留草稿、保存中禁用重复操作；组合按chat/timestamp/variant隔离，取消后不发布旧结果。解析器保留标签间逐字空白，输入回调同步更新编辑值；模式切换不触发Effect写回。`testDebugUnitTest --tests '*MessageEditorContentTest' --tests '*ConversationAuditTextChangeTest'`通过（1m32s），新增4项与原审计4项合计8通过、0失败/错误/跳过。文档501/0及diff检查通过；Debug构建通过（56s），APK时间2026-09-09 14:17:07 +08:00，494438699 bytes，SHA-256 `E2A8CDE52ECE718C430AC4D43F32D61F0CF320B75C219497A16C0328564D6E6A`，`com.kiyori/45/0.1.0/arm64-v8a`，launcher/runtime packaging通过。编辑重发、分组/删除/重排仍未闭合。

阶段 B 输入插件/队列增量：提交hook异常安全传播并阻断发送；队列临时移出项在准备失败、切chat取消及协程尚未启动即取消时归还原owner，保持稳定ID顺序，不复活已删除聊天。插件明确消费或开始调用发送后不恢复，异常提示区分准备失败和派发后结果待核对，不将未知提交当作未发送。`testDebugUnitTest --tests '*ChatInputHookSubmissionTest' --tests '*PendingQueueSubmissionTest' --tests '*PendingMessageQueueStoreTest'`最终13项通过（3+6+4，0失败/错误/跳过，1m35s）；文档501/0及diff检查通过。Debug构建通过（3m55s），APK时间2026-09-09 14:33:37 +08:00，494440159 bytes，SHA-256 `9F9E7B1AB5E224518C813B68391038CD7723B29023D0599DC661E80AD7FA4634`，`com.kiyori/45/0.1.0/arm64-v8a`，launcher/runtime packaging通过。主/浮窗共享发送owner仍需继续审查。

阶段 B 历史删除增量：`ChatHistorySelector → ChatViewModel.deleteChatHistoryAwait → ChatHistoryDelegate.deleteChatHistoryAwait → ChatHistoryManager`等待实际结果，执行期间保留确认并禁重复操作，不提前隐藏条目。`completeChatDeletion`区分删除前失败与实体已删后的清理失败；后者清除原待发队列并显示部分完成，不在原确认里重复删除。DataStore清除选择在同次edit中比较ID；数据库预检异常不再误报锁定。`testDebugUnitTest --tests '*ChatDeletionCompletionTest' --tests '*PendingMessageQueueStoreTest'`通过（2m13s）：6+4共10项，0失败/错误/跳过。文档换行检查曾发现3项CRLF，已修正并复验501文件0问题，diff检查通过。Debug构建通过（2m14s），APK时间2026-09-09 14:43:02 +08:00，494442143 bytes，SHA-256 `4A207572276FC28272C02EB0561740FE3B0888D9275F60A40CBB5AADFE69D205`，`com.kiyori/45/0.1.0/arm64-v8a`，launcher/runtime packaging通过。分组/重排和消息重发仍未闭合。下一高优先级已确认：`GroupTarget/HistoryListItem.Header`只携带角色名，角色群组和未绑定分类丢失scope；`ChatDao`的null角色入口操作全局同名分组。须显式保留全部/角色卡/角色群组/未绑定范围，贯通改名、删除、等待结果与后台执行停止。

阶段 B 分组范围/改名/删除增量：`ChatGroupScope/ChatGroupTarget`统一原生分类匹配，修复群组/未绑定操作落入全局同名SQL，以及默认角色当前分类混入群组聊天。Header携带显式scope；角色资料尚未加载时不生成替代操作范围。`renameChatGroup/deleteChatGroup`在原manager事务中提交，服务先停止待删成员并迁移当前chat，事务重新核对成员/锁定快照；锁定聊天移到未分组，清理失败明确部分完成。UI等待结果、保留失败草稿、改名未保存关闭确认，删除执行期间禁重复操作；旧Web协议入口保留。`testDebugUnitTest --tests '*ChatGroupTargetTest' --tests '*ChatDeletionCompletionTest'`通过（1m54s），11+6共17项，0失败/错误/跳过；文档501/0及diff检查通过。Debug构建通过（2m22s），APK时间2026-09-09 15:00:26 +08:00，494444643 bytes，SHA-256 `31F22B5694726DDFF63961BED15C06FD1D37709ADEFC41B9FE48BC0FD75C6F3E`，`com.kiyori/45/0.1.0/arm64-v8a`，launcher/runtime packaging通过。分组创建与历史重排、消息重发仍待下一增量。

阶段 B 分组创建增量：`createGroupAwait/completeChatCreation`先使用原仓储创建（不顺带设置全局选择），再根据原选择身份申请切换；`compareAndSetCurrentChatId`在DataStore同次edit内比较，迟到创建不抢回新选择。UI固定打开时绑定、等待结果、失败保留名称、未保存关闭确认；角色资料未加载时不替代为未绑定。创建已提交但选择失败明确提示已创建，禁重复创建。首轮测试样本漏传messages导致编译失败，补齐后6+11共17项通过（1m5s，0失败/错误/跳过）。随后兼容反审确认旧Web改名可留下空白名称，已允许精确定位以便修复，不匹配null未分组；最终`testDebugUnitTest --tests '*ChatCreationCompletionTest' --tests '*ChatGroupTargetTest'`通过（1m16s），6+12共18项，0失败/错误/跳过。旧VM/服务层分组fire-and-forget无调用入口已移除，Web仓储兼容入口保留。文档501/0与diff检查通过；最终Debug构建通过（50s），APK时间2026-09-09 15:15:55 +08:00，494446579 bytes，SHA-256 `CBCF46195F757759F2F6546EFA9C2F3E49345C6CB1EF2908DD37CCF781B1D89F`，`com.kiyori/45/0.1.0/arm64-v8a`，launcher/runtime packaging通过。`loadChatMessages`吞异常和取消、旧collector迟到发布/加载状态仍须下一增量处理；创建完成不代表此项已关闭。历史重排与消息重发继续待审。

阶段 B7 聊天窗口增量（2026-09-09）：原窗口控制器复用原chatId/history Flow，统一选择/刷新/前后分页/最新/定位的请求资格与消息边界快照；旧完成、失败、取消不能重置新请求。实时更新（含同一stream对象）使旧SQL读取失效，原内容保留并显示手动重载。global collector改collectLatest，null清窗口，chatExists异常传播；开场白持久化后按快照发布。ChatArea错误条包含安全文案、重载与关闭，重试原子消费错误并固定目标，失败提示存在时阻止自动最新窗口请求。首轮控制器12项通过（1m51s，0失败/错误/跳过），最终控制器14项全部通过（4m35s，0失败/错误/跳过）；补齐MainActivity启动取消传播及WebChatHttpBridge安全500错误边界，Debug APK构建通过（5m10s），launcher与两项runtime packaging验证通过。产物时间2026-09-09 15:58:04 +08:00，494448207 bytes，SHA-256 `E74C4B68F6702D70490043E25FA34DE5D658CA617DCE0E628283694B8DC1FD17`，aapt核实`com.kiyori/45/0.1.0/arm64-v8a`；文档501/0、diff通过。用户疑似误改仅确认controller第60行末尾3个空格，已修复并保留本轮实现。选择提交失败反馈、历史重排及消息重发仍属后续审查，不宣称阶段B全部完成。

阶段 B8 历史移动增量（2026-09-09）：确认筛选/折叠子集被当作完整列表乐观回写，跨角色拖动分开派发绑定和顺序保存，350ms debounce子任务失败不进入外层catch。新增`ChatOrderMove/ChatPositionSnapshot`，原Room事务校验移动项与锚点位置/绑定，完整当前列表内移动单项，只更新顺序及移动项的组/绑定；其他字段、隐藏项和置顶类别保留。移除原生整表投影与debounce，UI等待服务持有结果，拖拽与上下移动共用提交，菜单内进度/错误可见。Web旧列表协议保持，内部改为事务内只写指定列。`testDebugUnitTest --tests com.ai.assistance.operit.data.repository.ChatOrderMoveTest`通过（2m3s），12项执行、0失败/错误/跳过；补充菜单错误与置顶边界禁用后，Debug APK构建通过（2m24s）；launcher与两项runtime packaging通过。产物时间2026-09-09 16:12:05 +08:00，494448863 bytes，SHA-256 `C5D2FCEAC0397391E9F64A8EA55FF814DB25CDFB8753839DF90CD8AF0A8AA51E`，aapt核实`com.kiyori/45/0.1.0/arm64-v8a`；文档501/0及diff通过。Room真实并发、连续拖动手感、TalkBack和大字体仍为`verification_pending`。

阶段 B9 选择提交/观察恢复增量（2026-09-09，本批本地验证通过）：接完历史入口 suspend 等待、原聊天统计快照、选择互斥与 DataStore 原值比较；共享 CurrentChatSelectionObserver 保留最后有效选择，显式 Loading/Ready/Failed，失败身份控制重载且无静默重试。主到浮窗改为 collectLatest 和实际结果等待，来源协程取消撤销本地读取资格；浮窗返回主应用等待持久化并禁重复点击。选择投影等待有5秒失败终态，不把超时当未写入或自动重发。69129集中测试通过（10m5s）：新增6项观察与原窗口14项合计20通过、0失败/错误/跳过；44644曾因R引用缺失编译失败，已修复。另将统计读取明确绑定原窗口chatId，避免统计collector迟到读错聊天。最终APK15997构建通过（6m3s，238任务中27执行），launcher与两项runtime packaging通过；实际产物2026-09-09 17:01:19 +08:00，494450227 bytes，SHA-256 `35B28A8F223FCC00EA4577D05FF8F7061ADCB4720D9A0B53E02CC6A9DF93BDCD`，`com.kiyori/45/0.1.0/arm64-v8a`。文档501/0、diff检查通过；父与terminal未提交推送，terminal仍干净。自动角色切换现已在角色查找及双偏好写入间核对聊天资格；若聊天已切换，后续清理写入会停止。主题/Waifu管理器内部的单次DataStore事务仍可能在资格变化后完成，按部分完成事实保留，不伪造原子跨存储。编辑重发/回滚部分完成和其余B域叶子仍未闭合，不将B9整体标记完成。

B9 编辑重发/回滚增量（2026-09-09，继续实施）：编辑器不再在启动异步回档/重发后立即清除输入草稿；工作区确认上下文暂不因调用协程而提前丢失。当前已新增 workspaceRewindState（Idle/Running/Succeeded/Failed/Unknown）并将执行中状态接入确认弹层禁用重复确认；索引/消息类型前置失败不会误留Running。成功/未知终态的上下文收口与派发观察仍需完成。角色激活协调器已统一请求代次、取消和原偏好写入资格；`:app:compileDebugKotlin` 通过（2m53s）。

### 上一轮起点（历史）

2026-09-09：`main` / `1ea07fd9193ac56b711eb9c2e8434313b87ff04a`，父仓库干净；terminal 固定 `88681c2d3c7bab2aec093b7b85f0451de85fc64e` 且干净。当前 chat 与 floating UI 目录共 202 个文件。

- 视觉优先级：[UI 设计来源](../../doc-src/decisions/0003_ui_design_source_hierarchy.md)、[固定主题](../../doc-src/decisions/0007_professional_browser_theme.md) 与当前 `com.kiyori.design.theme`。
- 本机 Operit 源码观察 HEAD 为 `f323d6c50fa661837fad06d4618462861779b562`；具体组件对照记录文件与刻意差异。参考不覆盖已接受 Kiyori 产品行为。
- 保留已有对话统计、请求失败可见、未知提交禁止重发、历史工具闭合与来源保持导航契约。

## 阶段计划

2026-09-09 用户补充：右上角终端、对话详情、工作区，以及左抽屉扩展、工具箱、工作流、记忆库等全部纳入本轮；覆盖深度到最小子页面、弹窗、菜单与按钮。P6 按下列功能域独立推进，每域完成调用链盘点后补全叶子清单与问题证据，不能用一级页面审查代替子页覆盖。

| 子阶段 | 必须展开的范围 | 状态 |
| --- | --- | --- |
| P6a 终端与工作区 | 内嵌终端、会话、环境、SSH、键盘/粘贴/选择、工作区绑定/文件/预览/命令/导出及确认弹窗；子模块变更独立审查 | 主批次本地通过，剩余叶子反审 |
| P6b 对话详情 | 消息定位、审计事件、payload、修订、导出、统计与筛选弹层 | 首轮叶子实现及本地验证通过 |
| P6c 扩展 | 脚本、ToolPkg 插件、Skill、MCP 各标签；详情、配置、环境变量、导入/创建/删除/刷新；市场列表/详情/作者/管理/发布编辑及所有确认弹窗 | 多批本地通过；标记持久化补充中，registry并发等剩余反审 |
| P6d 工具箱 | 实际注册工具清单及各工具子页，包括默认助手、权限、Shell、工具测试、UI 调试、数据库、日志、STT/TTS、HTML 打包、FFmpeg 等实际可达入口 | 入口布局与导航首批实施，Shell等叶子继续 |
| P6e 工作流 | 列表、编辑器、节点/连接编辑、触发器、变量、运行/停止、日志、导入导出及确认弹窗 | 待实施 |
| P6f 记忆库 | 文件夹、文档、图谱、搜索、编辑、链接、导入、搜索设置/模拟/测试及删除确认 | 待实施 |
| P6g 助手与 AI 设置 | 模型/协议/参数、功能路由、提示词、角色/群组/用户偏好、上下文、工具权限、语音、对话历史、备份及其编辑与选择弹层 | 待实施 |

可达 UI 的逻辑修复属于本轮范围；实际安装扩展、部署 MCP、执行终端命令、发布市场内容、运行工具/工作流、调用模型或操作设备作为验收时，仍须遵守相应外部副作用授权边界。

各阶段按“发现 → 实现 → 差异反审 → 必要测试 → 串行 Debug 构建 → 核验 APK”推进；一个阶段形成完整可构建增量后再进入下个阶段。回滚边界是该阶段精确文件差异，不重置工作区、不删除未确认文件。最终统一审计提交，不把中间未验收状态发布为完成。

| 阶段 | 范围与主要文件 | 重点场景与验收 | 状态 |
| --- | --- | --- | --- |
| P0 覆盖与方案 | AIChatScreen、ChatViewModel、ChatServiceCore、设计 token、正式契约 | 明确入口、共享所有者、既有工作、基线和风险；持续登记源码证据 | 进行中 |
| P1 主对话与输入 | AgentChatInputSection、ClassicChatInputSection、FullscreenInputDialog、AttachmentPanel、PendingMessageQueuePanel | 首批输入策略、提交插件挂起隔离、编辑恢复、附件/队列可读性已通过测试与构建；后续叶子审查继续覆盖 | 首批本地验证通过 |
| P2 消息与弹层 | ChatScreenContent、ChatArea、消息菜单、编辑/分享/删除/回滚、ConversationDetailsScreen | 首批稳定身份、分享取消与编辑布局已验证；详情和剩余叶子继续审查 | 首批本地验证通过 |
| P3 历史与导航 | ChatHistorySelector、ChatHeader、角色选择、AI 模态抽屉 | 搜索错误、标题/正文匹配、查询隔离与分组输入已修复；其余动作继续审查 | 首批本地验证通过 |
| P4 小窗 | FloatingChatWindowScreen、FloatingChatWindowInputControls、FloatContext、窗口 owner | 草稿保持、IME owner、窄窗输入与组合焦点已验证；真实窗口交接待设备 | 首批本地验证通过 |
| P5 语音 | FloatingFullscreenScreen、BottomControlBar、EditPanel、FloatingFullscreenModeViewModel | 录音 owner、TTS 归属、准备防重和失败保持已验证；附件部分成功与其他共享消费者继续审查 | 首批本地验证通过 |
| P6 可达子页与总复查 | 工作区/电脑/详情、模型/记忆/工具设置弹层、相关设置入口 | 按 P6a–P6g 子阶段和叶子清单逐项闭合 | P6c 实施中，P6a 剩余反审继续 |
| P7 交付 | 正式契约、专项状态、候选提交 | 本地检查、最终 APK、精确暂存审计、候选链接与 fresh clone、推送及三方 ref 核对；现场边界明示 | 待实施 |

## 验收矩阵

[页面与弹层定位清单](01_surface_inventory.md)记录源码入口和初始状态；定位不等于完成审阅，阶段验收还必须检查行内弹层与按钮回调。

| 维度 | 必须覆盖的条件 | 证据要求 |
| --- | --- | --- |
| 页面层级 | AI 根、深层页、模态弹层、历史面板、小窗、全屏语音 | 源码入口与关闭链；设备 Back/焦点待验收 |
| 主题与布局 | 浅/深色、默认/个性化背景、窄屏、横屏、大字体、长标题 | 使用语义 token 与内容约束；实际视觉待设备 |
| 输入 | 空白、附件、中文组合、换行、Shift+Enter、长按、快速重复、回复、排队 | 生产策略边界测试与调用链检查；IME 待设备 |
| 消息生命周期 | 准备、连接、输出、工具、等待授权、停止、错误、完成 | 唯一 owner、无重复请求、失败可见；风险匹配测试 |
| 数据与异步 | 切聊天、分页、重命名、删除、回滚、搜索、离开再返回 | 稳定身份、旧回调隔离、确认目标一致 |
| 小窗与语音 | 焦点、键盘、附件、模式切换、录音取消、TTS 中断、退出 | 生命周期与清理代码；真实设备/服务待验收 |
| 可访问性 | 有意义的动作名、选中/禁用态、最小点击区、正文滚动 | Compose 语义与布局审查；TalkBack 待设备 |

## 问题登记

问题只有确认调用链与触发条件后才能列为缺陷；纯待查线索不写成已修复事实。

| 编号 | 证据与触发条件 | 处理与验证 | 状态 |
| --- | --- | --- | --- |
| I01 | Agent 与 Classic 在每个 Enter KeyDown 执行提交，未隔离重复事件与中文 composition | 共用 ChatEnterKeyPolicy/Handler；3 项边界测试与 P1 APK 通过 | 本地已验证 |
| I02 | 小窗 BottomInputBar 只在焦点变化时暂停唤醒监听，需核对离开组合后的释放 owner | P4 检查服务与其他输入入口，防止清理污染新 owner | 调查中 |
| I03 | 输入插件挂起时重复点击可再次派发，返回后未核对当前草稿；取消异常被吞掉 | 提交 Job 隔离、chat/正文/附件/回复快照校验、取消继续传播；P1 构建通过 | 本地已验证，插件现场待验收 |
| I04 | 多选、编辑和回滚确认保留显示索引，分页或删除前项会改变目标 | 多选与确认绑定时间戳；删除固定 chat 与目标；2 项分页/删除测试通过 | 本地已验证 |
| I05 | 分享预览生成脱离弹窗生命周期，参数改变期间旧图可能回写 | 消息快照、生成 Job、DisposableEffect 取消与发布前 ensureActive；长标签换行 | 本地已验证，截图效果待设备 |
| I06 | 工作区回滚 I/O 后按当时当前 chat 截断和重发，可能操作新聊天 | 操作前固定 chat 与消息，truncate 显式接受期望 chat 并返回结果，后续草稿操作再次校验 | 本地构建通过；跨系统场景待进一步验证 |
| I07 | 历史正文搜索异常被改写为空结果且记录原搜索词；查询变化仍短暂保留旧匹配 | 数据层传播异常，UI 保留明确错误；查询隔离结果，标题/正文并集，搜索期间可清空 | P3 本地构建通过 |
| I08 | 浮窗收起扩展编辑器时无条件清空草稿；扩展输入延迟焦点任务不随组合取消 | 保留草稿，焦点请求使用 LaunchedEffect；窄窗输入分为文字与动作两行，IME 不隐式停止生成 | P4 本地构建通过 |
| I09 | 主窗口和浮窗直接覆盖同一 IME 布尔值，旧组件关闭可能恢复仍需暂停的唤醒监听 | 前台服务汇总独立声明身份，退出仅释放自身；2 项交错生命周期测试通过 | P4 本地已验证 |
| I10 | 语音启动协程在交接延迟后仍可能启动，取消等待最终识别时不会撤销结果资格 | SpeechCaptureOperations 串行操作与调用身份；取消立即清状态，清理不依赖已取消 UI scope | P5 首批本地通过；剩余叶子继续审查 |
| I11 | 语音最终结果可能被旧 partial 覆盖；编辑框 IME Send 没有事件绑定 | 最终非空结果进入待发送正文；编辑面板补齐 IME 行为并使用共享浅深主题 | P5 首批本地通过；剩余叶子继续审查 |
| I12 | 浮窗服务关闭时另起无归属协程取消共享录音/TTS，旧关闭可干扰新窗口 | 组合显式释放，SpeechInteractionManager 按身份及串行区间释放；附件采集返回成功状态，准备失败保留正文 | P5 首次构建通过，交接补充测试中 |
| I13 | 工作区 saveFile 忽略工具 success，保存按钮立即清 dirty，保存后关闭未等待写入 | 按文件路径绑定保存快照，成功且正文未再改变才清 dirty；保存后关闭等待成功 | P6a 保存增量本地通过 |
| I14 | 工作区外部更新整表覆盖 openFiles，未保护 dirty；关闭非活动标签强制切换活动标签 | 仅更新仍未编辑的相同文件版本，关闭按路径保持活动文件 | P6a 保存增量本地通过 |
| I15 | 文件管理器创建/删除忽略工具结果且吞异常，刷新被自身 isLoading 拦截，删除菜单无确认 | 固定目录/环境目标、可见错误、成功后刷新与删除确认；新建校验文件名及重名 | P6a 本地验证通过 |
| I16 | 回滚使用当前分页窗口前驱；备份清单读取失败可作为空清单，恢复对象缺失被跳过，之后仍可能截断聊天 | 从持久历史取得边界；预览失败禁用确认；恢复失败阻断历史截断并保留备份 | P6a 本地验证通过；跨文件系统/SQL 无共同事务 |

## 验证与交付记录

2026-09-09 P1 首批：`testDebugUnitTest --tests '*ChatEnterKeyPolicyTest' --no-daemon --console=plain` 通过，3 项测试、0 失败、0 错误；`assembleDebug --no-daemon --console=plain` 通过（1m27s）。APK 483934515 bytes，`com.kiyori / 45 / 0.1.0`。文档检查 499 文件、0 问题。尚未提交或推送；设备和真实服务均为 `verification_pending`。

2026-09-09 P2 首批：输入策略与消息选择合计 5 项 JVM 测试通过；Debug 构建通过（3m17s），APK 483935107 bytes，SHA-256 `E21388B1C4FE6A53A4D93EDDDDC9662CCB544BC44C9579FF8B3FC019B01CFA04`。构建报告一处新增恒真条件警告，已在后续 P3 批次去除。文档 500 文件、0 问题。工作区前一消息边界与预览失败处理仍需 P6a 深查，不能把这批身份修复当作整个工作区完成。

2026-09-09 P3 首批：Debug 构建通过（2m33s），APK 483936291 bytes，SHA-256 `EA83AA60D7AF2B4030A74E9CBADF77D18F3BFEAF0C34EFE45EAC3E1AAE8476DE`；上一批新增恒真条件警告已消除。

2026-09-09 P4 首批：首轮验证发现不可变引用遗留 `@Volatile` 导致 kapt 非法 `final volatile`，修正后 2 项 IME 所有权测试通过；Debug 构建通过（1m36s），APK 483936291 bytes，SHA-256 `75357289AA798728B99B5D49BB4D707918EF133FF64CB88A0381CA8A6203FD3E`。不得以产物字节数相同认定内容未变化，应比较哈希。

2026-09-09 P5 首批：修正长按等待块的 Boolean 条件推断后，补齐新 owner 先启动、旧 owner 后释放的交接场景；4 项 SpeechCaptureOperations 测试通过，Debug 构建通过（2m5s），APK 486769109 bytes，SHA-256 `742C18E1259C0491815FA830CFCBD5339F2BF885C7853737F9D688981712339F`。附件准备、TTS 归属、真实录音与触控保留设备验证边界；P5 其余叶子审查仍须继续。

2026-09-09 P6a 保存增量：4 项 WorkspaceEditorState 测试通过，Debug 构建通过（3m24s），APK 495028151 bytes，SHA-256 `C1094B3CBE4C0D6E5A3282F25DC574CC7DF41C36E25B9A44C8E8FDDD9CC929A6`。覆盖写失败保留 dirty、保存期间继续编辑、关闭非活动标签、外部刷新保留编辑及不重开已关闭文件。文件浏览器、回滚和终端仍在后续增量中。

2026-09-09 P6a 文件浏览器增量：1 项 WorkspaceEntryName 测试通过，Debug 构建通过（5m58s），APK 495029939 bytes，SHA-256 `5179D6BF5E561113088CED5059B29BC28D7E11DCA525A23A29B117EFAB2EA710`。编译报告一处不必要类型转换，已在下一批去除。此批闭合失败反馈、刷新、删除确认、当前目录名称校验及工作区环境约束；书签授权与模板绑定的进一步审查仍进行中。

### P6a 继续审查记录

- `WorkspaceSetup` / `WorkspaceUtils`：ToolPkg 导入先删除已有同 chat 工作目录，内置模板拷贝覆盖同名文件且 IOException 只打印；解绑后再次创建可能损失旧内容。需增加明确的已有目录处理和失败保持，不得静默重置。
- `FileBrowser`：书签添加/删除仍需补异步失败状态；管理模式不应移除仍绑定工作区的目录授权。文件创建当前以二次存在性检查保护重名，但各文件 provider 的原子创建能力还需核对。
- `WorkspaceBackupManager`：恢复失败现在传播，但跨文件系统与 SQL 没有共同事务；部分写入或 SQL 失败的恢复边界需继续复查。不能用单元测试宣称原子回滚。
- `SSHConfigEditDialog`：端口和心跳参数以 `toIntOrNull() ?: 默认值` 静默替代错误输入，范围与反向隧道字段未完整验证；配置卡片“exit 回到本地”与 SSH 断线不落本地的当前契约不一致。终端尚未修改。

2026-09-09 P6a 回滚首批：4 项 WorkspaceRestorePlan 测试通过，Debug 构建通过（5m11s），APK 495031095 bytes，SHA-256 `0965BCE38B28066E0DD09DFD2F671068AF1F5A2E99CA3CB405DDD4BC25D4B5A9`。下一批将前驱定位收敛为单条元数据 SQL（已用 SQLite 验证 5 个场景）、补充无效哈希拒绝与删除已不存在目标的幂等性；工作区创建使用 staging，旧目录提供重新绑定入口，测试和构建进行中。

### 仍需闭合的状态与覆盖

- 工作区编辑状态已由既有 ChatViewModel 按路径与环境持有，WorkspaceScreen 按 chat/工作区隔离组合；页面切换保留内存正文，不承诺进程重建持久化。重命名/解绑后的缓存生命周期与回滚交互继续审查。
- 创建完成时的回调、选择目录授权与当前 chat 的代际绑定需复查；创建失败保留原目录，不能把 UI 错误提示误作模板完整性证明。
- P6b 入口为 `ui/features/chat/details/ConversationDetailsScreen.kt`：时间线 payload、筛选、全部展开、原始 JSON、修订编辑、批注、导出均需逐项追踪；初读发现批注/修订在异步结果前关闭，确认期间的生成状态也需复核。
- 终端 SettingsTheme 固定深色，实际设置/SSH/键盘编辑子页共用它；主题改进应与宿主 MaterialTheme 对齐，保留原生终端画布颜色配置与 IPC/runtime 所有权。

2026-09-09 P6a 创建与引用增量：WorkspaceCreation 3 项、SpeechCaptureOperations 5 项通过，Debug 构建通过（5m1s），APK SHA-256 `B847A25DF88220A2CE51D67486FDE9F346716BD0439C6A7C1C6D89F674C2C9FB`。后续工作区编辑状态持有、书签写入等待/错误/移除确认与绑定引用保护通过 Debug 构建（7m45s），APK 483945515 bytes，SHA-256 `0B314272D48E788E32779E65F37DC336EA855AD969088C1F006108A9F9D80F5D`。这些构建均未操作设备。

P6a 回滚提交顺序继续修复：恢复先返回待清理清单，历史 SQL 成功后清理；SQL 失败保留备份依据。未保存或保存中的编辑阻止回滚，恢复中隔离编辑，结束后失效旧磁盘快照。新增 3 项编辑状态边界测试，测试与构建进行中。文件系统与 SQL 仍不是共同事务，部分 I/O 失败、取消和进程中断不得标记为原子回滚。

P6b 已确认待修：详情页 BackHandler 未接收宿主可见性；批注/修订在异步写入前清空输入；切 chat 未隔离页面临时状态；时间线和 Raw 搜索无匹配时隐藏“加载更早事件”；payload 失败显示堆栈且无明确重试；打开详情及旧分页加载的迟到回调需核对目标身份。

2026-09-09 P6a 回滚提交顺序增量：首次构建发现编辑状态引用早于声明，修正后 7 项 WorkspaceEditorState 测试通过，Debug 构建通过（14m49s）。APK 483947287 bytes，SHA-256 `7056E396BE1D0F0AAED1350610ACB03DD38624E45153828136E2529D2C7AA593`。恢复结束关闭的是已保存标签，未保存内容提前阻止操作；跨系统取消与进程中断仍不具有共同事务。

P6b 实施增量：三个标签页的搜索/列表位置按页保存，聊天身份及宿主 Back 隔离；批注/修订/导出等待结果，失败留正文与局部提示；修订仓储检查打开时原文；payload 读取绑定可见行并释放；分页无匹配仍可继续加载，旧分页清理核对代次；首次/实时加载失败提供重载。审计导出改为独立 staging，失败清理且不覆盖已有结果，文件已生成但完成记录失败明确提供路径。新增 3 项导出文件行为测试；本批测试与构建进行中。

终端继续调查确认：`SSHConfigManager.parseConfig/toJson` 没有读写 `enableKeepAlive/keepAliveInterval` 与三项正向端口转发配置，UI 设置在重载后会恢复默认；SSH 编辑器又重建整个对象，未保留不可见字段。心跳转换到毫秒的乘法使用 Int，范围验证必须防溢出。`SettingsScreen` 两个“前往环境配置”按钮实际只回终端首页，需接入现有 setup 路由。`SourceManager` 将源名称/URL 原样嵌入 Shell 字符串，需统一引用和输入校验；这些缺陷尚未修改。

2026-09-09 P6b：9 个 ConversationAudit 测试套件共 19 项通过（含 3 项导出文件失败/成功/重名测试），Debug 构建通过（8m49s）。APK 483951487 bytes，SHA-256 `BBC38893276F219F029C3CFA3D20CA4D74581590EFC86DBD86E0C00572E81EFA`。编译提示一处新 helper 的 parentFile 可空访问，已在终端增量开始前改为显式检查绝对目标父目录，将随下一次构建复核。终端现在开始实施，子模块尚未提交。

2026-09-09 终端首批：设置/SSH/键盘配置页消费宿主主题；缺工具提示接入 setup；SSH 心跳及正向转发字段补齐持久化，表单保留未暴露字段并校验端口、心跳毫秒溢出、认证及反向隧道；保存/删除等待结果。字号保留小数，字号与 FPS 显示范围错误并禁用无效确认。`:terminal:testDebugUnitTest :terminal:assembleDebug` 通过（1m47s），73 项中 71 项执行通过、2 项因未配置 `KIYORI_PTY_WSL_DISTRO` 跳过，无失败或错误；新增 SSH 表单测试 5 项通过。父 APK 构建正在进行。

终端软件源下一增量：已核实本地启动脚本在 `initializeEnvironment` 后使用 `configure_sources`；SettingsViewModel 的可空管理器向 `default` 发命令没有可靠实际目标。应保留正式启动配置 owner、明确新本地会话/安装的生效时机，清除失效派发；自定义源需 URL/配置文本/Shell 三层边界校验及引用、删除确认和等待保存。不得把配置命令发送到未知交互会话或 SSH 主机。

构建期间预查 P6c：`PackageEnvironmentVariablesSheet` 以 `remember(variableKeys, currentValues)` 重置编辑值，外部刷新可能覆盖正在输入的草稿；保存回调同步调用全局 `setAllEnv` 和逐个 host `setValue`，随后关闭。后续需检查仅提交变更键、并发原值校验、关联配置批次、失败/取消反馈及作用域隔离。当前仅记录源码事实，扩展模块尚未修改。

2026-09-09 终端首批父 APK 已通过（6m10s），483958531 bytes，SHA-256 `D261EB29E4F3913878A00D3F58D9ED579C3A934402A712927755B8C994496191`。后续软件源/环境主题/草稿保持增量模块测试与构建通过（1m39s）；随后的认证 URL 兼容、列表脱敏与环境全选投影修正再次进行终端验证。新软件源策略已测试 URL 和 Shell 载荷，不代表真实安装或 SSH 验收。

终端重置/FTP 后续根因队列：`SettingsScreen` 未消费 `isClearingCache`，可重复确认重置并同时启停 FTP；`getCacheSize` 的旧取消 finally 可清新 Job；`clearCache` 忽略 FTP 停止失败。更底层 `CacheManager.deleteRecursivelyNoFollow/deleteBinLinks` 吞删除异常，`readMountPoints` 失败当空列表，存在错误成功提示；`TerminalManager.prepareForMaintenance` 未清理 `isEnvInitialized`，清目录后下一次初始化可能只刷新脚本而漏建环境。需沿 provider disconnect/start/hidden-exec 的互斥边界完整修复，不能只禁用按钮便声称重置闭合。当前这些根因尚未修改。

2026-09-09 软件源与环境 UI 增量最终本地证据：终端测试 78 项中 76 执行通过、2 WSL 跳过；父 APK 构建通过（4m38s），483961911 bytes，SHA-256 `03751F32973E0B8F659C8150479E0968FA7F88073A7C481A8E50DA61234FAA52`。认证 URL 兼容、列表脱敏、环境全选派生投影均包含在此产物中。

终端维护增量正在验证：新增 TerminalEnvironmentOperations，围绕原 TerminalManager 注册初始化/provider/会话启动/隐藏执行；维护取消并等待这些操作，有界失败阻断删除，清理后失效初始化状态。现有 provider 关闭进程等待退出，隐藏进程确认退出前保留登记；CacheManager 的挂载读取与 no-follow 删除失败传播。FTP 单例共用启停/维护互斥区，UI 防重复和冲突动作。新增维护取消/拒绝/失败/超时 4 项、文件删除成功/失败 2 项测试；真实进程、挂载、FTP、并发外部文件访问仍待设备验证，文件系统清理不是可回滚事务。

2026-09-09 终端维护补充后：86 项测试中 84 执行通过、2 WSL 跳过（新增维护测试共 6 项、文件清理 2 项），父 APK 构建通过（5m25s），483962663 bytes，SHA-256 `A85C65261DDBC82742B2FD73BBB43EC9048E0D138B75CBEB8E34878963A480D1`。文档复查 501 文件、0 问题。字体/键盘下一增量现在已实现并进行模块验证：像素单位、保存等待/失败保持、文件加载失败可见、键盘原布局冲突检查；此批尚未包含在该 APK。

终端 SSH 连接应用待闭合：`setSSHEnabled` 只改偏好；`createNewSession` 依偏好标记类型，而 `getTerminalProvider` 缓存首次 provider，可能出现 SSH 标签配本地运行目标。应让会话身份来自实际 provider，应用连接更改时经明确确认停止现有会话并释放原 provider，复用已有维护边界。`SSHConfigManager.getConfig` 的损坏 JSON 返回 null 可使启用 SSH 的路径落到本地，需显式失败及可恢复的配置 UI；当前尚未修改这些根因。

2026-09-09 字体/键盘增量：终端 86 项测试中 84 执行通过、2 WSL 跳过；父构建通过（4m34s），APK 483964983 bytes，SHA-256 `5844A36ED7A39CD408E05BD5FD061A335DD5CB7B129A7E072763FAE2E891CCE0`。新增保存等待、像素单位、字体读取失败提示与键盘布局冲突保护已包含该产物。

终端 SSH 与人工输入增量正在验证：会话类型由实际 provider 派生，连接偏好仅在明确确认应用后经既有维护边界重连；配置损坏明确失败并提供重载/移除入口，开关等待持久化。创建会话纳入维护取消并移除脱离创建的启动协程；会话被移除时停止等待。人工命令、直接输入和中断固定触发时会话，命令提交失败保留正文并提示，新增会话防重复。真实 SSH、进程/网络中断及输入交错保留设备验证边界；模块验证会话 50273 运行中。

2026-09-09 终端 SSH/输入增量：模块测试86项中84通过、2 WSL跳过，父 APK 通过（5m29s），483972939 bytes，SHA-256 `B6D05B123F6FD68001A6C631F50D02849F3FA9472E9844742A0DB2A2C4FA8C93`。字体/键盘、维护、连接应用与人工输入目标修复均已包含此产物。

工作区命令下一增量已实施待验证：引用绝对目录并以同次 Shell 的目录成功条件阻止错误目录执行；共享结果/取消固定 commandId、呈现退出码/未确认结果；输出限制2000条并明确省略数量，查看旧输出时不强行跟随。专属启动防重、等待实际写入及切chat后的面板隔离。终端原始输入、输出块和命令正文调试日志移除，保留事件身份。工作区绑定/解绑、工具按钮、workingDir契约、配置编辑和导出仍待后续审查，不能把此批当作工作区完成。

工作区后续根因确认：绑定/解绑在 ChatHistoryDelegate 内另起协程，界面回调不等待数据库；ViewModel 与 WorkspaceManager 同时修改 LocalWebServer，且无工作区时旧根目录未清理。服务器磁盘静态文件与文件列表的 canonical 路径检查仅 startsWith 根目录字符串，相邻同名前缀目录可误判内部。下一增量需闭合持久化等待/原绑定冲突、单例目标协调及路径边界，避免仅修改弹窗反馈。

2026-09-09 工作区命令测试：6 项 WorkspaceShellCommand/WorkspaceCommandExecutionState 测试通过；终端86项中84通过、2 WSL跳过，文档501文件0问题。父 APK 会话2850正在构建，尚未取得最终产物证据。

2026-09-09 工作区命令父 APK 通过（3m28s），483975235 bytes，SHA-256 `181B212B30189645049AE9B35D9BF057411B9C2F258FE086D35C1508CEFD8EAA`。后续绑定/预览增量已实施并准备验证：绑定与解绑等待、SQL 原值比较、未保存/保存中阻止解绑，服务器更新集中协调和逐请求路径/环境快照，canonical 边界拒绝相邻目录，预览不隐式创建已不存在路径。实际 DAO 查询已在 SQLite 验证6个绑定/冲突/环境/删除场景；新增2项文件边界测试待运行。聊天与服务器不构成共同事务，服务器失败单独可见；真实 WebView 请求交错仍待设备。

2026-09-09 绑定/边界测试2项通过（5m56s），编译两处冗余表达式已去除；同属 P6a 的导出闭环合并到下一份 APK。导出增量：包名/版本号从现值校验、保留 beta 字母与无效草稿、图标通过 Coil 异步加载；临时产物完整后发布且不覆盖、进度不倒退、主线程回调及取消隔离。AIChatScreen/ChatScreenContent 固定导出目录并按聊天管理生命周期；共用 HTML 打包器补实际取消、输入快照、独立临时目录、取消后清理及 SAF 复制失败传播。新增4项验证/发布测试待运行；真实 APK签名导出、SAF/cropper/provider I/O与设备现场仍为 verification_pending。

2026-09-09 导出4项测试通过（5m14s），无新增编译警告。绑定持久化与内存投影继续保留原服务 owner，通过 await 向 UI 返回结果，页面离开只取消等待。父 APK 会话82578正在构建。

下一轮反审队列：LocalTerminalProvider 的隐藏执行日志仍包含命令首行和输出正文；ExportDialogs.copyDirectory 与 ApkReverseEngineer.addWebContentToZip 尚需补齐符号链接越界/循环及读取失败边界。终端首次初始化在实际 provider 创建前运行，需核查未出现会话时的准备/失败提示。FileBrowser 初次读取失败仍可点击绑定，需让目录可绑定资格来自成功读取。HTML 打包器首次目录读取仍使用主线程 DocumentFile.listFiles，另在 P6d 继续闭合。上述条目均未标记完成。

2026-09-09 绑定/预览/导出父 APK 已通过（6m20s），483977915 bytes，SHA-256 `7B27451A6F1AD645F1E850E37247466BA2B4F44A5ED146E8F9720C9FD70BF034`。后续导出树与 ZIP 路径测试通过（4m28s）：越界链接、目录循环、内部别名、空目录、取消传播和模板路径边界；`resolveZipEntryTarget` 编辑误删已按 HEAD 原实现恢复。结果弹窗改为语义主题、滚动与文本选择；尚未包含在该 APK。

P6a 下一增量待验证：目录绑定须有读取成功依据；工作区命令使用 workingDir，拒绝无 argv 合同的 shell=false，Android 本地路径拒绝投向 SSH 会话；工具按钮固定环境并防重复、取消继续传播。默认终端准备过程由原 TerminalManager 发布状态，首次失败提供重新准备/设置入口。LocalTerminalProvider 隐藏执行命令及输出正文不再进入日志。Linux 文件环境与独立 SSH 文件连接的目标匹配、配置加载错误及其余叶子继续反审，尚未关闭 P6a。

2026-09-09 终端准备/工作区参数增量通过：终端86项中84通过、2 WSL跳过，工作区命令4项通过；父 APK 通过（4m23s），483979787 bytes，SHA-256 `397C3CE4660C2BF54B8BEBCCF409BDB7C7BEC67C80147C89A55C227BC9D2A423`。导出树、空目录、取消、结果弹窗主题和目录绑定资格包含在此产物。

下一增量正在验证：人工输入按触发顺序等待，不依赖 IO 调度，提交后清正文核对草稿修订号；PDF页失败呈现终态、渲染最长边2048px，非本地预览独立临时文件随组合释放，HTML转换临时文件始终清理。编辑器复用既有LightTheme/DarkTheme跟随宿主，语言切换不覆盖主题；补全菜单采用窗口坐标并限制可用边界，符号栏和补全行48dp，新增输入顺序2项和弹窗定位3项测试。WorkspaceManager后台预览Back复核已有可见性投影，重命名冻结输入并检查saving/restoring，取消继续传播。配置加载与远端文件环境等剩余问题尚未闭合。

2026-09-09 输入/预览/编辑器增量测试通过（5m3s）：终端88项中86执行通过、2 WSL跳过；补全定位3项通过。测试后补充文件工具调用移出主线程，Linux/书签预览更新使用本次刷新版本；代码编译由随后父APK确认。

2026-09-09 输入/预览/编辑器及文件IO父APK通过（6m50s），483980831 bytes，SHA-256 `069D1B93ED18A77649A5FBB27C4B5F161248613D0674AEF407567C189ABAC080`。

配置与环境增量正在验证：工作区配置只有实际不存在时使用既有默认值，损坏/失效链接/读取失败传播且解析异常不带原文入日志；页面异步加载、错误重载与文件管理入口、保存配置后刷新；失败停止预览服务但仍可打开工作区修正。Linux配置通过实际文件provider读取并保留借用身份，命令检查当前文件provider与终端provider一致；会话缓存和结果按路径/环境区分。工具按钮的Linux路径参数必须明确匹配environment，不猜测任意工具的环境合同。ChatServiceCore捕获watcher创建失败并显示一次错误，保持历史观察器存活。新增4项配置读取测试，会话75198运行中。

2026-09-09 配置4项与命令状态3项测试通过（2m6s）。构建前补充工作区动作菜单：保留可拖动FAB，按56dp真实按钮与当前可用窗口约束位置，拖动结束才持久化；操作列表使用标准可滚动DropdownMenu，撤销/重做消费当前编辑器状态。文件标签48dp、关闭区40dp、选中语义与明确关闭目标；没有启用服务器且没有预览地址时显示文件入口，不请求空的localhost。非本地工作区禁用只接受Android File的导出入口，避免导出同名手机目录。本批父APK随后验证。

2026-09-09 P6a 配置/菜单增量：构建先因嵌套作用域的 `maxHeight` 隐式 receiver 失败，提取 `menuMaxHeight` 后配置及命令状态 7 项、路径与导出树 5 项测试通过。父 APK 构建通过（1m23s），483985439 bytes，SHA-256 `8916958900F9392155A11FFB792C08EFFBC5BF9A1013390DF51D9151476E2539`。后台工作区弹窗/菜单与模板选择受可见性约束；本地 canonical 与虚拟规范化路径均检查工作区边界；导出改用显式栈并保留祖先循环检查。此产物尚不含以下 P6c 增量。

P6c 首批正在验证：环境变量表单固定打开时字段与原值，只提交变更键，现有两个偏好仓储各自锁内校验与批次 commit，冲突/失败保留输入，部分保存后可重试同一目标。外部刷新不重置草稿；退出/跳转先确认未保存内容；保存中禁用写入和关闭，底部按钮可换行；敏感字段及默认值不明文展示。两个偏好文件之间没有共同事务，部分完成文案明确说明。共享底部抽屉增加关闭确认回调，拒绝手势关闭时回到部分展开位置。终端 SSH/OpenSSH 缺失说明同时补滚动与文本选择。

2026-09-09 P6c 环境配置首批：4 项冲突/重试校验、2 项变更键投影和9项既有分类策略测试通过；父 APK 构建通过（1m53s），485966551 bytes，SHA-256 `FA2F1B7077AD6F5EB02AF4830325A93CC88E40DF1FFF3578C2B3047C75C20EC0`。文档 501 文件、0 问题。终端缺工具/OpenSSH 弹窗滚动与文本选择已包含该产物。

P6c 详情/脚本下一增量实现并构建中：详情读取错误不显示为空工具，提供重载；删除固定包身份、等待、防重并保留失败确认；子包开关串行 UI 操作、成功读取后发布，工具详情先显示加载态且关闭取消旧读取；脚本固定点击时正文/参数、Main 发布状态，执行中关闭先确认，调用者取消不写成普通失败。此批仅闭合该界面的重复操作和迟到读取，底层 PackageManager 启用/禁用跨入口的读改写互斥与两个启用字段批次持久化仍需专项核对，不能宣称整个 registry 并发问题已解决。

P6c 详情首批构建通过（2m22s），随后反审继续：默认工具改从原始元数据读取，防止将当前状态合并结果再次作为继承基底；工具列表 remember 依赖实际包元数据。脚本父页每次回传最近结果曾重置整个结果列表，已改为仅初始化时读取。加载错误弹窗新增固定源文件确认、等待、防重与局部错误，成功删除后单独刷新目录。环境枚举/布尔选项保留标准单选行为，使用独立清除动作。这些补充处于下一轮构建，不能使用前一产物代替。

2026-09-09 P6c 详情/执行补充：父 APK 构建通过（2m22s），485981883 bytes，SHA-256 `1194D267338C9BF3E72C9506F9117A21E35D4A7ADC4833FF84A1C3061862A9CE`。源文件确认、默认工具基底与流结果回传修复已包含。随后开始 Skill 详情：目录预览显式栈、不跟随链接、取消检查与失败终态；详情读取随可见目标取消，删除固定目录并等待，列表刷新串行且失败保留原列表。4 项目录预览测试进行中，详情长说明滚动与 Tab 索引约束也在此批。

Skill 导入进一步确认待修：UI 用外部 DISPLAY_NAME 直接构造 cacheDir 下文件，可越界或覆盖同名缓存；ZIP 元数据 name 直接拼接最终技能目录，缺少单目录名/根边界校验；ZIP 导入 copyRecursively 失败可能残留最终目录；手动导入在 SkillManager mutationLock 之外提前创建最终目录，失败按路径递归清理。应复用 SkillManager 作为唯一发布 owner，完整 staging 后发布，详细结果决定是否关闭表单，保留既有字符串兼容入口；当前仅完成调查，尚未实施这些导入修复。

2026-09-09 Skill 目录预览4项测试全部执行通过（无跳过）。随后 Skill 导入边界增量已实现，测试进行中：新增 SkillImportFiles 单目录名/root检查、完整目录移动发布、no-follow删除；ZIP与手动输入复用SkillManager锁；Repository提供详细结果并保留原字符串入口；UI冻结输入、临时随机文件、失败不关闭。GitHub原下载池保留，池外临时下载文件隔离。当前源码尚未生成新APK；既有 SkillManagerLifecycleContractTest 因委托签名新增参数需要按实际锁边界同步断言，下一批重验。

2026-09-09 Skill 文件发布4项及目录预览4项测试全部通过，均无跳过。反审后移除新增冗余safe-call警告、同步原有锁契约测试、补导入弹窗滚动/48dp标签/执行中禁用切换，并将多个候选技能改为明确歧义错误。当前再次进行9项相关验证，完成后仍须生成包含这些源码的父APK；最新已核验APK仍为详情批次 `1194D267`。

2026-09-09 Skill 本批收口：9项测试全部执行通过，无失败/错误/跳过；Debug APK构建通过（1m23s），483997411 bytes，SHA-256 `5F219B569BC945E4F5E2A99975ED2C6CA3798A6BC3196705B45E7F442DE73ADC`。文档501文件零问题，父/terminal差异空白检查通过。真实SAF、网络下载、设备文件权限、IME和取消时外部I/O仍待设备；当前没有执行真实技能导入。

P6c MCP待修已确认：远程编辑立即启动ViewModel任务并关闭/显示成功；MCPRepository的新增日志包含bearerToken前10字符；MCPLocalServer.saveMCPConfig吞写失败，多个配置更新先发布内存再无锁writeText；savePluginConfig以异常从完整格式切到单服务格式，完整格式缺目标可被错误重解；部署命令状态未绑定pluginId，UI以列表非空判断可复用，旧命令可跨目标保留。MCP环境变量与命令编辑被外部StateFlow刷新重置草稿。以上尚未写入MCP源码。

MCP 解析/日志首批已实施并测试中：新增显式完整/单服务格式解析，完整格式缺目标或同时含单服务 command 不再重新解释；command、参数、disabled 和环境变量字段检查真实 JSON 类型，解析异常不携带原文/cause。移除配置预览、命令/参数和 bearerToken 前缀日志。接下来配置写入将在既有 MCPLocalServer 内复用一个串行临界区，先完整写入/发布文件再发布 StateFlow；要覆盖 reload、merge、启停、元数据、导入与清理各写入口，不能只包住 saveMCPConfig。MCP源码当前尚未实施该存储改造。

MCP 配置与编辑增量已实施，验证中：同目录完整文件原子发布、失败保留原文、配置锁覆盖所有读改写与 reload；加载失败状态进入页面并提供重载。远程新增/编辑等待保存，字段合并保留并发启停和安装信息，同字段冲突保留草稿；校验 HTTP(S) 端点、唯一 Header 名称和单行值，认证字段隐藏。详情配置固定可见插件，正文直接传入保存，校验原配置、未保存关闭确认、固定目标卸载确认。首轮41093因引用不存在的retry字符串编译失败，已改已有mcp_retry；44762正在测试。部署命令、安装导入、市场及其余MCP叶子继续反审，不能标记整个MCP完成。

2026-09-09 MCP 配置增量：44762的11项测试全部通过；补充未部署插件原配置不存在与规范化重试后，52885的12项测试全部执行通过（1m29s），无失败/错误/跳过。文档501文件0问题，父与terminal差异空白检查通过。当前开始生成包含MCP改造的父APK。下一增量审查确认：MCPRepository每个实例创建无归属观察协程，页面手动创建ViewModel无清理；部署缓存未绑定pluginId，环境变量与命令编辑刷新覆盖草稿；底层部署目录/命令结果与取消清理还需核对。

2026-09-09 MCP 配置父 APK 已通过（1m20s），484001003 bytes，SHA-256 `55C0E956A1CA902F3777987E334F7799FCD1C9F93AD3674D9FF580119F9E2BD2`。部署与生命周期下一增量4503测试中：命令请求绑定目标/代次，默认部署重新分析，失败重试和草稿初始化分开；真正的ViewModelOwner持有页面操作，仓库观察随scope清理。进度滚动/输出上限、环境变量防重复与敏感值隐藏；底层目录检查退出码、同次命令目录条件、取消释放会话、拒绝部署配置歧义。没有执行真实部署；详情确认弹层层级、安装/卸载与其余MCP边界继续反审。

2026-09-09 MCP 部署6项测试通过（1m47s）：目标切换、迟到/取消、失败清旧结果及配置/路径输入。随后详情布局改为按实际高度测量底栏、标准48dp标签语义、长说明呈现，未保存远程编辑关闭确认；这些补充由当前父APK验证。继续确认ZIP直接拼接entryName未检查根边界，安装下载前删除旧目录且固定缓存名，卸载按未验证pluginId删除；下一增量处理这些根因，尚未实施。

2026-09-09 MCP 安装文件：48806首轮5项通过，补保留配置文件名/非目录目标拒绝、移除可空parent编译警告和下载清理后，9702的6项文件测试通过（1m30s）。89163的3项控制器测试全部执行通过（33s，无跳过）：重复提交、卸载错误身份与重试、固定ZIP URI。导入等待时禁用输入、认证值隐藏、48dp标签和空下载清理已补，28976正在构建。最新已核验APK仍为部署/详情批次7C9736BF。

2026-09-09 MCP 目录/工具增量：48985项目目录3项测试通过（1m32s），明确已发布路径优先、旧目录唯一候选和依赖/边界行为。随后配置合并改为完整类型预检、失败不带原文，64438解析6项测试通过（1m39s）；ZIP选择统一Compose Activity Result，移除双启动及手工注册回调。配置正文滚动、图标真实异步加载、长标题/说明和已有环境变量编辑已补，38712父APK构建中。

2026-09-09 MCP目录/工具/选择器与布局父APK通过（2m10s），484004851 bytes，SHA-256 `542FB54941ADB8C728E3182DB8A160DCD98E87D988B941F7051F5159E3955C70`。随后发现reload已改为传播异常，但页面首次refresh缺少catch；已补可见刷新错误，统计仅计启用插件，开关保存防重、后台弹层可见性和部署确认滚动。22146构建通过（2m9s），同大小，SHA-256 `9D05F136B41EED069A7D90C21C86FDDED8B6F33E91010986414994D6DCE78A65`，产物时间09:33:41。MCP运行时SSH环境匹配仍为剩余反审项，扩展市场继续展开。

P6c市场安装首批正在53864验证：Skill更新移除下载前删除，锁内检验目标/来源、完整目录与标记发布；MCP市场资料改为锁内合并并保留生命周期字段。安装状态无论取消、刷新失败或scope已关闭均随Job完成释放。市场详情评论错误清空/立即关闭、脚本更新先删除、外部下载文件名和分页代次问题已确认，下一增量继续处理；整体Goal保持active，全部未提交推送。

2026-09-09 市场安装首批53864的9项测试全部执行通过（1m50s），无失败/错误/跳过。随后移除MCP资料合并一处冗余可空表达式；评论新增/编辑明确成功才关闭，失败正文与错误留在弹窗，未保存退出确认、删除固定目标确认、点赞成功选中、同步防重与评论读取代次已实施。评论4项行为测试90627进行中，尚未构建包含市场增量的APK。

2026-09-09 评论90627的4项测试通过（1m33s）；移除重复状态释放并补下载缓存隔离、失败/取消清理后，8887的评论4项与文件3项全部执行通过（1m32s），无失败/错误/跳过。市场安装投影排除Skill staging/backup与符号链接目录，MCP开关回调缩进已复核修正。文档501文件0问题；35413正在生成本批Debug APK。脚本更新、分页及其他市场叶子仍未闭合。

2026-09-09 市场安装/评论/下载父APK35413通过（2m20s），484007083 bytes，SHA-256 `2180957988F8E689BFF99B077E45F302EDC14EF078569CC213BC2802431CACEA`，产物时间09:57:25。下一增量44661测试中：Browse刷新/排序与分页同步占位、请求代次及取消隔离，本地安装投影只发布匹配当前列表的结果；翻页失败保留明确重试入口，搜索说明仅筛选已加载条目且可继续加载；分类真实加载/空/错误终态，分类Tab和排序筛选控件48dp。页面通知/协议/发布/管理与脚本更新继续反审。

2026-09-09 Browse44661的3项测试全部执行通过（1m34s）。补充实际LazyColumn索引的分页触发、排序回顶部和搜索Back后，40812父APK通过（2m13s），484008623 bytes，SHA-256 `B074B41E11A35D208495B8F31F43AC2B4873CE52629018C49D5E4EDF2740C7E4`，产物时间10:05:18。文档501文件0问题，父与terminal差异空白检查通过。

作者/通知增量86548测试中：作者列表与详情打开分离状态，同步防重、失败可重载、离页取消待跳转；通知刷新有终态/错误入口，通知全文滚动与文字选择、固定通知打开条目，原Screen路由闭合；通知类别/相对时间使用本地化资源/平台日期，市场首页协议/发布选择/登录弹层受可见性约束。当前源码尚未生成新APK；账号切换下的市场缓存与请求隔离、管理/发布、脚本registry更新、标记原子持久化及其余叶子继续反审。

2026-09-09 作者/通知86548因新测试夹具缺少relation字段在测试编译阶段失败，源码编译通过；修正后7993的作者2项与Browse/通知4项测试全部执行通过（1m1s）。进一步确认MarketStatsApiService的进程级marketSession不核对当前账号，已改为凭据指纹绑定的唯一缓存，每次使用核验登录/过期，认证完成重验身份，不重发业务请求；通知页账号变化清空列表、关闭全文并取消旧读取。2665的认证4项和Browse/通知4项测试随后全部通过（1m40s），27971父APK通过（1m21s）。

2026-09-09 市场管理43822与补充后的98391测试通过，后者2项全部执行，无失败/错误/跳过。管理加载绑定账号，隐藏分类不自动请求；撤回失败保留确认与目标，等待禁止重复，切号清旧列表，离页取消待跳转。58969父APK通过（1m10s），484010643 bytes，时间10:29:59，SHA-256 `7E84FFC16E3E0AA55FE841F7415FC20F8B2548E787CDE37F0BE273B180B57E19`。没有执行真实撤回。Repo发布增量正在实施：原ViewModel唯一提交状态、调用时快照与防重、取消传播、草稿旋转恢复和未保存返回保护、贡献者描述权限、分类重载与滚动确认；测试和新APK待验证，Artifact发布及安装registry等剩余队列不变。

2026-09-09 Repo发布首轮97343因在非协程调用挂起的账号读取编译失败，移入原提交协程后源码通过；60988的一项测试因Mockito对Kotlin Result的thenAnswer装箱及Android日志桩失败，改用类型确定的thenReturn后12914的2项测试全部通过（38s，无失败/错误/跳过）。67156父APK通过（53s），484011263 bytes，时间10:43:31，SHA-256 `B129271730D853367C6BCED522F255CFD14C90ED975E49A95728E411C248ABC7`。Artifact发布下一增量45521测试中：同步占位、错误与取消终态，仓库地址变更清旧Release/资产并取消读取；表单等待禁用、后台弹层约束、滚动确认、未保存返回、本地制品读取错误与重载。上传后登记失败的精确恢复、旧Release附件发布及其余反审仍未闭合。

2026-09-09 Artifact目录45521的2项测试通过（1m29s）。补成功返回与登记快照后，29809的目录2项及恢复3项全部通过（4m2s）：同内容复用、不同内容保留旧资产、原账号仅登记不调用GitHub上传。随后补请求结果边界：市场HTTP类型明确区分拒绝，写请求不自动重定向或连接重试，下载统计保持单独语义；未知登记禁止重发，关闭说明不解除当前页占位，原账号不匹配但尚未发送可在恢复账号后重试。39942正在验证恢复与HTTP分类5项。仍未生成包含Artifact批次的新APK，且跨进程未知提交恢复、完整账号生命周期、其余发布叶子与registry更新须继续反审。

2026-09-09 39942的恢复3项/HTTP分类2项全部通过（1m52s）。补旧Forge弹窗关闭不重置活动提交、服务完成进度由ViewModel接收结果后发布、待确认登记时锁定表单后，74238的目录/状态3项全部执行通过（1m31s）。41429父APK构建中；文档501文件0问题，相关差异空白检查通过。下一安装增量已确认：`findPackageFile`仅扫描JS，TS/HJSON来源投影、发布和删除缺失；普通导入仅按包名查重却按文件名覆盖目标，可能覆盖同文件名的另一包；市场更新仍先删除。须在原PackageManager与PackageScanPublicationGate内闭合文件准备、原目标校验、发布与registry状态，不能把ToolPkg引擎销毁放进initLock。工具箱卡片延迟导航可重复排队和固定高度大字体风险已定位，尚未修改。

2026-09-09 发布批次41429父APK通过（6m12s），484014115 bytes，时间11:12:38，SHA-256 `2FC817820786D3B4ECA5CF870D354F2E512A74156001ACB87A233C13FC313E40`。这是该批已核验产物，未安装或执行真实发布。安装下一增量确认`parsePackageCandidate`只接纳JS/ToolPkg，TS/HJSON导入后刷新或重启会从registry消失；应同步修复来源定位、扫描格式、唯一目标、同文件名冲突与原子发布。整体Goal active，P6d–P6g与前序剩余叶子继续，父仓库和terminal均未暂存、提交或推送。

2026-09-09 普通脚本安装增量已实施：独立完整文件准备、同目录原子替换与失败恢复，在原registry发布锁内核验声明身份、旧SHA和文件名冲突；市场更新移除先删除。扫描/来源定位覆盖既有JS/TS/HJSON，来源索引每次投影只解析一次文件集合；没有新增TS编译器。62165与66021各12项测试通过。随后复用原环境变量校验，已激活脚本更新缺必填配置时保留安装与启用意图、撤去活动工具；类型化已安装结果让市场先记录来源并刷新，再显示需配置原因。环境校验抽取、部分完成反馈和跨扩展名失败恢复补充仍待本轮验证；最新APK仍为41429。普通启停/删除并发、ToolPkg状态发布、市场标记与其余扩展叶子仍需反审。

2026-09-09 安装补充93165的13项测试全部执行通过（4m39s），无失败/错误/跳过；85955父APK通过（1m59s），484014115 bytes，时间11:48:28，SHA-256 `A18E4FB592CCE5DD60514B680A0887AD9C6828DF0496B46B4E36D0CC7F7C723B`。随后市场标记改为完整写入并同步后原子替换，失败保留旧来源。工具箱入口参照同路径Operit组件，保留既有主题/图标/路由；去除延迟导航协程、回调跟随当前入口、卡片允许随大字体增高并消除图标重复朗读。此两项补充待验证。ScreenRouteRegistry现有16项宿主工具，另有动态ToolPkg入口；Shell键盘发送可重复执行、等待时新输入被清空、历史读写为空实现，SQL查询缺同步占位检查已定位，尚未修改这些叶子。

2026-09-09 标记19669的2项测试全部通过（1m20s），63095父APK通过（1m1s），484014115 bytes，时间11:53:22，SHA-256 `F44173DC5BDA88940C4501CAD9756BD264678E0D9E94D733FF2899E50B236B86`；文档501文件0问题。Shell下一增量76027测试中：ViewModel统一草稿/执行/最近100条会话历史，防重复与迟到清草稿、历史建议真实读取、历史清除确认、后台弹层隔离、48dp预设按钮。原历史持久化代码从未实现读写，本批保留会话内语义并清理这些空入口，没有新增磁盘命令存储。Shell布局/输出、工具箱其余叶子及扩展registry等剩余反审继续。

2026-09-09 Shell76027的3项测试全部通过（1m24s，无失败/错误/跳过）；补完整命令与可选择滚动输出后32418父APK通过（1m42s），484014771 bytes，时间12:00:39，SHA-256 `3E74BFE13F4937E4B7B19B620119BB099CC105C55ECB0E1D8A00EF4EFC7A6B37`，com.kiyori / 45 / 0.1.0。SQL下一增量52615测试中：原ViewModel同步防重、代次释放、翻页绑定结果查询和页大小，SELECT/CTE外层分页、PRAGMA/EXPLAIN直接读取、页大小显式校验、后台弹层隔离、参数面板滚动和正文错误提示。SQLite内存实证复现PRAGMA/重复LIMIT语法错误及页大小2改4跳过第3/4条；参数快照并不承诺跨页数据库一致性。其余工具箱叶子及P6c/P6e–P6g仍需继续。

2026-09-09 SQL52615的4项测试全部执行通过（2m44s），无失败/错误/跳过；20228父APK通过（1m4s），484015307 bytes，时间12:09:12，SHA-256 `265DF3D9DE7D0F8B8BC8FC459067A5DF273066FA5FC23CEA211B5CD9861CA303`。这是当前最新已核验产物。SQL Canvas表格的无障碍、单元格查看/复制及更多复杂SQL边界仍需继续，不将首轮分页修复标成整页完成。日志下一增量初查：清除无确认/无结果，复用的KiyoriLogger.resetLogFile为启动入口，会吞删除失败且不清所有包日志；导出结果3秒自动消失且取消捕获为普通错误，读取计数与实际导出是两遍活跃文件，尚未修改日志源码。全部改动仍未暂存/提交/推送，Goal active。

2026-09-09 日志增量23595验证中：显式清除与快照复用KiyoriLogger写入队列，清除失败可观察且只处理当前应用文件；导出计数/正文来自同一临时快照，取消传播并清理快照。原ViewModel持有导出/清除互斥状态，清除成功才关闭确认；路径结果手动关闭，后台页面不弹层，去重复标题并统一卡片/滚动布局。三个文件操作和两个页面操作测试及架构检查待核验；未运行真实日志清除或导出。

2026-09-09 日志20875的5项测试全部执行通过（1m38s），48786父APK通过（1m59s），484017503 bytes，时间12:27:36，SHA-256 `B0BA43C921D64D9E02CA5E803F07D48723BE13779092CF27B60FE72740B7ADA2`。完整架构85933曾因快照漂移失败：逐项审阅本轮Workspace/Env测试、普通脚本持久化与Shell空历史入口移除、日志原owner新增API，以及已有d3d1c2c8数据库singleton和182ff876统计面板迁移后更新精确快照。m05a2/m05b行为测试4项通过，83689完整架构命令PASS（phase=m03）。日志设备导出与清除仍待验证。

2026-09-09 工具测试页首批实施中：参考Operit f323d6c50fa661837fad06d4618462861779b562 同路径ToolTesterScreen，沿用分组与主题，改为单层网格、可增高卡片和正文完整查看/复制。新增页面ViewModel同步占位、用例身份、串行批量、真实范围确认、系统/UI单项运行、停止后续调度与异常终态；独立会话目录，ZIP位于源外，清理不再指向公共测试根。21586执行5项行为/目录测试；真实网络、设备操作未执行。下一个反审点为滚动后输入用例焦点与取消前未启动状态。工具箱其他叶子及P6c/P6e–P6g仍未完成。
