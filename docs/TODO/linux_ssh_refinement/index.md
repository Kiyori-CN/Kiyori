# Linux SSH 工具包与终端完善

状态：in_progress。范围为内置 Linux SSH 插件、脚本、终端 SSH 设置及共享连接；不部署或修改用户服务器，不改变稳定协议，不增加代理核心。

## 目标与计划

1. 审查配置、AI 调用、tmux、文件读写、终端入口与网络路由，记录可复现失败。
2. 修复执行目标、重复提交、错误传播、连接锁与资源生命周期；完善配置及结果 UI。
3. 使用现有应用代理所有者，为 SSH 传输提供明确路由；失败不得自动直连或重放命令。
4. 执行脚本行为回归、终端单元测试及串行 Debug 构建，核验实际 APK。
5. 审计精确文件清单，先交付 terminal，再更新父仓库 gitlink 并提交推送。

## 基线与风险

2026-09-09：父仓库 main，HEAD `64624222f5c4d562e58e189bf61303df73d7739c`，父子工作区均干净。
回滚以本轮父子提交的反向提交为边界，不重写历史。
UI 复用 Operit `examples/linux_ssh/src/linux_ssh_setup/index.ui.ts` 的组件及主题，修正配置、动作和错误状态。

已发现：连接锁重入死锁；UI 把失败当成功及异常后重复调用；脚本文件读取裁掉首尾空白；tmux 尾部标记掩盖失败；脚本执行目标受终端 SSH 设置影响。

## 验收

- [ ] AI 工具参数、超时、错误和提交状态有行为回归证据。
- [ ] 终端连接生命周期和代理路径完成代码验证。
- [ ] Debug APK 构建与产物核验通过。
- [ ] 父子 main 提交、跟踪及远端 ref 一致，工作区干净。
- [ ] Android UI、真实 SSH 密码/密钥、代理/断网和长任务现场验收：verification_pending。
