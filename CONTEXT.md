# Kiyori shared context

This file defines the stable product language and compatibility boundaries for this repository.
Current work status and implementation notes belong in `docs/TODO/`.

## Product identity

- **Kiyori** is the brand.
- **Kiyori** is the Android application and user-visible product name.
- **Kiyori** is the repository and Gradle root-project name.
- The project repository is `https://github.com/Kiyori-CN/Kiyori`.
- The Android application ID is `com.kiyori`.
- Android system surfaces, the default assistant persona, Kiyori-owned file-provider titles, WebChat, generated workspace display text, and Kiyori-owned companion UI use the Kiyori brand.

## Product position and ownership

- **Kiyori** is an all-purpose browser powered by Operit AI. Web access and content handling are the product center; video, music, reading, downloads, files, ad blocking, and later content capabilities are browser-owned domains.
- **Operit AI** is Kiyori's embedded AI subsystem. It owns AI conversation and AI-specific runtime capabilities, but it does not own the Kiyori application shell, global navigation, product identity, or system-wide settings.
- Operit AI conversation renders inline and block LaTeX through the single existing JLaTeXMath Android `0.2.0` path; KaTeX remains a MathML/plain-text conversion dependency and is not a second chat-formula renderer. Markdown code spans and fenced code blocks are opaque to every math delimiter, including `$`, `$$`, `\(`, `\)`, `\[`, and `\]`; block quotes remove one quote marker per line and recursively reuse the same block AST, so real display formulas inside quotes render while quoted code remains literal. Display formulas support top-level `\tag{...}` and `\tag*{...}` as separate right-aligned equation-number semantics; streaming does not expose an unfinished block as source text. The formula compatibility boundary maps JLaTeXMath's missing `\lvert`, `\rvert`, `\lVert`, and `\rVert` commands to supported delimiter atoms, normalizes `\ ` and a single backslash followed by a physical line break to the supported `\;` spacing command, and converts the documented basic `\ce{...}` chemistry subset into ordinary JLaTeXMath input without claiming full `mhchem`. Successful display formulas scale down only to `0.8` before using formula-local horizontal scrolling; failed formulas show an explicit render-failure marker and complete wrapping source text. Development diagnostics include original and preprocessed formulas, transformations, backend version, exception, command, and source position. `\boxed` and `\fbox` retain their existing syntax and metrics while using a fill-preserving frame implementation so framed and following glyphs remain solid.
- Every Kiyori capability may provide a human-facing page and an AI-facing capability contract. AI integrations call those contracts instead of directly driving Activities, Composables, or ViewModels.
- Kiyori has never had a user-facing release. The inherited Operit navigation drawer is not a published Kiyori interface, so the product-shell migration removes that design instead of maintaining a parallel legacy route.
- The current Browser tool contract is shared by Browser Home and Operit AI through the one `StandardBrowserSessionTools` / WebSession / real WebView runtime. A requested viewport is a per-session CSS layout contract; only the Host boundary converts it by Android display density into physical View dimensions, and the page's DOM viewport metrics are the evidence returned to the caller. Fixed `1×1` background-anchor wording in older presentation history is not current behavior. Real link clicks retain click semantics and report navigation settlement separately. Page console messages exclude userscript bridge/runtime diagnostics.
- The browser search-engine quick-switch bar is result-page chrome, not session-wide chrome. A text search submitted from the full-screen search surface records one `BrowserSessionSearchRecovery`; the bar remains hidden until the active WebSession URL is exactly equivalent to that recovery's resolved result URL. Custom home pages, direct URL navigation, redirects that have not settled, ordinary child pages, and sessions without that recovery never display it. Reprojecting the same result page preserves an explicit user dismissal.
- Browser ad blocking is owned by one `BrowserAdBlockStore`. It persists the master switch, allowlisted
  domains, custom URL rules, custom element selectors and subscription metadata in schema-v3 state.
  Raw subscription text is the authoritative input and uses content-addressed atomic app-private
  `browser_ad_block_subscriptions/<subscription-id>/<payload-sha256>.txt` payloads. Derived compiled
  snapshots are excluded from backup and stored under
  `noBackupFilesDir/browser_ad_block_compiled/<format>/<compiler-contract>/<subscription-id>/`.
  State records the committed payload SHA-256, byte count and storage version only after the new payload
  and compiled partition have been fully written. Five stable built-ins are enabled by
  default and shown as “广告拦截器 Pro” (My AdFilters, AdGuard 中文过滤器, AdRules Lite) and
  “Adblock Plus” (EasyList China + EasyList, 可接受广告例外规则). Missing built-in payloads synchronize
  on first use or immediately when a subscription is enabled; later process starts check enabled built-ins
  against their fixed update interval when automatic updates are enabled. Subscription downloads use a
  bounded chunk reader, so a normal EOF is accepted while the 32 MiB payload limit remains enforced.
  The settings page distinguishes enabled subscriptions from ready local payloads and exposes batch progress,
  per-subscription status, expanded details and readable update errors. Custom subscriptions remain explicit
  user-managed entries and a newly added or re-addressed enabled entry starts its first synchronization from
  the settings flow.
  Subscription parsing executes only the WebView-evidenced ABP/AdGuard subset: anchored, wildcard and
  regular-expression network rules, exceptions, domain/party/resource constraints, `important`,
  cross-enabled-subscription `badfilter`, page exceptions and standard `##/#@#` cosmetic rules.
  Unsupported action modifiers and extended cosmetic syntax are counted as ignored instead of being
  widened into ordinary blocking rules. `shouldInterceptRequest` supplies page URL, request URL,
  main-frame evidence, headers and inferred resource type to an immutable matcher snapshot on the WebView
  request thread; main documents are never directly blocked. Store construction never reads or compiles
  subscription payloads on its caller: settings metadata and local rules load on one IO lifecycle with
  explicit reading, compiled-snapshot loading, changed-rule compiling, ready and failed states, then publish
  one complete matcher atomically.
  While that lifecycle is incomplete, WebView requests continue without waiting for its lock or parsing
  work. Each subscription owns one compiled Engine partition containing the already-computed host/token/
  unindexed network index, element-domain index and `badfilter` records; the small user-authored partition
  remains separate. A valid snapshot is decoded directly into its partition without reading the large text,
  parsing ABP syntax, compiling rules or recalculating indexes. When a snapshot is absent or refreshed content
  has changed, one buffered UTF-8 line pass validates and directly emits compiled network rules, compiled
  element rules, `badfilter` records and metadata counts. That runtime path does not construct a complete
  subscription `String`, retain complete parsed spec lists or compile every accepted rule a second time.
  `BrowserAdBlockEngine.combine()` joins the
  partitions inside the same immutable matcher and one global candidate decision, preserving cross-source
  exceptions, `important`, `badfilter` and element exceptions. One changed subscription rebuilds only its
  partition; total switch, allowlist and subscription enablement changes rebuild only the lightweight
  matcher. A refresh whose exact payload SHA-256 is unchanged updates synchronization metadata without
  replacing the Engine, advancing `ruleRevision` or reapplying DOM rules. Each request is normalized once;
  page policy and registrable-domain facts use bounded caches whose first miss is computed once even when
  WebView emits concurrent subresource requests. Blocked-count publication and per-session network-log UI
  refresh are coalesced instead of emitting one Compose update per subresource, including a final race-safe
  blocked-count publication after traffic becomes idle.
  DOM selectors are indexed and cached by page, assembled into bounded CSS chunks off the main thread and
  injected into the same WebSession WebView at most once per document token and `ruleRevision`. Hiker-style
  selectors retain their dedicated mutation observer; ordinary CSS selectors do not run through it.
  Runtime-relevant changes advance one `ruleRevision`, whose single observer reapplies DOM rules to every
  existing WebSession. Network-log entries distinguish `REQUEST` from `ELEMENT`: request entries record
  WebView request decisions, while element entries record only user-created element rules for the current
  page as Kiyori client decisions. Subscription cosmetic selectors remain stylesheet inputs and are not
  fabricated into thousands of request-log entries. Both kinds participate in the network-log `blocked`
  filter, but element entries are never presented as HTTP requests or guessed server responses. The
  settings page, network-log “拦截” filter,
  element long-press actions and “标记广告” workbench never create a second Browser Runtime or rule
  owner. The ad-marking workbench is a fixed-height sibling below the live WebView rather than an
  overlay on top of page content. The live page keeps the remaining measured height, so scrolling to
  the document end aligns the page bottom with the workbench top instead of hiding the last elements
  behind the workbench. Its rule, node, HTML, parent/sibling/child, preview, edit, domain clear and
  navigation-policy actions remain Host-owned. Marking mode owns the native WebView touch stream:
  a tap selects at its release point, while a vertical drag and system-threshold fling scroll the same
  WebView. Neither gesture enters the DOM, and every frame, scheme and popup navigation is consumed;
  the selected element is resolved from the touch point even when a media layer or cross-origin iframe
  prevents top-document event bubbling. The workbench footer uses the browser bottom bar's shared
  50dp content-height contract with 40dp text-only actions. The
  `Default allow / Ask before navigation / Block navigation` policy applies only after marking mode
  exits: normal browsing keeps current-host navigation available and applies the selected decision to
  cross-host main-frame and popup targets.
  Domain clear removes user-created element rules and explicitly domain-targeted URL rules for the
  normalized current host; subscription and unscoped global rules remain intact. The marking script
  captures pointer/touch/mouse/click activation, uses composed paths and immediate propagation
  suppression, and extracts image/video/source/iframe/object/embed and background-image resources;
  cross-origin iframe internals remain outside the readable top-document DOM boundary.
- Public `browser:fill_form` fields contain a string, number, or boolean `value` plus exactly one of `ref` or `selector`; `name` is optional diagnostic text, and caller-supplied control `type` is not part of the contract. The real DOM selects the shared fill, checked-state, or option-selection operation. Checkbox and radio values are boolean. `browser_run_code` accepts a JavaScript function source and injects it directly into the current WebView without `eval` or a dynamic function constructor. Its Page subset is `title`, `url`, `evaluate`, `waitForTimeout`, `setContent`, dialog `on/once/off/removeListener`, and locator/getByRole `click/hover/fill/selectOption/textContent`. Locator input and `keyboard.press` use the same `__operitPw` runtime as package-level input operations. Keyboard supports one character, `Enter`, `Backspace`, and `Delete`; `page.dialog`, unknown keyboard/locator members, and keys without an implemented behavior return a structured `Unsupported Playwright API`. Trusted file-chooser activation remains owned by the public real-click path followed by `browser:upload`.
- `browser:close` and `browser:tabs action=close` commit and report the in-memory tab/session state change before observing the newly active page. Open-tab count, closed session, and active session are state evidence; page state and snapshot are separate observations whose failure cannot reverse a completed close.

- **OpenAI Hosted Web Search ToolPkg** is the independent hosted-search capability exposed as
  `openai_web_search:search`. `com.kiyori.openai_web_search` is a disabled-by-default ToolPkg
  container with an enabled-by-default subpackage and settings UI. Revision `7` uses one
  configuration source only: package-scoped `PACKAGE + HOST_SERVICE` values stored by
  `ToolPkgHostEnvironmentRepository`. It never follows the active chat provider, config, model, or
  custom parameters. The API key is sensitive/password and inaccessible to ToolPkg JavaScript.
  The settings UI opens the native environment editor through the fixed no-argument
  `ToolPkg.services.openAIWebSearch.openConfiguration()` service; JavaScript cannot submit an
  arbitrary package ID or variable name.
  The host owns endpoint, authentication, request compilation, one non-streaming Responses POST,
  cancellation, relay compatibility evidence, response parsing, XML-safe JSON transport, and the
  structured result card. `OpenAIHostedWebSearchRequestLifecycle` is the unique owner of request
  phase, submission state, cancellation owner, elapsed time and terminal settlement.
  `OpenAIHostedWebSearchAdmissionController` is the process-wide FIFO owner for plugin concurrency
  and RPM. Queue waiting has its own default `60s` budget; after admission, the HTTP call has a
  default `300s` budget with connect capped at `30s` and write capped at `60s`. Queue timeout never
  creates an HTTP call. OkHttp call timeout is classified as `REQUEST_TIMEOUT` before cancellation
  state is considered, while explicit cancellation and worker completion compete for one atomic
  terminal outcome.
  Relay probing is a potentially billable action that only the plugin settings UI runtime may
  start. Search execution has no automatic retry, redirect, backend switch, endpoint switch, model
  switch, key-source switch, background response, sequence resume, polling, SSE continuation, or
  second request. Response schema revision `7` keeps four explicit evidence modes:
  `url_citations_and_action_sources`, `url_citations`, `action_sources`, and `structured_feeds`.
  Successful results atomically separate `cited_sources`, `all_sources`, `source_summary`, and
  `execution_diagnostics`; the main-model projection does not repeat the full uncited
  source/action/query/usage payload. HTTP(S) annotation URLs and action-source URLs are never
  guessed from answer text. Native
  `url_citation` annotations are the authority for inline citation spans; action sources are an
  independent search-audit channel that can be complete, missing, partial, or contain invalid
  entries. A citation URL is not required to appear in `action.sources`: relays and official
  Responses can cite a page discovered after `open_page`, or return only one URL channel.
  Cross-channel differences are reported through `source_diagnostics`,
  `CITATION_NOT_IN_ACTION_SOURCES`, `ACTION_SOURCES_MISSING`, and
  `ACTION_SOURCES_PARTIAL`; they do not invalidate an otherwise valid citation. Source markers are
  inserted only from real citation spans. The official non-URL feed types
  `oai-sports`, `oai-weather`, and `oai-finance`, plus relay-only named `api` feeds, remain visible
  without a clickable URL. Unknown or malformed entries stay as diagnostics.
  Relay compatibility probing uses a fixed public-page target on `developers.openai.com` and
  requires a real `url_citation`; a time or weather query is not a URL-evidence probe because it may
  legitimately use structured live data. Successful and failed probes are stored as a bounded
  record set against the exact binding fingerprint and schema revision, with the successful
  evidence mode. The set retains up to 16 recent fingerprints. The fingerprint includes an
  irreversible digest of the selected credential, so changing only the Key also invalidates prior
  evidence. Successful and failed compatibility record-set mutations are synchronously committed
  before the probe callback is delivered, so an immediate process death after closing the settings
  page cannot lose an unchanged fingerprint's evidence. A current fingerprint still requires a
  fresh probe when any fingerprint input changes, including endpoint, model, auth mode, selected
  credential, non-secret header names, reasoning effort, external web access, or response schema
  revision. Domain filters
  serialize only non-empty `allowed_domains` or
  `blocked_domains`; an empty peer array is not sent because real Responses relays may reject that
  otherwise redundant field combination. Non-success HTTP responses read at most 64 KiB and expose
  only redacted provider type, code, message and request/trace ID. The settings page shows a short
  credential revision and `bearer` / `direct` / `custom` authentication classification, never the
  Key or custom scheme text. Its readiness object always contains
  `provider_contract`, `endpoint`, `model`, `credential`, `auth`, `extra_headers`,
  `search_options`, `admission`, and `compatibility`, including field-level failures when the whole
  binding is not yet valid.
  UI evidence parsing is explicit `NotApplicable`, `Parsed`, or `Invalid`; invalid diagnostics are
  bounded and never log query, answer, sources, or raw result JSON. The complete successful
  `ToolResult` used by the live chat renderer is separate from the bounded main-model projection:
  `formatToolResultForMessage()` retains `all_sources`, `search_actions`, `query`, `usage`, and
  the complete schema needed by the result card, while `formatToolResultForModel()` is used only
  for the follow-up model context and omits the uncited source/action payload. A single
  `openai_web_search:search` establishes the message-level tool group in every collapse mode.
  Inside it, the summary, sources, search trace, and diagnostics have independent presentation
  state; sources expand in stable batches of eight. The four persistent OpenAI chat-provider enum
  values remain separate global provider contracts and are presented as official or compatible
  Chat Completions and Responses choices rather than being removed with the Web Search
  configuration-source cleanup.

## Architecture ownership transition

## Current first-run authority (2026-08-11)

- Kiyori's current first-run flow is owned by `com.kiyori.app.startup.KiyoriMainStartupGateCoordinator`
  plus the `com.kiyori.integration.operit.onboarding` integration. Onboarding version
  `2026-08-11-r9` contains six Chinese pages: an AI-browser product overview, a browser/content
  workspace, AI and connected services, a local file/terminal/mini-app workspace, separate User
  Agreement and Privacy Policy entries with one explicit consent checkbox, and a flat
  permission-selection page. The first four pages use a shared visual, header, and card baseline
  with compact two-column capability cards instead of isolated promotional tags; their visible
  product vocabulary covers browser, downloader,
  video, music, reading, ad blocking, AI assistant, voice, accounts, toolbox, files, terminal,
  mini-app management, and logs without claiming that every product-domain settings entry already
  owns a completed detail screen.
  `HorizontalPager` is the only page owner: settled pages persist the current step, a fling advances
  at most one page, the unaccepted agreement page blocks forward swiping, and active authorization
  locks page movement until the current system handoff completes. The legal content screen remains
  outside the pager. The first-page title is centered; the three introduction titles with eyebrows
  remain start-aligned. The agreement content version remains `2026-08-09-r3`.
  The permission page processes only the items selected by the user and enters Kiyori directly when
  the selected queue is complete; there is no permission level selector or independent readiness
  summary page. Exact-alarm access is not part of the current first-run contract: scheduled workflows
  use WorkManager and the normal device-alarm capability uses `ACTION_SET_ALARM`, while no current
  Kiyori consumer requires exact `AlarmManager` scheduling.
- Before formal content is shown, `MainActivity` asks the single `ChatHistoryManager` owner for the
  persisted current-chat ID and verifies that its database row exists. A missing or stale current
  chat creates and selects one real empty conversation without requiring a configured model or API
  key. A valid conversation remains current unless the existing `startWithNewChat` preference
  explicitly requests another empty conversation for that launch.
- The bundled accessibility companion is version `1.7`. Its user-visible application and service
  names are `Kiyori 无障碍支持` and `Kiyori UI 自动化服务`, and its launcher foreground uses the
  same foreground asset as the main Kiyori launcher. The package
  `com.ai.assistance.operit.provider`, service classes, authorities, and
  `IAccessibilityProvider` action remain compatibility identifiers required by the current IPC
  contract. ARCH045 locks the branded APK hash, version marker, visible Kiyori strings, byte-level
  equality of the two icon entries with the main launcher foreground, and absence of the old
  `Accessibility Operit Support` brand.
- This implementation round maintains only the Chinese default onboarding copy. Other locale
  resources are outside the current Chinese-first acceptance scope and are not treated as the
  current product contract.
- `POST_NOTIFICATIONS` is requested only through the centralized
  `ActivityResultContracts.RequestMultiplePermissions` flow. The deleted
  `KiyoriMainNotificationPermissionCoordinator`, `KiyoriNotificationPermissionCapability`,
  `AgreementScreen`, `PermissionGuideScreen`, and `PermissionGuideViewModel` are historical
  implementation records, not current owners. No startup callback may request notifications again
  after onboarding.
- Software Home's first composition observes the lightweight process browser-window projection and
  does not construct `StandardBrowserSessionTools`. Browser profile cleanup, download recovery,
  ad-block runtime work, history access, and the presentation coordinator begin only when Browser
  functionality is actually requested. Static host navigation is available for the first frame;
  ToolPkg route discovery waits for two frame signals, then awaits the single `PackageManager`
  initialization before publishing the dynamic navigation model. The existing pending-route owner
  retains unknown external ToolPkg routes until that discovery has reached a success or failure
  terminal state. The explicit Coil prewarm is part of the existing delayed serial warmup queue,
  and startup MCP configuration is refreshed only by the plugin-loading chain.
- `PluginLoadingState` remains the sole MCP startup progress, plugin-list, and log owner. Its
  presentation permission is projected from the visible Kiyori Shell surface: AI Home, the AI
  drawer, visible AI children, and visible Operit AI settings details may show progress; Software
  Home, Minus-One, Full-Screen Web Search, Browser, Mini App, File Management, Kiyori Settings,
  bookmark/history/download drawers, and suspended Browser workspaces do not. Hiding the
  presentation never cancels MCP initialization. The root loading overlay subscribes to progress,
  plugin-list, expansion, and log flows only while presentation is actually allowed and visible;
  disposing the Kiyori root revokes the presentation permission.

- The accepted package direction has two roots: `com.kiyori` owns the Kiyori product shell, product features, design system, platform capabilities, and product integrations; `com.ai.assistance.operit` retains Operit AI runtime, ecosystem contracts, serialized/reflected types, native/IPC identifiers, and stable compatibility entrypoints.
- `com.kiyori.capability` and `com.kiyori.integration.operit` are the only planned cross-root contract and adapter boundaries. Neither side may create a second Browser Runtime, PlayerSession, download owner, userscript repository, settings store, or other factual state owner.
- This is an incremental target, not a claim that source files have already moved. Until a milestone is implemented and verified, current paths under `com.ai.assistance.operit` remain authoritative.
- Gradle `namespace` remains `com.ai.assistance.operit` during the initial package migration. Changing namespace, persisted data, protocol identifiers, AIDL/JNI names, Android stable components, or terminal ownership requires separate approved work.
- M-00 documentation authority, G-00 machine-readable ownership/stable-contract gates, M-01's original-package rename, M-02's Application global-access isolation, M-03's package move, M-04A1/M-04A2's host-contract and root-Composable moves, M-04B1 through M-04B7's Shell decomposition, M-04C's Operit navigation integration closure, M-04D1 through M-04D9's MainActivity responsibility extraction, and M-04E's compatibility-entry finalization are implemented. The unique Application entry is `com.kiyori.app.KiyoriApplication`, the unique root Composable is `com.kiyori.app.KiyoriApp`, the unique Shell host is `com.kiyori.app.shell.KiyoriAppShell`, primary destination presentation is owned by `com.kiyori.app.shell.KiyoriPrimaryNavigation`, Software Home is owned by `com.kiyori.app.shell.KiyoriSoftwareHome`, residual Full-Screen Browser Search is owned by `com.kiyori.app.shell.KiyoriBrowserSearch`, the unique Modal AI Drawer host is `com.kiyori.app.shell.KiyoriAiDrawer`, the unique pure Shell-state owner is `com.kiyori.app.shell.KiyoriShellState`, and the Browser exit result contract is `com.kiyori.capability.browser.presentation.KiyoriBrowserExitPresentation`. `com.kiyori.integration.operit.navigation` now uniquely owns `AppRouteCatalog`, the PackageManager-backed navigation model revision, ToolPkg runtime listener, AppRouterGateway/AppRouteDiscoveryGateway lifecycle, and route-root conversion helpers while the stable Operit route/gateway contracts remain in their original package. `com.kiyori.app.startup.KiyoriMainPendingRequests` is the sole owner of external pending files/text, browser, OAuth, shortcut, route, Shell destination and current-main-navigation state. `com.kiyori.app.startup.KiyoriMainIntentDecoder` is the sole reader of MainActivity Intent payloads and emits sealed commands without modifying the Intent or executing runtime/UI effects. The stateless `com.kiyori.app.startup.KiyoriMainDisplayCoordinator` uniquely applies sustained-performance, refresh-rate and hardware-acceleration window policy. `com.kiyori.app.startup.KiyoriMainSharedContentCoordinator` uniquely transfers pending external files/text into the unchanged Operit `SharedFileHandler`, which remains the sole AIChat StateFlow owner. The stateless `com.kiyori.app.startup.KiyoriMainTaskVisibilityCoordinator` uniquely restores recent-task visibility only after the existing AI foreground runtime has stopped. `com.kiyori.app.startup.KiyoriMainOrientationCoordinator` uniquely owns MainActivity orientation state and its confirmation dialog while Activity retains lifecycle dispatch and `recreate()` side effects. `com.kiyori.app.startup.KiyoriMainNotificationPermissionCoordinator` uniquely owns the MainActivity startup `POST_NOTIFICATIONS` launcher, request decision, rationale and result handling while the Android permission remains the sole fact source. `com.kiyori.app.startup.KiyoriMainStartupGateCoordinator` uniquely owns the MainActivity agreement/permission-guide/content UI projection while `AgreementPreferences` and `AndroidPermissionPreferences` remain the sole persistent fact owners; Activity retains lifecycle, delay, plugin-loading and content-host side effects. The stateless `com.kiyori.app.startup.KiyoriMainContentHost` uniquely projects one-shot content requests and mounts the existing `KiyoriApp` under the existing plugin-loading CompositionLocal without copying either state owner. `MainActivity` continues to expose the same public action/extra constants and remains the stable Manifest launcher, request-ID generator, Android lifecycle host and runtime side-effect boundary. Its exact compatibility ownership, imports, launcher contract, zero feature dependency and unique content-host edge are locked by ARCH039; ARCH018 through ARCH038 continue to lock the migrated owners. M-04 and M-05 are complete. The current architecture phase is Stage 4, the Browser product domain; Player remains a separate Stage 5 migration and keeps its existing fullscreen system-bar owner until that phase. Detailed scope lives in `docs/TODO/kiyori_architecture_refactor/`.
- M-05A1 is complete. `com.kiyori.design.theme.KiyoriColorSchemes` uniquely owns the fixed application and Browser color schemes plus the boolean resolver; `KiyoriBrowserTheme` and `KiyoriSettingsTheme` are pure design wrappers in the same package. The Operit `ThemeColorSchemeResolver` remains the sole preference adapter and delegates its boolean decision to the Kiyori design resolver. The old Browser and Settings theme source paths are removed, Operit UI presentation is the only Operit ownership area allowed to import `com.kiyori.design`, and ARCH040 locks paths, hashes, declarations, consumers, test ownership, forbidden dependencies, and the absence of a second owner. The M-04B App Shell snapshot locks its complete current project-import set, including the two approved Kiyori design imports, rather than assuming every project import remains under the Operit root. ARCH024/ARCH040, the full architecture gate, 154 Python tests, 135 JVM suites containing 810 tests, formal readiness, lint intersection, working-tree Markdown, the prescribed Debug build, and APK static audit pass. The sealed APK is `471291682` bytes with SHA-256 `B8CD99D49D3F93745282C4E04F7FD7C158A875BE8897FF17161EC4D2C7F93646`; device and visual acceptance remain `verification_pending`.
- M-05A2 is complete. `com.kiyori.design.theme.KiyoriSemanticColors` now uniquely owns the semantic enum, stable-ID mapping, immutable color pair, all fixed light/dark pairs, the `#FFC153` navigation fill, and the `#C57C00/#FFD166` weather colors; `KiyoriSemanticTheme` in the same package contains only the two `MaterialTheme` luminance adapters. The old Operit owner is removed without a facade, typealias, fallback, or duplicate declaration. ARCH041 locks both source hashes, declaration and dependency boundaries, exact color/mapping behavior, the current 98-import consumer snapshot, the four-test ownership split, and the affected M-04B snapshots; browser-local action identities do not import or expand the global semantic enum. ARCH025/026/027/040/041 and the full architecture gate pass at the sealed M-05A2 baseline. Full CI Python is 156/156, JVM XML remains 135 suites and 810 tests with zero failures/errors/skips, and formal/fresh-clone readiness passes. The fresh full lint report still exposes the repository's pre-existing 27 errors, 287 warnings, and 2 hints outside the A2 change lines; the structured baseline remains 5792 retained, 0 stale, and 316 current-only, while the two new design files have zero lint hits and the only affected-path hits are unchanged body diagnostics in `WebSessionUserscriptSheet.kt`. The prescribed Debug build and APK audit pass: `app-debug.apk` is `471292358` bytes with SHA-256 `60D613A5F9BFBD019296E21E05547DF16B0789FCDE73FF2699AFBAC415A8BBC0`, package/version/SDK/Application/launcher/process contracts are unchanged, it contains only arm64-v8a with 53 native libraries and no duplicate basename, and Debug v2 signing plus 16 KB alignment pass. Device and visual acceptance remain `verification_pending`.
- M-05A3 is complete. `com.kiyori.design.theme.KiyoriTheme` is the pure Material/background owner, `KiyoriTypography` uniquely owns the fixed zero-tracking Typography and pure font-family projection, `com.kiyori.app.theme.KiyoriTheme` is the sole preference/font/Glass host, and `com.kiyori.platform.window.KiyoriApplicationSystemBars` uniquely owns Application edge-to-edge and system bars. The old `ui/theme/Theme.kt`, `OperitTheme`, `Theme.Operit`, old Typography declarations, and old root-theme test are removed without a facade, typealias, compatibility branch, or second owner. MainActivity and the stable Widget configuration Activity reuse the same app host through two file-exact ownership exceptions; PlayerActivity fullscreen window behavior and its locked source hash remain unchanged. ARCH040/041/042, six M-05A1/A2/A3 positive and negative architecture tests, the full `phase=m03` architecture gate, full CI Python 158/158, JVM XML 134 suites containing 810 tests with zero failures/errors/skips, formal/fresh-clone readiness, and `git diff --check` pass. Fresh full lint remains the pre-existing 27 errors, 287 warnings, and 2 hints; all four new owners have zero findings, the structured baseline remains 5792 retained, 0 stale, and 316 current-only, and the earlier two Modifier warnings are gone. The prescribed Debug build and APK audit pass: `app-debug.apk` is `477957302` bytes with SHA-256 `9373518AEB8FA8BD2C02DCFDE5741833653A2D76AFE270D2C27F4D2BF88C0074`, package/version/SDK/Application/launcher/process contracts are unchanged, it contains only arm64-v8a with 53 native libraries and no duplicate basename, and Debug v2 signing plus 16 KB alignment pass. Device and visual acceptance remain `verification_pending`.
- M-05B is complete. `com.kiyori.platform.logging.KiyoriLogger` is the sole executor, early-bound internal files directory, package-log root provider, internal/package log file, and `enableFileLogging` owner; the existing `ApplicationContextAccess` remains the sole process Context owner, and `KiyoriLogTextFormatter` uniquely owns bounded Throwable/message formatting. The old `com.ai.assistance.operit.util.AppLogger` remains as a stateless compatibility facade because Operit consumers, Provider startup, log export, and two JVM static-mock tests bind that FQCN. Eight current Kiyori owners import `KiyoriLogger` directly: the current app/startup consumers, onboarding, and the Kiyori-owned Operit navigation integration that records ToolPkg discovery failure. Operit-owned source remains on the facade to preserve upstream patch locality. M-05B sealed with `OperitPaths` providing the package-log root; M-05E later moved that single calculation to `KiyoriPaths` and changed both Provider/Application bindings without changing the logging contract. ARCH043 locks source hashes, state ownership, API delegation, the current eight-consumer snapshot, old formatter removal, static-mock compatibility, ownership cleanup, and the affected architecture snapshots. Device and multi-process file-write acceptance remain `verification_pending`.
- M-05C is complete. `com.kiyori.platform.lifecycle.KiyoriActivityLifecycle` is the sole Android `ActivityLifecycleCallbacks` registration owner and, through its single `KiyoriActivityLifecycleFacts` instance, the sole current-Activity weak reference, created/started Activity count, and foreground-state owner. `OperitActivityLifecycleIntegration` uniquely retains the existing keep-screen-on preference and request counts, plugin lifecycle dispatch, External Chat foreground startup, microphone foreground ensure, PlayerCrash coordination, and VirtualDisplay/Shower cleanup. The old `com.ai.assistance.operit.core.application.ActivityLifecycleManager` keeps its object identity, `Application.ActivityLifecycleCallbacks` interface, four business methods, seven callbacks, JavaScript FQCN, and JVM ABI as a stateless facade; all 13 FQCN consumer paths and the unchanged `KiyoriApplication.initialize(this)` bootstrap remain. ARCH044 locks the three production hashes, unique facts/registration, exact side effects, consumer snapshot, facade ABI, ownership, and four JVM facts tests. Failure-first, positive/negative fixtures, real/full architecture, 162 Python tests, 136 JVM suites containing 817 tests, formal/fresh-clone readiness, Markdown, diff, lint impact, the prescribed Debug build, and APK audit pass. Full lint remains the pre-existing 27 errors, 287 warnings, and 2 hints, with zero findings on the four M-05C paths. The sealed APK is `477957302` bytes with SHA-256 `3792DD58C87D1DDD4F977BB8CD4B4407458EB911EC17CB0CB48CB8876184D2D3`; package/version/SDK/Application/launcher/process contracts, arm64 53-native packaging, the ten single-copy player targets, Debug v2 signing, and 16 KB alignment pass. Real-device lifecycle side effects remain `verification_pending`; M-05D Android permission capability is next.
- M-05D's standalone startup-notification design is a historical sealed milestone. The current
  first-run implementation removed `KiyoriNotificationPermissionCapability`,
  `OperitNotificationPermissionResources`, and `KiyoriMainNotificationPermissionCoordinator`;
  `POST_NOTIFICATIONS` now shares the onboarding `RequestMultiplePermissions` owner with the other
  selected Android runtime permissions. Browser download/userscript notification checks and the
  Manifest declaration remain unchanged. Real Android 13+ dialog, grant/deny, and notification
  delivery behavior remain `verification_pending`.
- M-05E remains the unique storage-owner boundary. `com.kiyori.platform.storage.KiyoriPaths` owns Kiyori public Downloads/Pictures projections, app-specific Browser downloads, internal/cache/files/backup paths, ToolPkg private generations, build transactions, content-addressed runtime directories, directory creation, package-key validation, plugin-ID normalization, and raw-snapshot exclusions. `KiyoriPublicStore` is the sole Kiyori-owned MediaStore and Android 8/9 public-file writer; `KiyoriStorageService` exposes that owner plus host-bound ToolPkg storage. ToolPkg private/cache operations never accept a caller-selected package ID or return internal absolute paths. Explicit SAF imports use package-specific migrators and atomic generation activation; artifact build/import/install uses the shared scanner, deterministic builder, content-addressed artifacts, audit records, and atomic package active records. `KiyoriBackupPaths` remains a stateless backup-domain projection, while `OperitPaths` and `OperitBackupDirs` retain their compatibility object/constants/method JVM ABI as calculation-free facades. ARCH046 locks the current four source hashes, exact `14 direct path / 7 direct backup / 35 legacy path / 0 legacy backup external` consumer sets, the compatibility API and the extended Kiyori-only API without slice-based coupling. Kiyori never automatically scans, reads, merges, copies, or deletes `Download/Operit`; real MediaStore, SAF, market transaction, cross-package isolation and third-party migration behavior remain `verification_pending`.

## UI design sources

- **Kiyori Professional Browser Theme** owns the fixed application theme, the shared semantic-color vocabulary, and the browser protection domain. Material Blue 600 `#1E88E5` remains the primary action color, while `KiyoriSemanticTone` provides stable blue, green, purple, orange, red, cyan, and pink icon/container pairs for App Shell, settings, AI support pages, statuses, and browser content tools. The browser four-row menu additionally owns a presentation-only `WebSessionBrowserMenuTone` table with one stable light/dark icon-container pair for each of its 18 fixed actions; every pair is unique, child drawers and dialogs reuse the originating action identity, and the detected-media count badge reuses `FLOATING_SNIFFER`. `PLUGINS` is the approved low-saturation deep-plum identity: light `#5E3A8A / #EEE8F4`, dark `#CBB8E2 / #2D2238`. Plugin Center, Userscript Manager, detail, editor, and their primary plugin icons reuse that identity; error, success, warning, and update-risk indicators remain separate state semantics. Minus-One data cards reuse `ADD_BOOKMARK / BOOKMARKS / HISTORY / DOWNLOADS`, and its eight inert quick tools each select a different existing browser identity pair instead of defining another palette owner. This browser-local identity table does not add global semantic meanings, persisted state, user color preferences, or a second application theme owner. The five closed App Shell navigation icons use the exact selected fill `#FFC153`; selection removes the blue outline, preserves page-background details, compensates each vector's visible bounds to match the original outline size, and replays a compression-to-spring-growth animation on every click, including a repeated click on the current destination. Home, Browser, Mini App, and Files use a lower damping ratio for a slightly larger yellow peak, while Settings retains the prior peak and all five destinations retain their prior settled sizes. The clear-weather sun is independent: `#C57C00` in light mode and `#FFD166` in dark mode. Neither color contract participates in stable entry-ID tone hashing. Large surfaces and text remain neutral. Browser Home, browser chrome, Full-Screen Web Search, and browser child-page surfaces stay neutral, while drawer tools may use small semantic icon badges without tinting the chrome or webpage. Software Home retains its fixed five-color search-frame border and fixed blue Search/AI selection. The theme does not use wallpaper dynamic color as a product default.
- **Operit Original UI** remains the visual-language authority for typography scale, shapes, component treatment, general icon behavior, dialogs, settings rows, glass effects, motion, and interaction density so AI and non-AI surfaces form one product. Browser Home, Minus-One Page, Settings Home, and their child pages must be compared against the current Operit source before UI implementation because these Kiyori surfaces directly reproduce Operit layouts and interactions. Kiyori-owned App Shell and browser chrome instead use the Legacy Kiyori outline Vector family. The inherited Operit purple palette and unconditional wallpaper dynamic color are intentional Kiyori product differences.
- **Legacy Kiyori Design Reference** is `Kiyori-CN/kiyori-android@24a2dfa91f0a4166dc58e5c4732d11861173f766`. It provides the page composition, navigation intent, browser behavior, and feature layout for Kiyori-owned surfaces.
- When the two sources differ, an accepted Kiyori product contract determines behavior, Operit Original UI determines visual treatment, and Legacy Kiyori Design Reference determines the Kiyori page's content structure.
- Source references are design and migration evidence, not runtime dependencies. New hard-coded visual systems must not be introduced beside the shared Kiyori theme.

| **Application Network Proxy Routing Invariants** | The encrypted proxy config is the single route source across processes; config reads compare file metadata and the encrypted header/random-IV prefix so the independent `:player` process observes main-process saves. Browser and AI download tasks freeze one route snapshot across HEAD, Range, retry, and concurrent-segment requests. Explicit `URLConnection` paths apply the same loopback/private-network bypass policy as OkHttp selectors. The `:player` mpv core applies `PLAYER` routing before each media load; HTTP(S) media that needs application proxying is served through the loopback-only `PlayerMediaStreamBridge` because mpv `http-proxy` does not cover HTTPS, then emits sanitized `PROXY/PROXY_BRIDGE/DIRECT` diagnostics. System `proxy=absent` in a player network snapshot is not proof that the application-level route is direct. |
| **Application Network Proxy Diagnostics** | Proxy logs are process-local and export the current process name/PID. Each Mihomo runtime receives a process-local monotonic generation and records its mixed/controller ports, readiness, mixed-port listener health, reuse health, elapsed operation facts, core tail line and explicit stop reason after the same redaction boundary. A health failure is projected as `ERROR`, blocks the current proxied route, and after two consecutive local health failures enters the same generation-scoped, rate-limited automatic recovery as an unexpected process exit; it never switches nodes or uses direct access. WebView main-document failures and OkHttp/browser-download connection failures retain sanitized host/error/runtime-health evidence. The `:player` process projects the same runtime state into the existing `PlayerDebugLogBuffer`, so player reports can distinguish mpv option application from Mihomo port/Controller availability. |

## Product shell language

| Term | Definition |
| --- | --- |
| **Kiyori App Shell** | The Kiyori-owned root navigation, adaptive layout, window-inset, Back, and top-level state boundary. Operit AI is hosted inside this shell. Its five-item bottom navigation keeps the original outline vectors while unselected; the selected icon has no blue outline or selection container, and is rendered as a theme-aware warm-yellow fill with the original line details redrawn in the current page background color. While the home Pager is being dragged from Minus-One or AI Home toward Software Home, bottom-navigation visibility follows the live Pager offset instead of waiting for the settled `softwareHomePage`; stationary non-center pages and non-root AI routes still keep it absent. |
| **Software Home** | The default center page shown after launch and the first of five primary destinations. It owns the three-page horizontal home space and uses one centered title-over-search column at the `<600dp`, `600-839dp`, and `>=840dp` width gates. On regular-height windows, the Search Frame top border is exactly `38.2%` of the available home height; short windows use the minimum top position that keeps the measured title visible. Compact, Medium, and Expanded cap content width at `544dp`, `584dp`, and `624dp`, with minimum horizontal padding of `24dp`, `48dp`, and `72dp`. |
| **Minus-One Page** | The page immediately left of Software Home. It is part of the home pager, hides the five-item bottom navigation, and reproduces the fixed legacy layout with four data cards and eight inert quick tools. Collection, Bookmark, History, and Download reuse the Browser Menu identities `ADD_BOOKMARK / BOOKMARKS / HISTORY / DOWNLOADS`; the eight quick tools use eight different existing browser identities. Bookmark, History, and Download observe their respective Browser Runtime counts and open the same shared drawers as Browser Menu. Collection remains a zero-count inert placeholder reserved for the future Mini App service and must not be routed to browser bookmarks or another current owner. |
| **AI Home** | The full-screen Operit AI conversation page immediately right of Software Home. It hides the five-item bottom navigation, shares Software Home's single `PagerState`, and has no edge-drawer gesture. Minus-One and Software Home retain the native `HorizontalPager` fling path; the permanently mounted AI surface records its own current down/up session and applies the same strict-half-page, `400dp/s`, one-page, LTR/RTL, boundary and spring contract without reading Pager-private gesture metadata. The App Shell keeps the AI ancestor `scrollable` mounted for the complete pointer stream. Only a completed AI Home gesture session may enter the strict snap policy, and Pager delta is accepted only while a real AI Home drag is still unfinished or the bridge is executing its already-decided settle; child nested flings and content-owned candidates therefore neither create page targets nor move Pager after their owner releases. One Shell-scoped multi-owner boundary is claimed synchronously by tables, non-wrapping code, horizontally scrolling formulas, Mermaid, and HTML previews before content movement begins. Wide tables use the standard Compose horizontal `ScrollState`, while a vertical drag remains available to the conversation list. Mermaid measures the final asynchronously generated SVG and exposes its complete scaled width and height through an internal two-dimensional WebView viewport; Mermaid and HTML/XML chart WebViews keep their touch stream inside the preview instead of forwarding nested fling to AI Home. A fresh install or stale persisted current-chat ID is repaired before formal use by creating and selecting one real empty conversation; model readiness affects sending, not whether the conversation surface exists. Its fixed top-right order is Browser / Terminal / Conversation Details / Workspace. Browser transfers presentation to the one shared Browser Home/WebSession Runtime; Terminal, Conversation Details, Workspace, and chat content are mutually exclusive `ChatPanelMode` projections. Active toggle actions add only their low-saturation semantic container, and an abnormal audit completeness state adds a compact badge to Conversation Details. The Character selector renders unselected character/group rows on the neutral theme surface with a thin outline and uses the blue semantic container, icon, text, border, and check mark only for the selected row or generated default avatar. The collapsed character button also uses the same blue default avatar while a real custom image keeps a transparent backing. Its sort popup keeps a `12dp` rounded shape, zero tonal and shadow elevation, and a `0.5dp` `outlineVariant` border. |
| **Extensions** | The AI drawer and host app bar expose this destination as `Extensions`. Its fixed left-to-right tabs are `Script / Plugin / Skill / MCP`, and a fresh screen defaults to Script. ToolPkg containers remain under Plugin, while ordinary JS, TS, and HJSON tool projects remain script packages internally but use the concise Script tab label. Plugin and Script publish Environment, Market, Add, and Refresh in that order; all four top-bar operations use outlined icons with one visual weight. Script Add opens a two-item `Create Script / Import Script Package` menu. Create Script asks only for the intended script and then enters the existing creation workflow; the removed remote quick-setup step and promotional quick-creation card have no remaining path. Search is a fixed standalone row below the app bar, followed by the fixed tab row; only the active tab content scrolls. Skill publishes conditional Error, Market, Add, and Refresh actions in the host app bar. MCP publishes Start, Market, Add, and Refresh there. Their dialogs, loading state, repositories, and execution callbacks remain owned by the active child page; page-level floating action buttons and duplicate in-card refresh controls have no remaining path. Loading feedback appears only on the action that owns the active operation, and conflicting child-page actions are disabled until that operation settles. Ordinary operations use outlined icons while error, success, category, and runtime-status icons retain their semantic fill. The Skill import indicator covers all three methods, and the MCP import indicator covers all four methods. Package load errors appear as an in-flow compact banner below the tabs instead of an overlay action. Initial entry, manual refresh, artifact-market installation, package deletion, and conflict-source deletion all reload the same complete `PackageManagerSnapshot`. A successful script or ToolPkg market install increments the process-local artifact catalog revision so a retained screen reloads immediately; a recreated screen reads current `PackageManager` state during initial load. The Script tab's academic APIs use the stable `Academic` category for arXiv, Crossref, PubMed, Semantic Scholar, and OpenAlex; that category has the unique `MenuBook` icon and fixed light/dark palette. The internal `packages` route, `PackageManager` enum order, types, ToolPkg identifiers, repositories, storage, permissions, and protocols are unchanged by this visible ordering and naming contract. |
| **Application Network Proxy** | `KiyoriNetworkProxyManager` is the sole application-process routing owner. Settings Home -> More Features -> Network Proxy stores one encrypted `KiyoriNetworkProxyConfig` and controls `AI_SERVICES`, `AI_TOOLS`, `BROWSER`, `DOWNLOADS`, `PLAYER`, `SCRIPTS`, and `APP_SERVICES` independently with `INHERIT / DIRECT / PROXY` overrides. The top-level proxy mode is `RULE / GLOBAL / DIRECT`: RULE sends requests through the shared Mihomo runtime with explicit custom matcher types (`DOMAIN`, `DOMAIN-SUFFIX`, `DOMAIN-KEYWORD`) before retained subscription rules; GLOBAL sends every proxied request to `KIYORI_APP_PROXY`; DIRECT bypasses Kiyori's runtime. A module or script `PROXY` override means “use the current top-level mode”, while `DIRECT` bypasses it; this is intentional because AndroidX `ProxyController` is process-wide and cannot host per-module rule cores. Traditional `JsEngine` calls carry a host-trusted package identity for an additional script-package override; the settings page discovers enabled executable traditional packages and keeps ToolPkg under `AI_TOOLS`. The page hierarchy is a compact home surface plus Node Selection, Subscription Management, Rule Management, Module Connection Mode, Per-script Connection Mode, and Proxy Logs child pages. Rule Management shows current subscription rules as read-only derived data, while custom domain rules are encrypted separately and support add/edit/enable/disable/delete; subscription updates replace only the subscription rule list and never custom rules. Node Selection uses the active subscription's horizontal group tabs, current-group search, header group testing, stable Default/Name/Delay sorting, direct `select` row selection, and trailing per-node testing; default preserves Mihomo order, name is case-insensitive and stable, and delay puts successful measurements before unmeasured/failed results. Long node names use a wrapping summary/list layout instead of the fixed navigation-row value slot; automatic groups remain non-selectable. Subscription rows switch the active subscription on tap and expose Update/Edit/Copy/Delete from a trailing overflow menu. Proxy Logs exposes the process-local bounded redacted history through view, copy, SAF export, and confirmed clear operations; raw YAML and controller credentials never enter it. The embedded Mihomo v1.19.30 runtime is loopback-only, has no TUN/LAN listener, consumes self-contained sanitized Clash.Meta YAML, and removes DNS matchers that depend on external GeoSite/GeoIP data or discarded rule providers before core validation; it also forces internal DNS not to re-enter business rules. Direct bypasses Kiyori's application proxy but cannot bypass Android system VPN routing. A detected external VPN blocks embedded startup until explicit concurrent-use authorization; authorized traffic is `Kiyori Mihomo -> system VPN`. Browser WebView proxy override is process-wide, so all remote WebViews share the `BROWSER` mode while localhost and local/private destinations remain bypassed by policy. Runtime reconciliation is serialized with encrypted-config writes and reads the current config under that lock; an expected stop is owned by the stop caller, while a Mihomo process exit not owned by a stop remains `ERROR` even when its exit code is `0`. |
| **Package Environment Drawer** | The three-state draggable bottom drawer opened by the first Extensions top-bar action. It reuses the same shared hidden/partial/expanded gesture surface as Browser child drawers without making Extensions depend on the Browser domain. Its fixed upper controls contain the title summary, metadata-only search, and an `All` entry followed by imported environment-bearing `ToolPackage.category` values in case-insensitive English A-to-Z order. The Script tab and drawer share one category policy: 18 registered categories have distinct light/dark icon-container pairs and semantic icons, custom categories receive a stable generated identity, and package display names sort by English or Simplified-Chinese pinyin initial with localized and internal-name tie breakers. Drawer groups use a compact two-line header with a `32dp` category icon instead of a name initial, and expanded variables use a `42dp` single-line editor. On every open, the initial expanded set contains every package with at least one required variable whose latest saved value is blank; packages whose required variables are complete, and packages with optional variables only, start collapsed. Groups may then be independently expanded or collapsed. Search matches display name, internal package name, environment key, and localized description, but never an environment value. One in-memory draft is shared by duplicate key declarations; dismiss, scrim, Back, and Cancel discard it, while Save alone merges through the existing single `EnvPreferences` owner. `ToolPackage.env`, package activation validation, and MCP environment configuration remain unchanged. |
| **Browser Runtime** | The single in-process WebSession state owner shared by Browser Home, the 1×1 background anchor, the minimized indicator, and Operit AI browser tools. The anchor and indicator own no tabs, navigation, downloads, or userscript state. |
| **Kiyori Public Store** | The single host writer for Kiyori-owned public downloads, exports, and images. Android 10+ uses pending MediaStore rows and deletes an uncommitted URI on failure; Android 8/9 writes the same projected hierarchy and scans the completed file. It owns collision-safe final names and returns the actual URI, display name, and user-visible path. |
| **ToolPkg Data Namespace** | A host-bound container identity shared by one ToolPkg and its subpackages. `privateData` is durable no-backup state, `cache` is rebuildable state, and both accept only validated relative paths under per-package quotas. Public compatibility files remain separate, while old public data can enter private generations only through a user-selected SAF tree and a registered package-specific migrator. |
| **Page Source Workbench** | The Browser Menu's status-bar-safe full-screen native editor for the active WebSession's live DOM snapshot. It preserves the document type, excludes Kiyori-owned selection UI, and binds the editable baseline to one session and one in-memory Document token. The workbench opens in horizontal browsing by default; both horizontal browsing and visual soft wrapping start at the document's top-left origin, and switching either mode returns to `(0,0)` without following a stale caret. Minified HTML, inline scripts, and long attributes use visual soft wrapping without inserting source characters. Long-line analysis, line navigation, cursor/selection status, bidirectional search/replace, accurately enabled undo/redo, formatting, copy, reload, diff review, retained process-local edits, and confirmed application all operate on one `NativeCodeEditor` buffer and history. Horizontal long-line drawing uses cell checkpoints to enter the visible range without rescanning from the logical line start; pinch zoom updates text geometry without rebuilding the wrapped layout for every gesture event, then rebuilds once after the gesture settles. The long-line information surface has a right-side close action whose visibility state is local to the current session and Document token; dismissing it does not mutate source text, wrapping policy, or statistics. Applying rebuilds the same Document with `document.open/write/close`, refreshes the shared runtime helpers and userscript state, and creates no second WebView, navigation entry, network-source archive, or persisted page copy. Refreshing or navigating reloads site content. AI reads the same live or editor source through `browser_page_source`; user confirmation remains required for the workbench Apply action. |
| **Browser Plugin Center** | The draggable Browser Menu child drawer opened by the browser's Plugin button. It projects browser-plugin providers into overview/search/add actions without owning another registry. Its overview has provider-level `This Page / Installed` sections; userscript rows appear only inside the Userscript Manager workbench, never again under the provider card. The built-in Userscript Manager workbench has `This Page / Installed / Updates / Logs`, compact multi-selection, script details, explicit safe-versus-risky update review, and a browser-hosted full-screen native source editor. The Plugin button and every primary plugin/script icon in those child surfaces use the same low-saturation `PLUGINS` deep-plum identity; availability, runtime error, success, warning, and update-risk state indicators retain their independent state colors. The top-right Add menu contains New/URL/Local actions and verified plugin-library sources. Logs open detail on tap, copy on long press, and expose full-report copy/export actions. The editor uses a status-bar-safe 44dp header and one bottom control layer at a time. Private drafts, immutable revisions, transaction journals, logs, values, source and dependencies remain owned by the single userscript repository/runtime. This surface is unrelated to AI ToolPkg, Skill, MCP, workflow, or package management. |
| **WebSession Profile** | The immutable website-data boundary of a WebSession: `normal` uses the default WebView profile, while each live `incognito` window group shares one uniquely named Kiyori AndroidX WebKit Profile generation. Closing the last window retires that generation immediately; the next group uses a new generation, and the next cold start deletes all retired Kiyori Profiles before loading any of them. |
| **Browser Home** | The immersive Kiyori browser root. It hides the five-item Kiyori bottom navigation and presents the shared Browser Runtime through browser-owned top-bar search, full-screen search, normal/incognito window overview, fixed browser-menu drawer, draggable child drawers, and bottom-bar chrome. |
| **Browser Presentation Owner** | The exclusive host currently mounting the active browser WebView: `APP_SHELL`, `BACKGROUND_ANCHOR`, or no presentation owner while the WebView remains session-owned and detached. `APP_SHELL` is the only complete visible browser UI; `BACKGROUND_ANCHOR` is a 1×1 invisible, non-touchable, non-focusable attach point used only for an explicitly minimized browser or an AI-activated browser while Browser Home is not mounted. Browser Home owns a one-shot App presentation lease whose terminal mode is `DETACH`, `MINIMIZE`, or `DESTROY`: Compose reconstruction detaches without showing an indicator, AI round trips minimize, and normal user exit destroys only the presentation host. An explicit exit followed by Compose disposal cannot release twice. Repeated requests for the current owner are no-ops. A real `APP_SHELL <-> BACKGROUND_ANCHOR` change removes the old WebView and background window, verifies a null parent, crosses one `Choreographer` frame, and only then attaches the same View to the requested host without navigation or reload. |
| **Player Runtime** | The main process owns one `PlayerSession`, while the non-exported `:player` process owns the single MPV core. Together they own one immutable media request at a time, in-memory request headers, content file descriptor, playback/track/render state, and one explicit Surface lease state machine. A lease records the floating/fullscreen role, owner-instance token, monotonic generation, current and pending owner, native attach/detach state, transfer target, and one-shot fullscreen Activity launch/finish requests; Android `Surface` objects remain session-private. Fullscreen and browser-floating presentations transfer only the Surface and never reload media. The old owner must report `surfaceDestroyed`, then `PlayerSession` completes the single native detach before a pending target may attach. Both presentations use `PlayerSurfaceView`; browser floating is fixed full width, fixed `16:9`, zero side margins and square corners, initially attached to the browser bottom bar and movable only on the Y axis until its top edge reaches the browser top bar. Browser floating marks its Surface as a media overlay while the existing unique WebView retains its normal layer type. A new request creates and initializes the single mpv core before Android Surface callbacks. The first valid Surface is attached only after `mpv_initialize`; `loadfile` remains pending until that Surface lease completes native attach. This ordering matches the packaged mpvlibAndroid binding and the legacy Kiyori player. Decoder backend is independently persisted as `software/mediacodec/mediacodec-copy` and owns `hwdec`; rendering profile is independently persisted as `fast/default/high-quality/low-latency/sw-fast` and owns the mpv profile. The unpublished default is `software + fast`. GPU Next selects `vo=gpu-next`, Vulkan selects `gpu-context=androidvk`, and both renderer switches apply when the unique mpv core is next created. Anime4K and playback speed begin from their stored value only when their independent memory switches are enabled. Volume enhancement raises mpv software volume to `150%` under a `300%` ceiling. The fullscreen control layer is the source-faithful `kiyori-android@24a2dfa9` layout and original drawable set. Its top status columns always expose value and unit/time as two single lines with identical `10sp` size, `11sp` line height and bold weight; the subtitle, danmaku, audio and aspect buttons keep their outer positions while rendering `24dp` icons. Its bounded `PlayerDebugLogBuffer` merges main-process lifecycle with `:player` command, MPV verbose, file-event and error records through the existing ordered AIDL callback. Each entry receives severity, stable sequence ID and one or more write-time diagnostic topics. The viewer polls the buffer revision at a bounded cadence, renders structured entries newest-first in a lazy list, and exposes one non-wrapping horizontal strip for all, error, warning-and-error, network/loading, playback, Surface/render, track/subtitle and runtime/MPV views. Header close and footer clear/copy/export stay outside the weighted log viewport; clear requires a second tap. Copy and export build a chronological full diagnostic report from the current view. Reports omit request-header values, Cookie, Authorization, URL query values, IP/DNS/proxy addresses, network handles, titles and private paths, and exports are written to `Download/Kiyori/exports/player`. Its arm64 native stack contains non-overlapping mpv and FFmpegKit AARs plus the single `libc++_shared.so` produced with the same Clang toolchain as `libmpv.so`; build and APK gates verify the required C++ symbols before delivery. |
| **Player Native Network Stack** | The non-exported `:ffmpeg` process exclusively loads the selected FFmpegKit wrapper `8.1.7-kiyori-n9.0.1-r6`, FFmpeg `n9.0.1`, OpenH264 `v2.6.0`, GPL-enabled filters, HarfBuzz and the qualified `drawtext`/`eq`/`boxblur` set through normal `libav*.so` names for AI tools, media processing and download remuxing; the main process submits bounded requests through Binder and never loads the FFmpegKit JNI closure. One FIFO worker is the sole top-level native execution owner, while request/session/result state remains independent and FFmpeg codec/filter/scheduler threads remain available inside the active command. Before transcode, the Android compatibility layer preserves an already-started application Binder threadpool instead of attempting to shrink it; callback completion drains both the active session and session `0` diagnostic queues before terminal delivery. Native FFprobe writes request-scoped private JSON through `-o`; Kiyori parses only bounded format/stream fields and never depends on FFmpegKit 8.1.7 reconstructing FFmpeg n9 stdout from logs. FFmpeg capability stdout is routed into the bounded FFmpegKit log callback, so `ffmpeg_info(section)` can observe version and list output without a Shell. The non-exported `:player` process resolves `libmpv.so` and `libplayer.so` only against the selected source-built FFmpeg `n9.0.1` closure, whose seven ELF files use equal-length `libmp*.so` SONAME/`DT_NEEDED` names and whose `libmpformat.so` was compiled with Mbed TLS `3.6.7`, RSA-PSS enabled and curl disabled. The selected player and FFmpegKit product AAR SHA-256 values are `F52ACA6F35C651BE7AAB55F2EFE6B5F40180D1EBAEB1404CC446470BF8DEB6A4` and `7E6B4C20A93DFB3B90BC7F3C5D724CF657B70E2469EA4F2B1110396A8D345394`. All 19 arm64 native members have no `RPATH`/`RUNPATH`, use `PT_LOAD >= 0x4000`, and are stored at ZIP payload offsets aligned to `0x4000`; the player AAR uniquely supplies `libc++_shared.so`. The FFmpegKit AAR carries a byte-exact GPLv3 license resource. Player initialization copies the fixed `cacert.pem`, sets `tls-ca-file`, keeps `tls-verify=yes`, and disables the unshipped ytdl hook. Build and APK gates reject FFmpeg 8 markers or majors, normal/namespaced basename overlap, cross-namespace dependencies, missing TLS/RSA-PSS/JNI/SAF/GPL/HarfBuzz/filter/license markers, duplicate entries, a mismatched C++ runtime, host path tags, or unaligned native ZIP payloads. |
| **FFmpeg Runtime Conversion Contract** | Every `:ffmpeg` Binder request has a fixed payload bound: `65,536` command characters, `1,024` argument entries, `16,384` characters per argument, `65,536` total argument characters, and a `4,096`-character probe path. A queued cancellation never emits started or enters native execution; its only legal result is `CANCELLED` with `sessionId=0`, return code `255`, zero duration and no native metadata, while active cancellation retains the real session. Native return codes remain signed FFmpeg AVERROR values; protocol failure and process death expose `returnCode=null`, and process death preserves any observed PID/session plus its private diagnostic-log path. Request logs are exclusively created, capped at `4 MiB` of valid UTF-8 including one truncation marker, and retained under `48 hours / 32 files / 32 MiB`; active and reserved paths are protected, normal terminal logs are read and deleted, late cancelled-coroutine logs are discarded, and process-death logs retain a diagnostic path while exception text is bounded. `ffmpeg_info(section)` routes fixed Android capability queries without Shell, `ffmpeg_probe(input_path)` runs Android FFprobe with a request-scoped private JSON capped at `1 MiB / 128 streams / 4096 characters per string`, and `ffmpeg_execute` rejects standalone Shell operators/redirections and leading executable prefixes without rewriting valid FFmpeg arguments. Android `drawtext` commands must use an absolute `fontfile`. The unpublished simplified conversion API has one qualified profile, `h264_aac_mp4`, mapped exactly to `libopenh264 + constrained_baseline + yuv420p + aac + mp4 + faststart`; output validation accepts both FFprobe's `Constrained Baseline` label and the equivalent numeric H.264 profile `578`, while ordinary baseline `66` remains invalid. It accepts only absolute non-identical paths, even `16..8192` dimensions and `64k..100M` video bitrate, never overwrites the final `.mp4`, writes a same-directory UUID staging file with `-n`, probes positive duration/container/codec/profile/pixel-format/resolution/audio invariants, and commits only through a same-directory atomic move serialized for the same in-process target. Unsupported profiles, invalid output, unavailable atomic move or any runtime/probe failure fail explicitly without retrying, changing encoders or switching to Ubuntu/player execution. The profile's OpenH264, AAC and MP4 muxer markers plus GPL/HarfBuzz/`drawtext`/`eq`/`boxblur` and the GPLv3 notice are part of the machine-readable FFmpegKit manifest and AAR/APK gates. |
| **Player Native Closure Upgrade** | The selected local product is dual M9 with FFmpegKit r6: mpv `2339eb727`, FFmpeg `n9.0.1`, Mbed TLS `3.6.7`, RSA-PSS enabled and curl disabled in `:player`, plus FFmpegKit framework `62b07bf097baf26b416c815aea514e05c9ad6d63`, wrapper `8.1.7-kiyori-n9.0.1-r6`, FFmpeg `n9.0.1`, OpenH264 `v2.6.0@652bdb7719f30b52b08e506645a7322ff1b2cc6f`, GPL/HarfBuzz and `drawtext`/`eq`/`boxblur` in `:ffmpeg`. The explicit constrained-baseline mapping and locked BsFlush empty-word patch remain necessary because the OpenH264 release predates upstream commit `40555ec684ec0fede3948c8f272c04d88d05189d`; r4 introduced the fixed FFmpeg Android Binder-threadpool preservation patch, r5 added FFprobe private JSON and HarfBuzz/`drawtext`, and r6 adds capability stdout capture, GPL-gated filters, exact GPLv3 notice packaging and the tool/profile contract fixes. The player source AAR SHA-256 is `7CB0B25DC15F21278992243CE2597193B7A54E1A488933555B572982203E2BC0`; its selected aligned thin/product AAR SHA-256 is `F52ACA6F35C651BE7AAB55F2EFE6B5F40180D1EBAEB1404CC446470BF8DEB6A4`. The FFmpegKit r6 source and selected aligned thin/product AAR SHA-256 values are `0BD7ADDAE2D17960DB940A17A3E2450ACB83EECB46BE0D3800D051BB2075C1CE` and `7E6B4C20A93DFB3B90BC7F3C5D724CF657B70E2469EA4F2B1110396A8D345394`. Both FFmpeg namespaces use majors `.63/.63/.12/.63/.61/.7/.10`; the two product AARs remain process-isolated and exact-hash qualified. M8 and FFmpegKit r2/r3/r4/r5 are retained only as dated attribution, root-cause, device-evidence and build-chain baselines, never as runtime selectors or downgrade paths. The manifests under `tools/player_native_build/` and `tools/ffmpegkit_native_build/` are the machine-readable source and selection contracts. The r5 device media matrix passed, but r6 deltas plus complete cancellation/process-death and player acceptance remain `verification_pending`. |
| **Browser Media Candidate** | Session-scoped evidence captured passively from the existing WebSession request path or a read-only DOM bridge. It preserves the browser-provided URL and actual request identity fields; MIME and URL shape are confidence evidence, never permission to rewrite or resolve another URL. |
| **Modal AI Drawer** | The Kiyori-owned, button-triggered left overlay that exposes AI top-level navigation without moving or rebuilding the page below it. Its scrim covers the window while its panel begins below the status bar. It has no edge or drag gesture. The Package, Toolbox, and Workflow quick cards reserve a dedicated top lane for count/status badges so text such as “正常” never covers the centered semantic icon. The Toolbox badge is derived from the authoritative navigation registry, but the visible Toolbox surface no longer contains the file-manager or legal-document host entries. Its bottom `设置` action opens the source-preserving Settings Home child without replacing the current AI route or stack. |
| **AI Top-Level Page** | AI Home, Packages, Permission Grant, Workflow, Memory, Toolbox, or a ToolPkg drawer destination. Native roots retain independent state and child stacks; ToolPkg roots retain them only when their route declares `keepAlive=true`. |
| **AI Assistant Settings** | The one AI-owned settings root, shown to users as `AI助手`. Its fixed five-group, thirteen-entry order is `模型与生成: 模型与 API / 功能模型分配 / 提示词与角色`, `个性化与交互: 用户资料 / 虚拟形象 / 回复与表情`, `语音: 文本转语音 / 语音转文本 / 语音唤醒`, `上下文与工具: 上下文与总结 / AI 工具授权`, and `服务与用量: AI 用量与费用 / 局域网与自动化`. Persona generation remains reachable from Prompts and Personas but is not duplicated at the root. Every entry retains its existing repository, preferences, service, runtime, serialization, and `Screen` route as the sole state owner. Long forms, editors, statistics, and multi-tab pages use `KiyoriSettingsWorkspacePage`; list-oriented pages use `KiyoriCollapsingSettingsPage`. API keys and service tokens are masked by default, destructive actions require explicit confirmation, and asynchronous failures are logged and projected through Snackbar or visible error state. In Model/API, model names remain an ordered chip list: the first chip is the test model, collapsed layout places at most three complete rows plus an inline hidden-count indicator, visible chips wrap long names, tap copies, the close action deletes, long press opens drag sorting, and manual input accepts one or multiple names. Removing bound models requires explicit confirmation and a remaining test model. Connection testing covers chat and every enabled native Tool Call, image, audio, and video capability. Primary Settings Home uses `RouteEntrySource.KIYORI_SETTINGS`; source-preserving Settings Home pushes the page onto the current AI stack so Back returns to that Settings Home before the original browser or AI page. |
| **AI Navigation Root** | An AI top-level route explicitly identified by its registered navigation-entry ID. Root ownership is stored on the route entry itself and is never inferred from route arguments, stack depth, or an instance-ID naming convention. |
| **Kiyori Edge-to-Edge Shell** | The window contract in which Kiyori-owned page backgrounds and the Modal AI Drawer scrim paint behind a transparent status bar. Interactive page content applies system insets, while the drawer panel itself starts below the status bar. |
| **Fixed Kiyori Theme** | The Kiyori-owned application visual contract. Light mode uses `#FFFFFF` backgrounds, `#202124` primary content, primary `#1E88E5`, and secondary `#536D79`; dark mode uses near-black backgrounds, layered gray surfaces, primary `#90CAF9`, and secondary `#B9CBD4`. Users can select light, dark, or system mode but cannot override the whole application palette. `KiyoriSemanticTone` supplies theme-aware blue, green, purple, orange, red, cyan, and pink icon/container pairs without becoming another persisted theme. AI chat background, bubbles, avatars, header, input, and local fonts remain AI-local personalization. Browser-owned surfaces use a fixed neutral ColorScheme and allow only small semantic accents for content tools and statuses. |
| **Permission Center** | A Kiyori system-level read-only overview and navigation destination that separates device capabilities from AI tool authorization. Its overview contains no direct permission switches and does not own another copy of either domain's persisted state. |
| **Device Capability Authorization** | Android and privileged-execution capabilities granted to Kiyori itself, including runtime permissions, file access, overlay, accessibility, battery exemption, Shizuku, Root, and debug capability. It is distinct from permission to invoke an AI tool. |
| **AI Tool Authorization** | The Operit AI policy that decides whether a registered tool is allowed, requires confirmation, or is forbidden. `ToolPermissionSystem` remains its single persisted source of truth. |
| **Software Home Search Frame** | The central same-background input surface, `114dp` high on regular windows and `96dp` in the short layout, with a fixed five-color `1dp` outline and no outer glow. Search/AI occupy one `120×32dp` rounded segmented control with equal halves, a center divider, clipped rounded press feedback, and fixed light/dark blue selection. The segments select the meaning and placeholder without navigating: Search opens Full-Screen Web Search when the frame is pressed, while AI enters the existing AI Home, preserves its conversation and draft, and requests focus for the real chat input after pager settlement. The saved mode survives return and configuration recreation; a cold launch starts in Search. The title ring is `18dp`. Attachment, voice, and camera remain immediate AI Home shortcuts. |
| **Software Home Weather** | The top-left location-derived city, current temperature, and WMO weather icon. Android location and `Geocoder` determine the city; Open-Meteo receives latitude/longitude for the current Celsius temperature and weather code. The icon uses warm yellow for clear weather, blue for partial cloud/rain, cyan for overcast/fog/snow, purple for thunderstorms, red for missing location permission, and orange for unavailable data. No unverified city or weather value is displayed or persisted. |
| **AI Home Quick Action** | A request-ID-bearing one-shot command from Software Home. It becomes consumable only after the shared pager settles on AI Home, then delegates input focus and IME display, attachment, microphone/fullscreen voice, or camera capture to the existing AI input owner and is consumed exactly once. |
| **Full-Screen Web Search** | The shared web-only search surface opened from the Software Home Search Frame or Browser Home top bar. It shares the browser search-engine owner and normal-mode recent records, requires a real normal/incognito Profile control, and represents normal with an open eye and incognito with a closed eye. The control is a borderless standard icon action; mode changes show only a short inverse-surface bottom message. Browser Menu's Incognito action calls the same default-Profile toggle and message without closing the menu or converting the active tab's immutable Profile. Search-history tag deletion is staged until the user presses Done; Clear opens a bottom confirmation dialog and, once confirmed, clears the shared history immediately without a second Done action. Software Home always creates a new shared WebSession; Browser Home reuses the active tab only when its immutable Profile matches the selected Profile and otherwise creates a matching tab. It does not search bookmarks, browser history, files, mini apps, or AI conversations; those domains own their search pages. |
| **Settings Home** | The Kiyori-owned settings root uses four fixed three-item cards in the order `我的账号 / AI助手 / 小程序`, `网页浏览器 / 文件下载器 / 文件管理器`, `视频播放器 / 音乐播放器 / 文档阅读器`, and `界面定制 / 数据备份 / 更多功能`. Its 12 entries use 12 unique icons and a dedicated adaptive light/dark palette. `小程序`, `音乐播放器`, and `文档阅读器` remain honest inert reservations until their product-domain owners exist. File Manager opens the same `FILE_MANAGER` Shell child and `FileManagerScreen`/`FileManagerViewModel` owner used by File Management Home; an active Settings session remains stored while the child is foreground and is restored unchanged when the child closes. Browser, downloader, player, appearance, data, and More Features entries use the existing capability-level `KiyoriSettingsRoute` stack. Ad blocking is owned by Browser Settings → Content Filtering and is not a Settings Home entry; its nested `AD_BLOCK_*` routes remain available only from that Browser page. AI Assistant owns separate `文本转语音` and `语音转文本` settings routes, both backed directly by the shared `SpeechServicesPreferences` owner. `MORE_FEATURES` presents an `应用与隐私` group whose `用户协议与隐私政策` row pushes `KiyoriSettingsRoute.AGREEMENT` and reuses the current legal-document/version source, plus a `系统能力` group whose `权限` row opens the existing `Screen.ShizukuCommands` with `RouteEntrySource.KIYORI_SETTINGS` and the active settings `sessionId`; neither path copies persisted state. The legal-document route owns an opaque full-size background and a shared `WindowInsets.safeDrawing` content boundary, so Settings Home cannot show through its overview or document pages. Back from Agreement returns to More Features. Back from the Operit permission owner restores More Features, then Settings Home, before a source-preserving session returns to the unchanged Browser Home/WebSession or AI route stack. `KiyoriSettingsNavigationState` remains the sole settings-session owner: it stores a stable session ID, the `BOTTOM_NAVIGATION / BROWSER_HOME / AI_HOST / EXTERNAL_BROWSER_PRESENTATION` origin, the full route stack, and the current presentation. Primary Settings Home is the fifth bottom destination; Browser Menu and the Modal AI Drawer's bottom `设置` action open the same source-preserving settings session and hide the five-button bottom bar. The first three header actions remain inert reservations; the fourth is a `156dp` menu anchored below the button and writes `跟随系统 / 浅色模式 / 深色模式` directly to the sole `UserPreferencesManager` theme owner. Its visible icon follows the effective theme and switches between a warm sun and a cool moon. |
| **Settings Detail Surface** | The shared visual shell for Kiyori-owned settings children. Account, AI Assistant, Text to Speech, Speech to Text, Appearance, Data, More Features, Browser, Ad Blocking, Download, and Player settings use the same section title/description, borderless `16dp` group card, two-line setting row, `0.6dp` divider, Material Switch, `42%` dependency-disabled state, and `26dp`-corner selection sheet with a visible drag handle, current value, and option-description rows. Complex forms use one `14dp` outlined-field contract with the Settings Surface page fill, divider border, and blue semantic focus/cursor color. `KiyoriSettingsWorkspacePage` gives complex AI forms an opaque status-bar-safe `56dp` top bar, Settings background, Snackbar host, optional page action/FAB, and bottom safe-drawing inset without imposing another scroll owner. Virtual Avatar and Voice Wake-up remain separate children under the AI root and reuse `AssistantConfigViewModel`/`AvatarRepository` and `WakeWordPreferences`/speech runtime respectively. Settings-owned Operit child routes use direct opaque replacement semantics: the no-crossfade policy remains active until the next real route change, final alpha bypasses tween interpolation, the current target is drawn at `alpha = 1`, every retained non-current screen is drawn at `alpha = 0`, and AI Home remains composed without exposing its conversation or internal history drawer as a transition frame. The collapsing root title is one continuously transformed text element: the first `72dp` of list scrolling moves it from the expanded `32dp / 60dp / 26sp` frame to the pinned `56dp / 16dp / 20sp` frame while the header content height contracts from `128dp` to `56dp`; the navigation action and real status-bar inset remain fixed. |
| **File Management Home** | The Kiyori-owned file root reproduced from the fixed Legacy Kiyori reference. It renders eight categories, seven quick-access entries, and four storage locations with zero placeholder counts. The phone-storage row reads available and total bytes from the app's actual data volume on composition and host resume, and opens the shared `FILE_MANAGER` Shell child backed by the existing `FileManagerScreen` and `FileManagerViewModel`. The shared file-manager page owns an opaque full-size background and applies `WindowInsets.safeDrawing` to its toolbar and content while preserving the Shell's edge-to-edge background. Closing that child returns to File Management Home without creating another file state or operation owner; the other current page actions remain inert. |
| **Browser Settings** | The Kiyori browser-settings page whose visible root title is exactly `网页浏览器`, matching its Settings Home entry. It is a `KiyoriSettingsRoute.BROWSER` entry in the active settings route stack, so header Back and system Back pop the same route sequence before closing the settings session. It exposes eight groups and `2/4/3/3/2/1/5/3` entries: Content Filtering, Web Plugins and Scripts, Home and Navigation, Startup and Windows, Webpage Display, Webpage Interaction, Site Permissions and Data, and Media Sniffing. The top scope notice defines this page as the global capability ceiling. Content Filtering projects the sole `BrowserAdBlockStore` global switch and management route; Site Permissions and Data separates the website-password save/fill switch from the native password manager. Webpage Interaction contains the default-enabled `长按网页元素菜单` switch; changing it closes any active element menu and synchronizes every existing WebView without reload, while editable controls remain owned by Android WebView native selection. Web Plugins and Scripts contains one runtime authorization switch plus Plugin Center, permission/site scope, and combined current-page diagnostics/log navigation; it does not duplicate Userscript Installed, Current Page, and Logs workbench tabs as separate settings rows. Opening Plugin Center, diagnostics, or an individual script detail suspends the same settings session with a `BrowserWorkspaceReturnToken`, projects the requested Browser workspace over the existing Browser Runtime, and restores the exact settings route stack when that workspace closes. If the settings source had no Browser session, opening the workspace does not create a hidden configured-home window. `WebSessionBrowserSettingsStore` uniquely owns the custom home URL, return-without-reload switch, edge swipe-history navigation, three ordinary-window startup restoration switches, force-page-zoom switch, webpage text zoom, element-long-press-menu switch, webpage external-app and geolocation policies, website-password auto-save switch, search-bar media-candidate entry, automatic floating playback, its minimum duration, and the exact-host site-disable rules for those supported boolean capabilities. `BrowserCredentialVault` uniquely owns encrypted ordinary-profile website credentials; `CookiePrivacyManager` owns explicit ordinary-site Cookie clearing. The text-size and password-manager entries open native routes over the same Browser Runtime. The `MEDIA_CANDIDATES` drawer contains candidate filters and actions but no settings. Options without a real current owner or consumer remain absent. |
| **Browser Site Settings** | The Browser Menu's third-row fifth action opens the shared draggable `SITE_CONFIG` child drawer for the HTTP(S) host frozen at click time. Every control is negative and defaults off; the effective contract is always `globalEnabled && !siteDisabled`, so an exact-host rule may further disable a globally enabled capability but can never enable a globally disabled one. The drawer groups eleven controls as Content and Scripts, Page and Interaction, Permissions and Privacy, and Media. `BrowserAdBlockStore.allowlistedDomains` remains the sole ad-block site-disable owner and preserves its existing parent-domain coverage; the UI identifies an inherited parent rule and cannot pretend that deleting an exact child entry would re-enable blocking. `WebSessionBrowserSettingsStore.siteSettingsRules` owns the other ten exact-host disabled-feature sets and deletes an empty rule instead of persisting a second configured-state flag. Non-HTTP(S) pages show an empty state and create no rule. Return-without-reload is resolved from the actual Back history target; edge swipe-history navigation, force zoom, element long press, credential capture/fill, media badge, and automatic floating playback are resolved from the current page; external-app and geolocation decisions read the active page or request origin. Site-level userscript denial immediately blocks new bootstrap, bridge, menu, webRequest, and GM network work and revokes native authorizations, while already executed page-only DOM effects require an explicit page refresh and are never described as reversibly removed. |
| **Download Settings** | The Kiyori download-settings page is a projection of the single version-2 `BrowserDownloadSettingsStore`. Its five groups and entry counts are `5/4/2/2/1`: Downloader and Performance, Network and Background, M3U8 and Storage, Installation and Confirmation, and Network Protocol. Default destination is one selection across the application directory, public ordinary-file transfer, and a persisted SAF tree; public transfer and SAF remain mutually exclusive and `BrowserDownloadManager` owns permission release. Network policy selects any network or Android-classified unmetered network, roaming is an independent opt-in, result notifications can be disabled, and the system notification-settings entry opens the app channel settings. When Android System DownloadManager is selected, only internal-engine-owned rows are dependency-disabled; engine choice, network policy, roaming, system notification settings, and skip-confirmation remain available. |
| **Browser Download Runtime** | `BrowserDownloadManager` remains the only persisted Kiyori task, queue, scheduler, and file-owner. Its `AtomicFile` JSON records publish one `StateFlow` projection shared by Browser Home, Shell, Minus-One, the Download Drawer, notifications, and AI events; no WorkManager database or second download owner exists. File names are reserved before queue insertion, confirmation requests use a bounded FIFO, persistence is serialized and atomic, and interrupted HTTP work is restored to an explicit non-networking state until the user resumes it. Android 14 and newer run network work through one persisted user-initiated data-transfer `JobService`; Android 8 through 13 use one `dataSync` foreground service. The runtime notification reports aggregate progress and speed, supports Pause All, and opens the shared Download Drawer; terminal notifications report completion or failure. Each new task freezes concurrency, ordinary Range threads, M3U8 threads, offline-package policy, public-transfer policy, chunk size, HTTP protocol, network policy, roaming policy, and optional SAF target. Runtime scheduling and Android `DownloadManager` both enforce the selected network and roaming rules. Ordinary HTTP uses one task-owned `BrowserDownloadTransport` with bounded retry classification, strong ETag or Last-Modified `If-Range` validation, exact `206`/`Content-Range` checks, rollback of failed attempts, and `max(chunkSize, 1 MiB)` range parts. M3U8 text is size-bounded and must begin with `#EXTM3U`; package mode follows at most four highest-bandwidth master levels, rejects cycles and external audio/video/subtitle rendition playlists rather than producing an incomplete package, deduplicates repeated and byte-range resource URLs, downloads `KEY`, `MAP`, `PART`, and media resources in bounded batches, removes live-only preload hints, verifies every local resource before commit, and keeps the playlist plus companion directory together for rename and delete. Completed ordinary files can rename, change suffix, move to SAF, copy their original URL or saved location, share, transfer publicly, and redownload. Status output removes URL credentials, query, and fragment; explicit Copy URL retains the original address. APK auto-clean no longer uses a timer: the task record persists the expected package name and version, a SAF APK uses a short-lived cache copy only to read package metadata while the installer receives the original granted SAF URI, and the original task/file is removed only after Android broadcasts an exact installed package/version result; disabling the setting clears pending install state. A confirmed `SYSTEM` request is owned only by Android `DownloadManager` and is never mirrored into Kiyori JSON or progress. The Download Drawer adds case-insensitive search, status filters, visible eligible Select All, separate batch cancel/delete semantics, accessible controls, and explicit record-only versus record-and-file deletion. |
| **Browser Network Log** | The current WebSession's in-memory current-document resource directory captured by its existing Android WebView `shouldInterceptRequest` path. Each session keeps at most 2,000 normalized resource identities and clears its own directory on navigation or explicit user clear. Search and `all / video / audio / image / web / script / style / data / font / other / blocked` filters are presentation state. A cross-host marker is a factual host comparison, not an ad or failure verdict. Copy, download, external-open, details, bounded image thumbnails, and the shared full-screen image viewer use the recorded URL, current Profile request identity, and existing browser/download owners. The same viewer also serves webpage-element full-screen and bounded page-image mode, but those snapshots do not write entries into the network log. The log does not invent response status, response MIME, response bodies, server failure state, or playback capability. |
| **Kiyori Capability API** | UI-independent contracts through which Operit AI observes and operates Kiyori browser, media, reader, file, download, and later domains. |
| **High-Impact AI Action** | An AI-requested operation with destructive, privacy, financial, external-communication, account, permission, or persistent-state impact. It requires explicit authorization and an inspectable operation record. |

## Navigation contract

- App launch opens the center Software Home page.
- The Software Home horizontal order is Minus-One Page, Software Home, then AI Home.
- All three home pages use one `PagerState` and one product snap contract. Minus-One and Software Home use the native `HorizontalPager` fling path; the permanently mounted AI Home uses a public-API bridge with a fresh per-gesture session, strict `> 0.5` positional threshold, inclusive `400dp/s` velocity threshold, at most one page, the same LTR/RTL direction mapping, and the same bounded spring. Movement follows the pointer, a real reverse drag can cancel an unfinished settle, a tap remains available before horizontal touch slop, and Shell state changes only after the pager settles.
- The five primary destinations are Software Home, Browser Home, Mini App Home, File Management Home, and Settings Home.
- The five-item bottom navigation is visible on Software Home, Mini App Home, File Management Home, and Settings Home. Browser Home is an immersive root with its own bottom bar; Minus-One Page, AI Home, Full-Screen Web Search, child pages, and immersive content pages also hide the Kiyori bottom navigation.
- Settings navigation is one explicit source-aware route stack. Bottom Settings unwinds detail routes to Settings Home and only then returns to Software Home. Browser Home opened from the Settings bottom destination suspends the same settings session and restores it on exit; a restored Settings Home without a transient session establishes one before pushing a child route. Browser Menu and the Modal AI Drawer unwind the same detail routes to Settings Home and then close the settings session, revealing the original Browser Home/WebSession or AI page/router stack. Operit settings routes carry the active settings `sessionId`; Browser plugin workspaces carry a one-shot return token. `KiyoriShellChild` no longer owns Settings Home or any settings category.
- The current browser presentation contract distinguishes the Shell return target from the terminal presentation mode. Only Browser Home mounts complete browser chrome, drawers, text-selection actions, and the browser floating player. Entering Browser Home records the actual App Shell source among Minus-One Page, Software Home, AI Home, Mini App Home, File Management Home, and Settings Home. The Browser Home top-left action releases the current presentation and restores that recorded source without consuming WebView history. Normal entries close the presentation without showing the minimized indicator. Browser Menu's AI Dialogue action and AI Home's top-right Browser action use `MINIMIZED_INDICATOR`; the first enters AI Home and requests real chat-input focus, while the second returns to the preserved AI Home. AI `browser_*` operations may also create the background anchor and indicator when Browser Home is not mounted. Tapping an existing indicator uses a dedicated restore action so the next terminal system Back or top-left exit restores that same indicator instead of becoming a normal Browser entry. Long-pressing it consumes the open action and creates one transparent `28dp` temporary WindowManager close action outside the indicator's upper-right edge, rendering a smaller `16dp` red close icon. That close window is non-touchable while the original gesture remains pressed, becomes touchable after release, follows indicator dragging, stays inside the physical display bounds, and is removed after three seconds. Activating it routes through the same `onExitBrowser` presentation teardown used by the fourth browser-menu row. The temporary close window owns no WebView, session, navigation, download, or Browser Runtime state. Browser Home activity keeps no empty background-anchor window, direct Browser Home entry does not pre-mount the anchor, and cross-ViewRoot transfer uses a detach/frame/attach transaction. Historical expanded-overlay, overlay Back, cutout, IME, and focusable-window clauses retained in older long-form records are superseded and are not current behavior.
- Browser Home is the only complete presentation of one Browser Runtime. Tabs, the active WebView, history, bookmarks, downloads, userscripts, and pending confirmations are never copied into the 1×1 background anchor or minimized indicator. `WebSessionHistoryStore` owns one `history_json`: normal Profile main-frame visits write `WEB` records, and the unique `PlayerSession` writes accepted media requests as `VIDEO` with `ONLINE` or `LOCAL` origin. A media write removes the same URL from the web category so video resource links never appear as webpage history. The store never persists Cookie, Authorization, or request headers; online replay reconstructs the current User-Agent, matching Profile Cookie, and source-page Referer when the user opens the record. Browser Menu and Minus-One mount the same searchable, category-filtered, time-range-deletable history drawer. Browser downloads retain one atomic JSON record set as their only persisted Kiyori task state, publish one snapshot `StateFlow`, and use one global FIFO scheduler. Every browser child drawer uses `WebSessionDrawerHeader` as the single header geometry owner: `52dp` height, `34dp` semantic badge, `8dp` badge-to-title gap, one title hierarchy, and distinct title-adjacent versus trailing action slots. Bookmark, history, downloads, network log, media candidates, placeholders, and plugin/userscript pages therefore cannot independently drift their header spacing. Project-owned modal content from those drawers uses `WebSessionBrowserDialogSurface` for the semantic badge, title divider, `20dp` shape, border, elevation, and scrim language; the history time-range selector preserves its bottom-sheet interaction while reusing the same dialog header. The shared Downloads drawer uses the green semantic tone; its four-bar menu remains immediately after `我的下载`, retains a `36dp` target, and clips press feedback to a circle so no square shadow appears. These are presentation-only changes over the same manager state. Each HTTP task freezes ordinary Range concurrency, M3U8 concurrency, offline-package/public-transfer policy, chunk size, protocol, target directory, network policy, and roaming policy when created; later settings changes do not alter its running plan. Internal dispatch enters `BrowserDownloadManager`; system dispatch gives Android `DownloadManager` complete ownership and never mirrors progress into Kiyori. Website data remains isolated by immutable WebSession Profile, normal sessions alone write shared search/browsing history, and Browser Home entered from AI Home returns to AI Home after releasing the App Shell presentation.
- Browser Plugin Center is a control-plane projection over the same Browser Runtime. `BrowserPluginCenterFacade` publishes the built-in Userscript Manager's installed, enabled, current-page, page-menu, support, pending-install, update and global runtime-authorization facts without storing them. Browser Menu opens `PLUGINS / OVERVIEW`; userscript links and update previews open `PLUGINS / USERSCRIPTS`; script rows may continue to `USERSCRIPT_DETAIL`, and new or existing private drafts open `USERSCRIPT_EDITOR` as a browser-hosted full-screen native page rather than another WebView. Back unwinds editor, detail, userscript workbench and overview in order. Leaving an editor with unapplied content opens an explicit keep-private-draft, discard, or cancel decision. The editor keeps its background edge-to-edge but applies status-bar padding to the content column, uses a 44dp header, requests resize for the IME, shows only the 36dp symbol row while typing, and otherwise shows the browser's 44dp bottom-action geometry with navigation-bar padding. The Overview uses provider-only `This Page / Installed` sections; individual scripts are rendered only in the Userscript Manager workbench, so the same script is not shown twice. The workbench uses `This Page / Installed / Updates / Logs`; installed rows support long-press multi-selection and batch enable, disable, or confirmed delete. Checking updates is read-only. A user-triggered batch apply commits only updates whose source identity and origin are unchanged and whose grants, connect hosts, and page scope do not expand; every risky update remains pending until its permission and unified source diff is reviewed explicitly. Script details project rules, run timing, injection world, frame policy, grants, sites, dependencies, source, logs and immutable revision history from the same repository. Log rows open details on tap, copy directly on long press, and the Logs workbench copies or exports all retained entries independently of the active filter. Draft autosave, metadata editing, Terser syntax validation and formatting, permission/source diff, staging and atomic application do not write executable source into the active revision before confirmation. The top-right Add menu contains New/URL/Local actions and verified library sources, which open in an active new WebSession tab. The persisted `Allow User Scripts` permission defaults on for a fresh install; disabling it keeps the WebView-lifetime page and shared isolated registrations inert, returns no script payload, clears per-script capability authorizations, reply proxies, page menus and webRequest state, and cancels active GM requests while preserving each script's desired enabled state. Browser Settings observes this same userscript state and exposes the master permission, Plugin Center, a read-only permission/site overview, and one combined current-page diagnostics/log entry; Installed, Updates, and Logs remain workbench tabs rather than duplicate settings rows, and no second plugin setting is persisted. Each WebView owns one isolated userscript runtime; individual scripts still receive distinct capability tokens and grant sets. `unsafeWindow` with privileged grants remains in that isolated runtime and reaches the original page `window` only through a synchronous DOM-backed page-object bridge that contains no native bridge or capability token. Explicit page-world injection with privileged grants remains unsupported. Status distinguishes no active page, matched, queued, running, completed, failed, and genuinely unmatched; the same matcher feeds bootstrap, manager baselines, and UI diagnostics. Matching treats an omitted `@match`/`@include` as all pages, supports `http*`, explicit ports, common host wildcards, regular-expression flags, and document-body timing, and resolves document-start matching from the injected document URL instead of a possibly stale `WebView.url`. Plain `@connect` domains include their subdomains, and each webRequest or HTTP redirect hop is re-authorized. `GM_info` and `GM.info` always expose the same local metadata-only object, including the stored author, homepage, support/update URLs, grants, connects, page rules, resources, and raw run-at value; neither symbol grants native host capabilities. `@grant none` still exposes no privileged GM method or native bridge. Chrome/Edge extension packages are not accepted by Android System WebView, and AI-generated scripts or future plugins must pass local validation, permission review, user confirmation, and the owning provider's install transaction.
- Operit AI discovers human-created browser state through `browser_tabs list`. The list enumerates the same Browser Runtime used by Browser Home and the background anchor, exposes each stable `session_id`, `profile`, title, URL, index, and active state, and requires the intended tab to be selected before `browser_snapshot`, click, type, or navigation operations. `browser_tabs create` accepts `profile=normal|incognito`; omission uses the Browser Runtime default Profile. Incognito isolates website data, not Kiyori AI actions already authorized by the user.
- Browser Home's top bar is ordered Back, Search, and Refresh. Refresh always reloads the active WebView, retains the refresh glyph during loading, and uses the same circular press surface as the full-screen search Profile action. The top-left action is App Shell navigation: it releases Browser Home using the recorded `CLOSE` or `MINIMIZED_INDICATOR` presentation mode and restores the recorded entry page without calling WebView Back. System Back and the browser bottom-left Back action retain the ordered browser state machine: dismiss text selection, web dialogs, download confirmation, menu or child drawers, the engine panel, and full-screen search; then navigate the active WebSession's history and configured-home root; then leave Browser Home using the recorded presentation mode. Search opens the shared full-screen web search surface without recreating the active WebView. Its Profile control is always present; submission reuses the active tab only when the selected immutable Profile matches, and creates a matching shared WebSession otherwise. Engine selection and recent records remain browser-owned state. The full-screen search top row reuses Browser Home's exact `8dp / 6dp / 40dp / 42dp` outer-gap-action-input geometry, so Back, the fixed-width input, and Profile occupy the same absolute slots. The input wraps instead of scrolling horizontally, grows downward from one through three visible lines with a top-anchored content-size animation, and then keeps its three-line viewport while the text scrolls vertically. The full-screen search surface shows the selected engine icon instead of its name, opens a blue-tinted engine panel over the lower content, presents the active page title and URL with copy/edit actions, and renders search records as adaptive tags whose staged deletions commit only from the explicit Done action. The bottom bar is ordered Back, Forward, Home, Tabs, and Browser Menu. Home navigates the active WebSession to the custom home URL, and human/AI new-tab creation uses the same URL. App Shell and browser bottom bars use five equal slots with `16dp` horizontal, `0dp` top, and `6dp` bottom padding, `44dp` circular targets, and `26dp` icons; the App Shell center icon is `25dp`. The browser window counter is a `20dp` square with a `1.75dp` outline. Browser Menu opens a content-sized four-row surface: its three five-column rows use `21dp` outline icons in `32dp` containers with `1dp` row spacing, while its fourth row directly distributes three `46×36dp` actions with `22dp` icons and no synthetic five-slot placeholders. History, bookmarks, downloads, userscripts, network logs, page source, and capability explanation pages open draggable child drawers. The History drawer uses a compact title/count/delete header, title/link/source search, `All / Web / Video / Music / Novel / Other` horizontal filters, and a time-range deletion sheet for past hour, past 24 hours, past week, or all time. Web rows open in the Browser Runtime; online and local video rows open the unique PlayerSession and are labeled separately. Long press opens the shared HISTORY modal action surface: web records expose Open Web, Add Bookmark through the existing bookmark editor, Copy Link, Copy Title, Delete Record, and Batch Delete; video records expose Play Video, Open Source Page only when a valid saved source exists, Copy Video Link, Copy Title, Delete Record, and Batch Delete. Direct video URLs and local media URIs are not bookmark inputs. Single and batch deletion require confirmation and remove exact `url / category / visitedAt` identities through `WebSessionHistoryStore`; batch mode retains the active search and category filters, supports select-all over the current result, and exits before the drawer itself on Back. User-Agent is a direct modal action: opening it closes the menu and shows `Android / PC desktop / iPhone / Symbian Wap / custom global / custom website`; all three UA modal levels share the blue semantic dialog surface. `WebSessionBrowserSettingsStore` owns the global mode, custom global value, and normalized-domain site rules. An explicit session UA has highest priority, then a matching site rule, then the global mode; applying any choice updates the same WebSession WebViews and reloads only the active page. The menu's Incognito action toggles the default new-window Profile and shows the shared short status message while keeping the menu open; AI Dialogue minimizes the browser, enters AI Home, and focuses the real chat input; Exit Browser uses a power icon and removes the presentation layer while retaining sessions. The fourth-row Settings button starts a source-preserving settings session instead of changing to the bottom Settings destination. The window overview keeps `普通窗口` and `无痕窗口` as two equal-width, single-line text tabs in one row rather than button surfaces. The normal tab uses a blue underline and the incognito tab a purple underline; selection changes only text emphasis plus underline opacity and thickness. The overview uses 2/3/4 columns, filters the one WebSession registry by immutable Profile, and displays `5:8` vertical in-memory thumbnails that fit the complete current WebView viewport without cropping. Empty states, new-tab actions, active cards, thumbnail placeholders, and identity marks keep the matching blue or purple profile tone. New and clear actions affect only the selected Profile. When `homeUrl` is custom, clearing that Profile immediately creates a same-Profile home tab through the existing new-tab callback and dismisses the overview; `about:blank` keeps the remaining-profile or empty-overview behavior. Unsupported Multi-Profile or failed cold-start private-profile cleanup disables incognito explicitly and never substitutes a normal window.
- Every product window records an explicit `BrowserWindowCreationReason`. Ordinary same-site and cross-site page navigation, user `_blank`, and user `window.open()` resolve to the current WebSession; automatic/dialog popups and popup targets without a stable HTTP(S) URL are rejected. `onCreateWindow()` uses a same-Profile temporary target resolver WebView that is never registered as a product window and never receives Kiyori bridges, userscripts, downloads, or credentials. The only automatic product-window exception is a real user cross-site navigation from a confirmed configured-home root: it creates one same-Profile child with an opener-home relation. When that child's history is exhausted, it closes and activates the still-valid opener; every other window returns to the latest configured home.
- Edge swipe history navigation is disabled by default. When enabled, a left-edge right swipe requests Back and a right-edge left swipe requests Forward using the same Browser Runtime history commands. The gesture is disabled while sheets, search, page-source confirmation, text selection, element actions, ad marking, download confirmation, JavaScript dialogs, or accessibility touch exploration own interaction; a configured-home root never exits Browser Home through the gesture.
- Ordinary-window launch recovery uses a schema-validated DataStore file under `noBackupFilesDir/kiyori/browser_session_recovery.json`. The minimum projection contains only retained normal-window order, active ID, URL/title, creation/activation times, creation reason, valid opener relation, and explicit `SOFTWARE_HOME / BROWSER_HOME / SEARCH_HISTORY` search provenance. It excludes incognito profiles and metadata, Cookie, headers, DOM, forms, page text, screenshots, credentials, and network logs. `保留多窗口` restores all retained normal windows; otherwise `恢复上次的搜索结果` restores the newest still-open explicit search result; otherwise `询问是否恢复页面` can offer the active normal page. The ask switch converts an available multi-window or search auto-restore into the same one-shot confirmation. Leaving a loaded search result for another page immediately removes that window's search-restoration eligibility. With all three recovery switches off, the recovery file is cleared.
- The Page Source entry in the preceding Browser Home drawer enumeration is superseded by the Page Source Workbench contract: History, Bookmarks, Downloads, Userscripts, Network Log, and capability explanations remain draggable child drawers, while Page Source closes Browser Menu and opens the browser-hosted full-screen native workbench.
- Full-screen search uses Browser Menu scale rather than page-title scale: its action icons are approximately `21dp`, labels are `11–12sp`, and engine cards, current-page actions, history tags, and vertical gaps are compact. Copy/Edit glyphs sit optically closer to their labels. The `26dp` history trash glyph stays inside a fixed `34dp` header row, so switching to Clear/Done does not move the title vertically. The engine-panel scrim dismisses the panel when the surrounding area is tapped. Tapping the current-page title/URL surface dismisses search and returns to the already active WebView without a reload.
- Browser Home renders the last submitted text query in presentation state. The active WebSession's
  explicit `BrowserSessionSearchRecovery` initializes that presentation when a session becomes
  active or when a newly created session receives its recovery after its first projection. A stable
  session/recovery key prevents ordinary page-state projections from reopening a bar that the user
  closed; switching to a non-search session hides the bar, and switching back restores that
  session's query. A text search exposes the horizontally scrollable nine-engine quick-switch bar
  below the top row; selecting another engine persists that engine and resolves the same query in
  the active Profile, while address navigation and the explicit close action hide the bar. The
  Browser Menu fourth row uses `2:1:2` weighted slots so Exit and Settings align midway between the
  first/second and fourth/fifth columns above, with Collapse remaining centered.
- `WebSessionBrowserSettingsStore.allowWebPageOpenApp` is the only external-app authorization owner. Kiyori has no per-navigation external-open prompt state or UI. An external scheme launches only for a main-frame request with an explicit user gesture while the saved setting is enabled; automatic redirects and disabled-policy requests are consumed without opening another application.
- Browser Network Log exits Browser Menu into the existing draggable child-drawer host. It projects only the active session's bounded current-document resource directory, keeps search/filter/action/image-viewer state in the presentation, and clears only that session's resource entries. A logged HTTP or HTTPS URL can be copied, handed to the current default Browser Download Runtime with the active Profile request identity, opened externally by explicit user action, or inspected without navigating/reloading the WebView. Images may also be viewed through the single edge-to-edge viewer without writing another resource entry. Server response status and playback remain absent until their runtime owners provide confirmed data and commands; `blocked` is only the existing client ad-rule decision recorded before network loading.
- Browser bookmarks remain part of the single Browser Runtime and persist through `WebSessionHistoryStore`. The existing `bookmarks_json` records retain backward-compatible defaults for stable ID, root folder, manual order, icon, and secret-space membership; `bookmark_folders_json` stores the folder tree in the same DataStore. The Browser Menu keeps the add-bookmark glyph fixed and changes only its label between Add Bookmark and Remove Bookmark. Adding opens a four-field editable draft populated from the active page; its folder picker lists `/` first, then uses manual sibling order for a depth-first tree with one indentation step per level. Removing affects only ordinary-space records for the normalized current URL, so the ordinary menu does not expose secret-space membership. The Bookmarks child drawer shares the Download Drawer's partial/expanded viewport owner and adds a dynamic folder title, bookmark search, clickable breadcrumbs, favicon rows, validated folder/bookmark mutations, manual drag ordering, versioned clipboard import/export, and ordinary/secret-space separation. Bookmark and Download menus use the same compact `40dp` option rows, anchored placement, and blocking scrim; centered edit, sort, confirmation, and action dialogs use one full-window scrim. Opening in the current tab or a new foreground/background tab always delegates to the existing WebSession registry.
- Software Home uses one centered single-column composition on phones, tablets, and landscape
  screens. Its top-left weather owner requests coarse/fine location only through the Android runtime
  permission flow. A complete successful result is stored in the private
  `kiyori_home_weather` SharedPreferences file with schema version, `Address.locality`, Celsius
  temperature, WMO visual, and observation time. A snapshot is shown only when every field is valid,
  its time is not in the future, and it is at most six hours old; it remains visible while a live
  refresh runs, and a refresh failure does not replace it with an error icon. Live refresh selects
  the newest valid enabled-provider last-known location no older than 30 minutes or requests one
  current location with an eight-second timeout, then resolves city and Open-Meteo
  `temperature_2m / weather_code` concurrently. No unverified, expired, or invented city or
  temperature is displayed. Open-Meteo's free endpoint is non-commercial and subject to its
  published limits and privacy terms, so commercial distribution requires a separately licensed
  provider decision. A successful weather tap searches `<city> weather` with the current browser
  engine in a new shared WebSession. The top-right count is a lightweight process projection written
  from the single `StandardBrowserSessionTools` registry; observing it does not create Browser
  Runtime. Tapping it enters Browser Home and sets the original host route to
  `WebSessionBrowserSheetRoute.TABS`.
- Software Home's Search action opens Full-Screen Web Search above the existing home pager without rebuilding Software Home or AI Home. The input is focused on entry; Back closes an open search-engine panel before leaving the page. Blank input creates no state. Its Profile button changes the Browser Runtime default new-window Profile when AndroidX Multi-Profile is available, uses open-eye normal and closed-eye incognito icons, and shows only a `1.2s` bottom status message. `BrowserAddressResolver` resolves a submitted address or query with the selected engine, `BrowserPresentationCoordinator.openSearchResultInNewSession` creates and activates a new shared WebSession with that Profile and explicit `SOFTWARE_HOME` search provenance, writes the session recovery, and the Shell then enters Browser Home with Software Home as its return target. The Browser Host recognizes the recovery even when the new session's first projection occurred before that write, so the first post-launch keyword search immediately shows the engine quick-switch bar. Only normal-mode searches are written to the `WebSessionHistoryStore` observed by Browser Home; the optional startup-recovery projection is separate and retains a search candidate only while its normal window remains on the loaded result page. The AI segment resets navigation to the AI Chat root and registers a focus request; pager settlement marks it ready, after which the active classic or agent input requests focus, shows the IME, and consumes the request exactly once. The Software Home attachment quick action uses the same `Icons.Default.Add` glyph and `24dp` size as the default Agent input. Attachment, voice, and camera use the same one-shot channel and delegate to the existing `ChatViewModel` attachment state, input microphone launcher, or `AttachmentSelector` camera launcher.
- Every `WebSessionBrowserSearchScreen` host declares Android system Back ownership explicitly. Full-Screen Web Search is the current Shell child and passes `systemBackEnabled = true`; Browser Home search passes the retained Browser subtree's shared `LocalWebSessionBrowserSystemBackEnabled` value. The CompositionLocal remains strict for Browser hosts, so an omitted Browser provider is a development error rather than an implicit ownership decision.
- Settings Home uses a fixed single-column information layout: a four-action header followed by four `16dp` rounded cards containing three rows each. `KiyoriSettingsTheme` owns adaptive page, card, text, divider, switch, disabled, and sheet tokens; `KiyoriSettingsHomeIconPalette` adds one fixed light/dark icon-container pair for each of the 12 homepage entries. Real detail roots and the two speech capability pages share `KiyoriCollapsingSettingsPage`, its continuous expanded-to-pinned title motion, and the same row/selection surface. The first three header actions stay inert; the fourth is the sole quick-theme projection of `UserPreferencesManager.useSystemTheme/themeMode`. The Settings Home, Browser Settings, AI Settings, TTS Settings, STT Settings, account, appearance, data, More Features, download, player, and ad-block pages do not mirror persistence or create parallel route owners. |
- Download Drawer is a Shell-level presentation opened by the Minus-One Download card and a browser child-drawer presentation opened by browser-menu Download; both render the same `BrowserDownloadManager` snapshots. Its settings action closes the drawer and opens the single Download Settings owner, so Back returns to the drawer owner page rather than to a deleted full-screen center. Download Settings uses the current `5/4/2/2/1` fourteen-row contract with engine, destination, concurrency, ordinary/M3U8 threads, any/unmetered network, roaming, result notifications, system notification settings, M3U8 offline packaging, chunk size, APK auto-clean, confirmation, and HTTP protocol consumers. Thread changes synchronously narrow the allowed task range through `128 / max(normalThreads, m3u8Threads)` capped at `8`; task-scoped settings remain frozen after creation. System-owned tasks remain only in Android's download UI. Internal APK auto-clean persists exact package/version identity and deletes only after a matching protected system install broadcast. The shared drawer keeps the draggable two-tab host, adds file/source/error search and nonterminal status filters, and keeps sort, category, time, query, filter, and selection as presentation-only state. Select All affects only visible tasks eligible for the current batch action; batch cancel never deletes records or files, while batch delete retains explicit records-only versus records-and-files confirmation. Empty state distinguishes no tasks from no filter matches. Completed-file directory changes use the dedicated transparent SAF `ComponentActivity`; failed grants are released when unowned. Manual Add dispatches the explicit `http/https` request to either the same internal manager or Android `DownloadManager`; browser context carries Profile headers, Minus-One can use a standalone request identity, completed network tasks can redownload, and path-based M3U8 packages can be remuxed through FFmpeg before the same task record atomically switches to MP4.
- Download Drawer batch delete and batch cancel are distinct presentation actions. Batch cancel accepts only records whose manager projection reports `canCancel`, confirms cancellation, and invokes the manager cancel action without deleting the record or owned files; batch delete remains available for record/file removal. A newly persisted SAF tree grant is released after a settings-directory or file-move failure when neither current settings nor a retained task owns that tree.
- AI Home and every explicitly identified AI Navigation Root show the hamburger button, including roots opened by shortcuts, widgets, raw routes, or router-gateway requests. AI deep pages show Back until navigation reaches an explicit root. Browser, Mini App, File Management, and Kiyori Settings pages do not expose the drawer button.
- Host navigation roots match their registered route ID. ToolPkg plugin roots match both route ID and registered route arguments. Route arguments, stack depth, and instance-ID prefixes never establish root ownership.
- The drawer's fixed quick row is Packages / Toolbox / Workflow. Their badges show enabled package count, the number of `NavigationSurface.TOOLBOX` entries in the sole `AppNavigationModel` (including ToolPkg toolbox entries), and workflow count. AI Dialogue and Memory remain list entries below it, followed by ToolPkg dynamic destinations and the bottom Settings action. Assistant Configuration is no longer a visible drawer entry; Virtual Avatar is reached through Settings Home → AI助手 → 个性化与交互, while Voice Wake-up is reached through Settings Home → AI助手 → 语音. AI Dialogue returns to the existing AI Home without creating a conversation or clearing its draft. Settings opens source-preserving Settings Home without replacing the current AI route. Package management remains an AI-drawer capability and is never surfaced through Settings Home's Mini App Management reservation.
- AI Home always renders the conversation surface and input controls, even when no usable model configuration or API key exists. Model configuration is opened as a separate settings route; missing credentials are reported when the user attempts model-dependent work, never by replacing or trapping the AI Home.
- Permission Grant is no longer visible in the AI drawer. Settings Home / More Features / Permissions now opens the existing `Screen.ShizukuCommands` device-capability owner through the active settings session, while AI Tool Authorization continues to use `ToolPermissionSystem`.
- Conversation history, new-chat, search, and delete actions remain in the original AI Home history selector. They are not duplicated in the drawer.
- The Terminal button remains in the upper-right AI Home toolbar during the product-shell migration. This plan does not add another Terminal entry.
- Opening the Modal AI Drawer never translates, scales, tilts, rounds, shadows, fades, or otherwise transforms the page below it. The page continues rendering but cannot receive touch while the drawer is visible.
- The Modal AI Drawer follows the shared Kiyori theme tokens. Dedicated drawer glass, background-color, and accent-color preferences are not restored.
- Back first closes an open drawer. Back from an AI Top-Level Page returns to AI Home, and Back from a deep AI page returns to its owning top-level page. Back from AI Home or Minus-One Page returns to Software Home; Back at a non-Software root returns to Software Home; Back at the center Software Home requests exit confirmation.
- Each primary destination retains its own child stack and scroll state while the user switches roots.
- Native AI roots use stable host instances. Every ToolPkg root entry creates a new route instance; only a ToolPkg route declaring `keepAlive=true` retains its composition key and saved child stack across drawer switches.
- AI Assistant Settings retains one page composition, form state, scroll state, and persisted settings. Re-entry from the same source family may restore its child stack; crossing between the AI drawer family and Kiyori Settings always opens the AI Assistant Settings root so the visible Back contract matches the current source.
- The Modal AI Drawer is `75%` of window width below `600dp`, `320dp` from `600dp` through `839dp`, and `360dp` from `840dp`. A separating hinge caps it to the left physical region. Window-size and fold-posture changes preserve the current route, form state, scroll state, and AI Home lifecycle.
- Detailed ownership, gesture, adaptive-layout, and migration rules are defined in `docs/doc-src/architecture/kiyori_product_shell_and_navigation.md`.

## System bar contract

- The Kiyori App Shell uses one edge-to-edge status-bar policy for Software Home, Minus-One Page, AI Home, all five primary destinations, and AI pages. Page backgrounds paint to the physical top edge. The Modal AI Drawer's full-window scrim follows that policy, while the drawer panel is an explicit exception whose top edge starts at the status-bar bottom; panel content applies only horizontal and bottom safe insets.
- The status bar is transparent whenever it is visible, and platform contrast scrims are disabled where the Android API supports that control. Status-bar icon brightness follows the actual foreground surface presented by the Kiyori Shell.
- The inherited transparent-status-bar and custom-status-bar-color controls and persisted preferences do not exist in Kiyori. The user may still hide or show the status bar.

## Settings ownership

- Kiyori system settings own application language, product-wide appearance, storage, downloads, notifications, privacy, shared permissions, browser, player, reader, file management, and other cross-product behavior.
- AI Assistant Settings own model providers, model routing, prompts, personas, user preference, AI context, AI tool authorization, conversation policy, AI usage information, and external HTTP chat configuration. Packages, MCP, Skill, ToolPkg, and workflows remain separate AI subsystem destinations and are not copied into Mini App Management.
- A setting has one owner and one persisted source of truth. Other pages may deep-link to that owner but do not duplicate its state.

## AI conversation audit contract

- `ConversationAuditRepository` is the sole writer for immutable conversation-audit events, encrypted payload references, message revisions, current revision projections, completeness, hash-chain heads, cryptographic seals, historical reconstruction, branch inheritance, portable import, and unreferenced-payload collection. `messages` and `message_variants` remain the current chat projection; Provider execution and tool-ledger tables remain resumable execution state rather than a second audit owner.
- Room schema 22 stores `conversation_audits`, `conversation_audit_events`, `conversation_audit_payloads`, `conversation_audit_event_payloads`, `conversation_message_revisions`, `conversation_message_projections`, and `conversation_audit_seals`. Events are append-only and strictly sequenced per chat. Projection mutations and their audit event commit in one Room transaction; content-addressed payload files are atomically committed before that transaction and are collected only when no event or revision references them.
- Audit payloads are redacted before hashing and encryption. API keys, Authorization, Cookie, passwords, private keys, access tokens, request signatures, credential URL parameters, and HTTP Basic user-info never enter persistent plaintext. Redacted payloads use GZIP plus Android Keystore AES-GCM; independent EC P-256 / `SHA256withECDSA` keys seal chain heads.
- `ConversationAuditProviderRequestRecorder` synchronously writes `FINAL_PROVIDER_SEMANTIC_REQUEST` before each actual Provider hop and passes the exact same `ProviderRequestContext` to the Provider. The snapshot includes final prompt turns, model parameters, complete tool schema, Provider/model, thinking/stream flags, and hop identity while excluding credential values. A failed critical write prevents the Provider call.
- Prompt Hook input/output/error, Provider semantic events, aggregated visible text deltas or revisions, tool request/start/result/error, cancellation, complete throwable chains, terminal usage, and final assistant projection enter the same audit chain. Kiyori records only locally constructed or Provider-returned facts; hidden server reasoning, TCP chunks, heartbeat, polling, and idle time are outside the contract.
- User and AI visible text can be edited only by creating immutable revisions. The selected AI variant has its own revision chain; future context and the current bubble use the latest projection, while historical Provider requests, actual token counts, wait/output timing, tools, exceptions, and Provider events remain unchanged and read-only. Evidence-qualified repair of an interrupted assistant replay surface is also a system-authored immutable revision: it records before/after and diagnostics under `ASSISTANT_REPLAY_HISTORY_REPAIRED` without rewriting the original Provider events. The existing edit-and-resend operation remains separate.
- Message deletion, rollback, variant deletion, branch replacement, and aborted branch creation use tombstone events. Deleting a whole chat physically removes that chat's audit; shared payload content remains until no surviving chat references it. A branch receives a self-contained chain through its branch point and does not depend on the parent remaining present.
- Visible completeness values are `COMPLETE / IN_PROGRESS / PARTIAL / BASIC / RECORDING_INTERRUPTED / CONTENT_CORRUPTED / SOURCE_UNVERIFIED / KEY_UNAVAILABLE`. Native complete/in-progress transitions cannot overwrite historical partial/basic/source-unverified or more severe integrity facts.
- Conversation Details exposes Timeline, Conversation, and Raw JSONL views. It loads the newest 100 events, pages older events by sequence cursor, appends new events from a last-event observer, and decrypts large payloads only when expanded. The header shows total/loaded events, chain head, Provider/model, storage, and completeness. Back closes annotation, expansion/search, message editing, or Raw search before returning to chat.
- A complete `.kiyori-audit` ZIP contains manifest, chat/audit JSON, timeline, event/revision/projection JSONL, integrity/redaction reports, and independent payload files. Import validates paths, bounds, IDs, hashes, event/revision/projection relations, payload sets, seals, and ECDSA signatures, re-encrypts payloads with the target device key, then appends a local `IMPORTED_CONTINUATION`. External signatures are mathematically verified but have no local trust anchor, so imported chains are `SOURCE_UNVERIFIED`.
- `OperitChatArchive` format 3 can carry the full portable audit; v1/v2 remain importable and enter honest historical reconstruction. The separate AI-review Markdown additionally pseudonymizes identity fields and private paths. Detailed implementation and validation boundaries are in `docs/doc-src/dev-core/AI_CONVERSATION_AUDIT.md`.

## AI provider and protocol contract

- `ApiProviderType` and `apiProviderTypeId` identify the supplier; serialized `ApiProtocol`
  identifies the wire protocol. Compatibility enum values remain readable for persistence,
  audit keys, ToolPkg/runtime references, and Responses execution state, but the provider picker
  exposes one canonical row per supplier.
- The provider picker has only `China-based`, `International`, `Local and custom`, and `ToolPkg`
  sections, in that order. DeepSeek is the first China-based supplier; OpenAI and Anthropic are
  the first international suppliers, and xAI is a canonical international supplier. There is no
  separate featured-provider section.
- `ApiProviderConfigs` is the protocol catalog and declares every built-in supplier's default
  protocol, supported protocols, protocol-specific endpoint, endpoint choices, model-list
  endpoint, and API-key requirement. Unsupported supplier/protocol pairs are rejected rather
  than translated into another protocol.
- The protocol picker has no automatic-detection mode or action. For multi-protocol suppliers,
  the user explicitly selects one serialized `ApiProtocol`; switching suppliers applies only that
  supplier's catalog default. A trailing `#` remains an endpoint-completion control marker, not a
  protocol signal. Runtime requests never switch protocol after a failed request.
- OpenAI-compatible Chat normally preserves the supplier-specific adapter. Anthropic and Google
  OpenAI-compatible Chat use the generic `OpenAIProvider`; xAI Chat also uses that generic
  capability owner while retaining xAI identity. Responses use
  `OpenAIResponsesProvider`; Anthropic Messages use `ClaudeProvider`; Google, MNN, and the
  embedded llama.cpp engine retain their declared native owners. Model-list authentication,
  endpoint, and parser are selected from supplier identity plus protocol, not protocol alone.

## AI model execution contract

- “思考模式”与现有 `thinking_quality_level: 1..5` 是唯一用户 reasoning 接口。普通 OpenAI
  Chat Completions / Responses profile 的五档显式映射为 `low / low / medium / high / high`；
  关闭思考固定编译为 `none`。`gpt-5.6*` 前缀模型使用 Codex 五档
  `low / medium / high / xhigh / max`。Pro、Fast、Ultra、传输和执行持久性不是第六档，
  也不能改写用户选择的五档。
- `ModelCapabilityResolver` 与 `ModelRequestCompiler` 是模型能力和 wire 参数的语义所有者。
  所有 `gpt-5.6*` 前缀模型进入同一 Codex reasoning profile；普通 OpenAI/兼容模型仍可在
  已声明 reasoning 能力的情况下使用思考模式，但不会使用 `xhigh` 或 `max`。官方能力同时
  要求 provider 类型为官方 OpenAI，并且补全后的请求地址精确属于
  `https://api.openai.com/v1/responses`；仅保存为 `OPENAI_RESPONSES` 不能把 Pipio、Pixel、
  Sekiro 或其他自定义地址提升为官方合同。`gpt-5.6* + Responses` 在官方和兼容 endpoint
  上都请求 `summary=auto`，使服务端真实返回的 reasoning summary 可以进入现有
  `<think>` 可见消息；兼容 Responses 仍不得自动获得 Background、sequence resume、
  Prompt Cache、Tool Search、`reasoning.encrypted_content` 或 strict schema。摘要增量、
  part 完成、output item 完成和终态快照由一个单调 projection 去重；内容分叉必须报协议
  错误，不能拼接损坏文本。普通 OpenAI Chat 请求使用 `reasoning_effort`，Responses 请求
  使用 `reasoning.effort`。
- 官方 GPT-5.6 Responses 使用 `background=true`、`store=false`。`ProviderRequestContext`
  在模型请求前固定 chat、message timestamp、variant 与 hop；`ProviderExecutionRepository`
  持久化 response ID、事件和单调 sequence cursor。未应用事件使用 `-1` 哨兵，官方
  Responses 首事件固定从 `sequence_number=1` 开始，后续事件必须严格连续。已知 response ID
  后的传输中断只能用 `starting_after` 续接同一 response，不能创建新 response 或撤回已确认
  UI 内容。
- `response.created` 前只有明确的 HTTP 429 拒绝允许重新提交；408、409、5xx 和传输异常进入
  `SUBMISSION_UNKNOWN`。已知 response ID 后瞬时 HTTP/传输错误只重试同一 GET，404/410
  标记远端状态已 `EXPIRED`。没有声明官方 sequence resume 的兼容 Responses 仍使用同一
  at-most-once 提交边界：未知提交状态不执行普通流式整轮回滚或重新 POST，已经确认的部分内容
  保持原样，当前回合以可见错误结束。
- `OpenAIResponsesExecutionPersistence` 是可恢复 Responses 协调器的持久化依赖边界；生产
  唯一实现直接委托 `ProviderExecutionRepository`，不持有第二份执行状态。本地 JVM 故障注入
  从公开 `sendMessage` 流进入同一生产协调器，通过 loopback HTTP 返回首个
  `response.created` 前的 502，并精确验证只发送一个 POST、repository 收到
  `SUBMISSION_UNKNOWN / HTTP_502_SUBMISSION_UNKNOWN`，且异常到达流收集者。
- 冷 Provider 流转为共享流时，`SharedStream` 是唯一的上游终止传播所有者：正常完成无原因
  关闭，上游失败携带原异常关闭，当前和晚到收集器都从共享流接收失败。完成转发的 owner
  协程不得再次把同一 Provider 异常抛入 `GlobalExceptionHandler`，否则一次可见发送失败会被
  重复升级为 `APP_FATAL`。首包前的请求编译、持久化、HTTP 或协议错误必须继续传播到
  `EnhancedAIService` 和消息层；禁止把失败按零内容正常完成关闭。
- 普通发送由主 `sendJob` 唯一持有消息失败状态。`streamCollectionJob` 在结构化子作用域中
  收集正文，并通过 `CompletableDeferred` 把原始终止原因交给主任务；非取消失败交付后该
  收集 Job 必须正常结束，`CancellationException` 继续保持取消语义。自动朗读、Waifu 分段、
  修订事件、首包探测和可修订文本渲染属于次级观察器：它们记录并结束自己的非取消失败，但
  不得第二次升级同一个 Provider 终止原因。主任务收到原异常后进入现有
  `InputProcessingState.Error`、错误提示和运行态清理链。
- `InputProcessingState.Error` 与错误弹窗是同一次失败的两个投影：前者驱动发送状态，后者由
  `ChatServiceCore` 持有的唯一 `UiStateDelegate.errorMessage` 驱动。消息处理委托的
  `showErrorMessage` 回调必须把安全用户消息写入该共享 delegate；`ChatViewModel` 取得同一
  实例，`AIChatScreen` 再从中显示 `ErrorDialog`。只记录日志会让 Provider 失败正确结束加载，
  却在主界面表现为无提示静默结束。提交状态未知异常的外层安全文本必须包含底层 HTTP 或传输
  摘要，使 502 和上游错误能直接显示，而不是只显示本地 execution ID。
- 模型服务实例按请求持有 `ServiceLease`。单功能刷新和全量配置刷新只清除新请求可见的缓存
  并将旧实例标记为 retired；活跃租约归零前不得调用 `cancelStreaming` 或 `release`，新请求
  必须创建并使用刷新后的实例。最后一个租约归还后，retired 实例只释放一次。
- 助手取消请求以 chat 和 turn ID 为身份记录。只有明确的用户停止和破坏性历史修改可以静默
  进入 `Idle`；配置刷新、生命周期失效、应用退出和来源未知的 `CancellationException` 必须
  投影为可见 `Error`。已取消的发送 Job 只允许在受限 `NonCancellable` 区域提交终态和错误
  提示及 replay-safe 部分消息，不得重新执行请求、工具或其他业务逻辑。用户停止由外层取消
  owner 保存同一安全投影；破坏性历史修改不保留部分回答。
- 工具执行和工具结果 follow-up 属于同一消息主失败链。长期工具 scope 使用
  `SupervisorJob` 隔离不同任务；当前 `Deferred.await()` 仍把原始失败交回调用者。一次
  follow-up 失败不得取消长期父 scope、并行污染其他独立回合或使后续发送继承旧异常；
  `launch + join()` 不得作为失败传播合同。`processStreamCompletion` 不得把异常改写为
  `Idle`，当前响应轮零输出不得投影为 `Completed`。
- `MessageProcessingDelegate.completeAssistantResponse` 是普通发送进入成功终态前的最终
  内容 owner。它必须从共享流重放和消息状态重建最终正文，记录 chunk 数、可见字符数、
  provider、model、provider request context 和最后输入状态；空白正文以
  `AI_STREAM_EMPTY_TERMINATION` 进入可见 `Error`。普通非取消异常必须同时写入
  `finalInputStateAfterSend`，保证服务随后重放 `Idle` 时，cleanup 后仍由消息 owner 恢复
  Error；缺失任何终态的回合以 `AI_TURN_TERMINAL_MISSING` 失败。写入 `COMPLETED` 前还必须由
  `AssistantReplayHistoryProjector` 证明所有工具事务闭合，不能把 call-only 正文标记为成功。
- `AssistantReplayHistoryProjector` 是 durable assistant replay surface 的唯一纯状态机。它按
  原顺序识别随机后缀 `tool_* / tool_result_*`、独立 `name`、协议工具名
  `provider_tool_name`、可选 `provider_call_id` 和流式终态标记 `provider_result_terminal`。
  `name` 保留代理目标等 UI 展示名，协议匹配和 Provider follow-up 使用原始 invocation 名；同一
  工具 Flow 的中间结果标记为非终态并只留在实时流与审计中，只有正常结束后的终态结果可以闭合
  调用。旧 `package_proxy / proxy` 的同消息结果只有在调用参数能证明原始协议工具名时，才会补写
  `provider_tool_name`；跨消息结果不能原地规范化展示别名，因此不会作为安全闭合证据。并行工具的
  live result 按完成顺序到达，投影必须把终态结果匹配到整组 pending calls，
  并只在 durable 副本中规范为原调用顺序；即时拒绝和实际执行结果交回 Provider 前也必须共同恢复
  invocation 顺序，结果缺失或无 owner 结果直接失败。已经有序的完整事务逐字保留。未闭合、
  截断、孤立结果、无法唯一匹配或身份冲突从所属事务起移除整个后缀，不生成工具结果。正常完成
  owner 必须保存闭合检查返回的规范化正文，不能校验后写回原始错序正文。实时
  `contentStream` 与 conversation audit 继续保存原始完成顺序；
  每秒消息快照、普通失败收尾、手动停止和非预期取消只把 replay-safe projection 写入
  `messages / message_variants`。
- 下一次发送在读取自动 compaction snapshot 和添加新用户轮次之前检查旧历史；通用自动压缩
  入口先等待当前活动回合完成安全终态持久化，再执行同一检查，不能把仍在运行的工具事务当成
  中断残留。恢复读取采用严格失败语义，数据库读取失败时不得以空历史继续请求。截断或未闭合
  事务必须先检查是否在后续持久 AI 消息中按 FIFO 顺序、协议工具名和 call ID 合法闭合；普通
  user/summary 正文中的工具样式 XML 不会被请求编译器解释为工具结果，不能作为闭合证据。若直到
  replay boundary 仍未闭合，它已经是 Provider 无法重放的历史，必须修复，不得再以
  `completedAt != 0` 或缺少同 timestamp 失败审计为由保留。完成时间与终态审计只属于诊断事实，
  不是修复资格。跨消息保留还要求调用消息自身已经满足 Provider FIFO、没有流式中间结果，也没有
  待应用的结果重排或协议名规范化；`call A, call B, result B` 后接 `result A` 不能被误认成合法
  闭合。真正损坏的当前 variant 通过
  `ConversationAuditRepository.reviseMessage` 原子写入修复 revision、投影、审计事件和 seal；
  已由后续消息合法闭合的跨消息事务不改写。`ConversationCompactionContract` 随后只对修复后的
  同一 runtime history 建立 snapshot，并继续执行 digest、route、工具闭合和相邻 summary 门禁。
  通用自动压缩与“插入总结”的切片压缩入口都必须先等待活动回合并执行这项修复；单条消息重新
  生成在保存新 variant 前还必须通过 `regeneration_completion` 闭合断言。
- `ConversationService` 把同一批并行调用产生的连续 `TOOL_RESULT` 合并成一个 Provider turn，
  与首次工具 follow-up 的聚合形状一致；`TOOL_CALL` 继续保持独立，不能丢失调用级元数据。
- 多工具 follow-up 的 `64000` 字符总预算先为每个真实调用预留完整 `tool_result` XML 信封，再把
  剩余 payload 空间按结果数分配；不得在达到总长度时停止追加后续结果，也不得对最终 XML 直接
  按字符截断。结构信封本身无法装入上限时必须在本地显式失败，不能让 Provider 收到部分结果组。
- OpenAI 请求体编译、工具历史校验或本地请求创建在 `call.execute()` 之前失败时，异常必须原样
  交回消息失败链，不进入网络重试，也不能包装成“连接超时”；HTTP 提交开始后的传输失败继续由
  既有重试与 at-most-once 合同处理。
- Provider 原生工具标签中的 `name`、`provider_name`、`provider_call_id` 和
  `provider_response_id` 是四个独立属性。工具解析只读取独立 `name` 属性，不能把
  `provider_name` 的值当作工具名。
- `response.completed` 的完整 `response.output` 是终态一致性快照。工具项必须保留原始
  `response.output` 位置，并通过稳定 `call_id` 与流事件绑定；过滤后的工具数组位置和随机
  XML 标签只属于本地投影，不能成为工具身份。`response.function_call_arguments.done` 携带
  的完整参数在关闭 XML 前进入同一解析器。中间文本或 `function_call` 事件缺失时，只补齐
  尚未交付的正文尾部和工具调用；快照与已确认正文、工具名、调用身份或参数分叉时按协议错误
  失败，不撤回或拼接已确认内容。
- 普通发送实现按单回合状态载体、用户消息准备、助手请求准备、Responses 提交、共享流收集
  和完成收尾分开编译。该结构是针对 vivo Android 16 `SIGABRT / Unexpected instruction:
  unused-e6` 的 ART DEX 兼容性修复；它不增加请求重试、不改变提交次数、不引入模型降级，也
  不改变已知 `response_id` 的 `starting_after` 续接。
- `message_provider_states` 保存同一个 Responses execution 的 provider-private output items、
  sequence cursor 与 usage，只服务已知 `remoteResponseId` 的断流续接；普通下一用户轮次、
  PromptTurn 历史编译和 compaction 不读取该表。跨轮 `function_call / function_call_output`
  仍由 replay-safe `ChatMessage.content` 编译，typed execution state 不能成为第二份跨轮历史
  owner。相同 provider 与 `call_id` 只能对应同一工具名和语义一致的参数；相同身份
  在 XML 投影、当前回合执行和 Responses 历史重放中都只保留一次，冲突必须在副作用前失败。
  compatible endpoint 没有真实远端 response ID 时只做当前回合身份规范化，不伪造 response
  ID。`tool_invocation_ledger` 仅以真实 provider、response ID 和原始 `call_id` 为键，已运行
  或完成的 provider-native 工具调用不能再次执行；每个 `call_id` 的历史输入最多包含一个
  `function_call` 和一个语义一致的 `function_call_output`。
- Prompt Cache key 使用 profile revision、精确模型、稳定系统/首用户锚点和 canonical 排序的
  tool schema，不包含当前用户消息、effort、trace ID 或时间戳。官方 GPT-5.6 Responses 仅在
  大工具集存在足够长尾函数时启用 Tool Search；常用文件、搜索和计算工具保持 eager。
- `WebSessionBrowserSettingsStore` owns the browser home URL, return-without-reload switch, edge swipe-history switch, restore-last-search-result switch, ask-before-restoring-pages switch, retain-multiple-normal-windows switch, force-page-zoom switch, webpage text zoom, webpage-element long-press-menu switch, webpage external-app policy, webpage geolocation policy, website-password auto-save switch, search-bar media-candidate badge, automatic floating playback, automatic-floating minimum duration, and exact-host disabled-feature sets for the nine supported non-ad-block site capabilities. A fresh install starts with `https://go.itab.link` as the configured home; the four navigation/recovery switches default off, the webpage-element menu defaults on, and the site-rule list is empty. Empty site rules are removed. Toggling the global or site element-menu setting synchronizes existing WebViews without navigation or reload. `about:blank` remains the explicit blank-page reset target. Return without reload changes only the WebView history navigation selected by system Back, the browser bottom Back action, or AI browser Back; the Browser Home top-left App Shell action and each session's configured-home root are unchanged. Force page zoom installs a reversible viewport override in the current document. Text zoom is `50%..200%` in `5%` steps, defaults to `100%`, and is applied to every current and future WebSession. The automatic-floating minimum duration defaults to 60 seconds, accepts the fixed `30 seconds / 1 / 3 / 5 / 10 / 30 / 60 minutes` options or a whole-second custom value from `1..86400`, and is consumed only by automatic floating playback; manual candidate playback is unaffected.
- `BrowserCredentialVault` is the sole website-credential owner. Auto-save is enabled on a fresh install. Only ordinary-profile HTTP/HTTPS pages may capture a non-empty account plus exactly one non-empty password field during login submission; records are keyed by normalized exact origin and account, and autofill uses the captured field selectors without submitting the form. Incognito profiles never capture, read, or fill credentials. Every complete credential record is AES-GCM encrypted with a non-exportable Android Keystore key and atomically stored under `noBackupFilesDir`, so current raw-snapshot and Android automatic backup flows do not copy this device-bound data. The native manager exposes search, masked reveal, explicit reveal, sensitive clipboard copy, edit, and delete without logging credential values.
- `PlayerSettingsStore` owns decoder backend, rendering profile, GPU Next, Vulkan context, default and remembered speed, speed-memory policy, precise seeking, button seek step, double-tap action and seek step, long-press speed boost, chapter markers, seekbar thumbnails, automatic queue advance, final-queue behavior, the single network-cache policy, subtitle scale, background behavior, fullscreen-to-floating behavior, gravity rotation, Anime4K default and memory policy, volume enhancement, and the optional screenshot/video SAF directories. The settings page exposes 25 entries in `4/7/6/2/2/4` groups. Network cache is exactly `COMPACT / BALANCED / LARGE / FULL_VIDEO`, displayed as `省流模式 / 智能均衡 / 流畅优先 / 完整缓存`; there is no independent full-cache boolean. A fresh install uses the `SOFTWARE` decoder backend with the `FAST` rendering profile, Anime4K `OFF`, `BALANCED` (`智能均衡`) cache, non-exact seeking, no remembered speed/Anime4K, no long-press boost, and no seekbar thumbnail extraction. Runtime profile application occurs before the explicit backend, seek, subtitle, volume, and shader properties; identical settings are not rewritten before the first `loadfile`, while a changed profile invalidates every explicit-property cache so those dedicated settings remain the final owner. The network-cache policy is snapshotted when a new media request is created and is not applied by live settings updates, so one request never changes cache owners mid-playback. `FULL_VIDEO` includes its own `256 MiB / 128 MiB / 300 s` base buffer and prepares the single mpv core's app-private immediate-unlink disk cache before `loadfile`; after `FILE_LOADED`, it becomes active only for network video with a non-HLS/DASH actual format, finite duration up to four hours, `seekable=yes`, `partially-seekable=no`, known `file-size` up to 20 GiB, and available space of at least the complete file plus `max(1 GiB, 15%)`. Qualification does not depend on `stream-start`, `stream-end`, a URL suffix, or a second network probe. Ineligible media disables disk cache, releases the request directory, and retains `FULL_VIDEO`'s own base buffer without switching policy. Cache-state evidence is explicitly `NOT_APPLICABLE / AVAILABLE / UNAVAILABLE / MALFORMED`; only `bof-cached=yes`, `eof-cached=yes`, and exactly one seekable range establish `COMPLETE`, and a later transient missing/malformed node cannot revoke that authoritative same-request fact. Actual disk cache above 20 GiB or free space below 512 MiB terminates the media with an explicit reason. Replacement, close, destroy, and next runtime initialization clean only the verified `noBackupFilesDir/player/mpv-session-cache` session scope. A completed full-cache network request may export only a small target-time excerpt from that same demuxer through `dump-cache`, then run the existing thumbnail extractor against the private local excerpt; ordinary network media never opens a second headerless thumbnail connection. Active, pending, superseded, closed, and next-runtime paths clean only `cacheDir/player-seek-preview`. Full cache never creates a browser download, offline file, second request or second state owner. Blank player directory values mean that the live browser-download settings are resolved at use time. Main-process `PlayerSession` sends immutable snapshots to the only non-exported `:player` runtime; settings UI never writes mpv properties directly.
- The full-screen control layer uses one stable pointer detector whose lifetime is independent of high-frequency playback position updates. A single tap toggles controls; a locked tap reveals the two unlock buttons; double tap, long press and horizontal/vertical drags remain owned by that same gesture sequence. The default double-tap action for an unset preference is play/pause. When long-press acceleration is enabled, the temporary target is `1.0x` for a current speed below `1.0x`, `2.0x` for a current speed from `1.0x` through below `2.0x`, and `3.0x` for a current speed from `2.0x` through below `3.0x`; `3.0x` has no higher target. The start feedback remains visible for one second and then disappears while acceleration continues; release or cancellation restores the exact pre-press speed and shows the restoration feedback for one second. The temporary speed and both feedback timers never write speed memory. Left-side vertical drag still controls brightness but renders its indicator on the right; right-side drag still controls volume but renders its indicator on the left. Auto-hide runs only while actively playing with no popup, log dialog, seek, or full-screen gesture in progress, and every real control interaction restarts its timer.
- Fullscreen and floating progress bars keep drag position in a local draft and submit exactly one seek when the drag ends; cancellation submits nothing. `PlayerSession` immediately projects an accepted target into UI state and prevents older 250 ms progress snapshots from pulling the slider back before mpv confirms output. Because mpv seek events carry the load identity but no seek command ID, one load keeps at most one runtime seek command in flight; additional button, gesture, or progress actions are coalesced into the latest visible target and submitted after the active `MPV_EVENT_SEEK -> MPV_EVENT_PLAYBACK_RESTART` pair completes. Surface/VO reconfiguration seeks remain internal events and cannot consume a future user target. New media, close, runtime death, command failure, and explicit errors clear stale pending/seeking state. Normal mode uses `absolute+keyframes`; explicit precise mode uses `absolute+exact` with `hr-seek-framedrop=yes`.
- Player UI exposes only active commands or explicit disabled states. Subtitle, audio track, fit mode, speed, playback, seek, queue navigation, Anime4K, chapters, seek preview, rotation, screenshot, lock, browser download, and diagnostics call the single `PlayerSession` or existing browser owner. The floating player uses the real subtitle/audio/fit/queue commands and leaves danmaku visibly disabled until a real owner exists. Fullscreen and floating preparation indicators wait 160 ms before appearing so a fast first frame does not flash a blocking card. Landscape Anime4K is a fixed-width lower-left control with compact two-line text. Gravity rotation uses `FULL_SENSOR`; while enabled, manual rotation is disabled instead of issuing competing orientation requests. The more menu owns the same gravity-rotation switch plus the diagnostic-log action.
- Every `WebSession` owns one current-document static resource directory and one bounded in-memory media-candidate set. The resource directory is not an event history: `credentialDocumentToken` is the document generation, and up to 2,000 entries aggregate by generation plus normalized original URL. Fragment is excluded from identity, query is preserved, default ports and host/scheme casing are normalized, and repeated requests increment `requestCount` while retaining first/last evidence and merged request identity. The drawer groups `VIDEO / AUDIO / IMAGE / WEB / SCRIPT / STYLE / DATA / FONT / OTHER`, does not show a per-row clock, and uses observed headers plus the active WebSession's missing User-Agent, Profile Cookie, and HTTP(S) page Referer for bounded visible image thumbnails and the shared image viewer. The viewer freezes its input snapshot, renders each image with `ContentScale.Fit` in an edge-to-edge undimmed Dialog, pages horizontally, shows `current/total` and Save at the bottom, exits on image tap, exposes Save Original on long press, supports `1x..5x` pinch zoom, and fades the black layer while a vertical drag reveals the browser before release exits. Network Log supplies the current filtered HTTP(S) images; webpage-element Full-Screen View supplies one image; Page Image Mode scans at most 2,000 current-document elements, deduplicates HTTP(S) images, retains at most 200, and starts from the selected image. Saving closes the viewer before calling the sole `BrowserDownloadManager` path. Candidate playback calls the single `PlayerSession`; candidate download calls the existing `BrowserDownloadManager`. “资源嗅探” observes both `HTMLVideoElement` and `HTMLAudioElement`. Media identity is evidence-based: video DOM outranks a misleading `.mp3` suffix or audio MIME, audio DOM identifies real audio, controlled response MIME or `Content-Disposition` can identify an opaque direct resource, and the original URL is never rewritten. Audio is displayed and downloadable, but its play action remains explicitly disabled until a real music-player owner exists. At `FILE_LOADED`, the mpv runtime publishes the actual file format, video/audio codecs, video-track count, active hardware decoder, pixel format, and selected track's `codec-profile`; a stable `VIDEO_RECONFIG` rereads only that light identity, suppresses identical snapshots, and sends a dedicated update without rebuilding tracks or rerunning full-cache qualification. `hwdec-current` values that are blank, `no`, or `none` mean no active hardware decoder. A disguised URL is therefore diagnosed by actual demux/track state rather than its suffix. Actionable video candidates derive a quality height from observed DOM dimensions or explicit original-URL hints such as `1920x1080`, `quality=1080`, or `1080p`; only videos participate in automatic floating playback. No active media probe, response-body sniff, URL rewrite, second WebView, second player core, second download manager or second database is introduced.
- Browser Network Log classifies a top-level main-frame request as `WEB`; otherwise a recognized URL extension is authoritative for the visible resource group, including `SVG -> IMAGE`, `HTML -> WEB`, `JS -> SCRIPT`, `CSS -> STYLE`, data formats, fonts, audio, and video. When the URL has no recognized extension, the parser selects the first recognized type-specific `Accept` media range after ordering by quality and original position; `*/*`, `q=0`, invalid ranges, and otherwise opaque requests remain `OTHER`. It never treats the presence of any later `image/*` alternative in a mixed navigation header as proof that the resource is an image. Kiyori's process-wide sole Coil `ImageLoader` registers `SvgDecoder` beside the existing platform/GIF decoders, so Network Log thumbnails, the shared image viewer, webpage-element image viewing, and QR recognition use the same SVG-capable loader, request identity, memory cache, and disk cache.
- Browser long press has one explicit owner per target. Editable text inputs, textareas, and effective `contenteditable` nodes are handed to Android WebView's native long press, system `ActionMode`, and native selection handles, which own cut/copy/paste, select-all, and character-level range adjustment; Kiyori does not paint an input selection or show input-specific copy/select-all/cancel buttons. Ordinary elements continue through one bounded payload containing page, tag, text, link, resource kind, resource URL, selector and client point. The Host resolves `IMAGE_LINK / IMAGE / LINK / MEDIA_LINK / MEDIA / TEXT / ELEMENT` and builds only executable actions: foreground/background window opening, exact link/text/resource copy, external opening, image viewing/saving/page mode/QR recognition, quick selector blocking, advanced marking workbench, and exact URL blocking. QR recognition is image-only, loads one bounded software bitmap with the active WebSession request identity, and uses the existing ZXing reader; load failure and no-QR are distinct terminal states. WebView consumes native long press only for ordinary elements; editable targets return `EDIT_TEXT_TYPE` unconsumed so the system selection can start. No second selection menu, WebView, rule store, downloader or selection state owner is introduced.
- Player presentation is exactly `BROWSER_ONLY`, `FLOATING_PLAYER`, or `FULLSCREEN_PLAYER`. `FLOATING_PLAYER <-> FULLSCREEN_PLAYER` retains the same `PlayerMediaRequest`, playback position, duration, pending seek target, pause/speed state, complete-cache state, `loadGeneration`, `runtimeGeneration`, runtime PID, mpv core and demuxer cache; it only detaches the old Surface and attaches the new generation. Presentation changes do not call `loadfile`, `loadUrl`, `reload`, resniff, rebuild a WebView, or execute JavaScript that changes webpage media state. Closing a floating player immediately projects `BROWSER_ONLY` so Compose removes it before asynchronous Surface detach/runtime close finishes. A browser page's automatic-floating opportunity is consumed as soon as one of its candidates is successfully accepted by the single `PlayerSession`, whether the entry was automatic or manual. This consumption gates only automatic startup: closing, natural completion, or fullscreen `CLOSE` cannot reopen the same page's candidate as a new request, while manual playback remains available. Closing an older page's player while browsing a new page does not consume the new page, so its stable candidate may open after the prior runtime reaches the closed state.
- The single mpv core completes `mpv_initialize` before any Android `Surface` is attached. `loadfile` is issued only after the current Surface lease completes native attach.
- Browser candidates retain their observed request identity for candidate and download owners, but `:player` removes `Range`, `Accept-Encoding`, hop-by-hop fields and Chromium-only `Sec-CH-UA` / `Sec-Fetch-*` metadata before `http-header-fields`; mpv/FFmpeg exclusively owns byte ranges, content encoding and connection framing. Origin, User-Agent, Referer, Cookie, Accept and unknown end-to-end authentication fields remain when observed. Diagnostics expose only field names/counts and ownership decisions, never values.
- Every player Surface callback carries a role, owner token, generation, and Surface identity. Only the current generation may update or detach; a new owner may register as pending, but native attach is forbidden until the old owner has completed native detach. Generation values never decrease or get reused.
- Floating-to-fullscreen removes the floating `PlayerSurfaceView`, waits for its `surfaceDestroyed` and native detach, then emits one fullscreen Activity launch request. Fullscreen-to-floating emits one finish request, keeps presentation fullscreen until the fullscreen Surface is detached, and only then permits Browser Home to compose and attach the next floating Surface.
- The packaged `mpvlibAndroid@168e0a5e` Surface lifecycle remains authoritative: detach keeps `vo=null`, `force-window=no`, and native-window release; attach restores the configured VO and `force-window=yes`. Kiyori does not replace that lifecycle without a reproducible native source/build closure. `STRETCH` instead fixes geometry ownership: it sets `panscan=0` and `video-aspect-override=surfaceWidth/surfaceHeight` from the current accepted Surface generation, and reapplies on valid resize. Landscape stretches to the left/right Surface bounds, portrait stretches to the top/bottom bounds, and floating/freeform/foldable layouts follow their actual Surface rather than display metrics.
- Fullscreen player UI keeps the existing player-specific back, subtitle, danmaku, aspect, overflow, transport, speed, screenshot, lock, download, brightness and volume assets inside one modern dark control system. Player controls render their icons or compact text directly without static circular/pill backgrounds or borders; their full touch targets and bounded click indications remain intact. Anchored menus use one rounded dark `DropdownMenu` surface with no second container exposed at the corners, plus titles, supporting text, selected states and switches. The progress bar remains a 3dp track with a 12dp blue-violet thumb; Material player icons and Material Slider are not parallel supported interfaces. The compact speed menu is exactly `3.0x..0.25x` in descending `0.25x` steps and adds a custom `0.00x..3.00x` input with at most two decimal places; `0.00x` pauses without replacing the retained nonzero speed. The browser floating player reuses the same speed menu, blue-violet progress gradient, white track, accent thumb, and top/bottom landscape overlay gradients while remaining a scaled full-width control layout.
- Anime4K is exactly `OFF / A / B / C / A_PLUS / B_PLUS / C_PLUS` using the Balanced/M shader chains from `mpv-android-anime4k@32f5f169`. The required upstream shader bytes are SHA-256 locked, copied to the versioned private directory only after packaged-asset verification, rechecked after caching, passed as absolute paths to `:player`, and accepted only when MPV reads back the same `glsl-shaders` value. Runtime and MPV compile messages remain visible in the existing diagnostic log.
- Existing development installs migrate legacy Anime4K IDs before strict settings parsing: `off / fast / balanced / quality` are rewritten once to `OFF / B / A / A_PLUS`. Any other unsupported ID still fails validation instead of being silently accepted.
- `PlayerDebugLogBuffer` is cleared only for a newly loaded media request and keeps at most 2,000 normalized, timestamped and leveled entries. `MpvPlayerEngine` registers `MPVLib.LogObserver` with `msg-level=all=warn,ffmpeg=info,demux=info`; the `:player` process forwards bounded runtime commands, MPV messages, file events and every visible error through the ordered AIDL callback. MPV `END_FILE` reads the node's string `reason` and optional string `file_error`; the first concrete DNS/TCP/TLS/HTTP evidence outranks generic `loading failed`. After `MPVLib.init()`, the engine strictly reads the required version, protocol, demuxer and decoder properties once. Hardware-decoder introspection reads the `option-info/hwdec` Node map and treats its `choices` field as optional: an exposed list produces confirmed `AVAILABLE / UNAVAILABLE` target states, while an absent option/map/choices contract produces `UNKNOWN` with explicit `hwdecEvidence`; malformed map or list nodes also emit a warning. The capability digest includes that evidence, so unknown metadata cannot be mistaken for confirmed lack of MediaCodec support, and optional hwdec metadata never blocks core initialization. Browser suffixes do not define runtime support. The fullscreen dialog defaults to all entries and can filter warning-and-error or error-only entries; viewing, copying and exporting use the same current filter. Exported reports add app/device/session metadata, actual demux/codec/track identity, seek state, full-cache phase/reason/range/byte facts and a passive VPN/network-capability snapshot. The `:player` runtime observes default-network changes on its single Handler, coalesces callbacks for 250 ms and appends only changed redacted facts; every load records another request-start snapshot, while export records the main-process snapshot at report time. When a process-bound network exists it is the effective network for transports, DNS, routes, proxy and metering; active and effective transports are recorded separately. No snapshot records a network handle, interface name, IP, DNS name or proxy address. Request-header values, Cookie, Authorization, URL query values, titles and private paths remain omitted.
- `WebViewClient.shouldInterceptRequest` is a background-thread observation path. It may read immutable or volatile WebSession request state, but it must not call `WebView`, `WebSettings`, navigation, DOM, or other UI-thread-only APIs.
- Permission Center is a coordination surface, not another settings owner. Device-capability controls remain in Kiyori system security settings, and AI tool policy remains in the existing Tool Permission destination backed by `ToolPermissionSystem`.
- Permission Center groups High-Impact Action policy and AI Operation Records under one AI Security section. Neither entry is shown until its complete policy or record page exists.
- Backup, conversation-data management, token statistics, and the Toolbox utility that changes permissions of other installed applications are not Permission Center domains.

## AI action authorization

- Risk is assigned to a concrete command and its target, scope, reversibility, data sensitivity, and external impact. A tool name alone never determines risk.
- **R0 Read-Only** operations do not change local or external state and need no operation confirmation after the tool gate permits them.
- **R1 Low-Impact** operations are local, bounded, and easy to reverse or stop. Persistent tool authorization may permit them without per-operation confirmation.
- **R2 High-Impact** operations change persistent state, access sensitive data, affect multiple objects, or prepare external impact. They require one-time confirmation by default; an explicit session grant is allowed only for a fixed target and scope.
- **R3 Critical** operations are difficult to reverse, privileged, financial, account-sensitive, or create an external action that cannot be recalled. Every operation requires confirmation; session and persistent bypass are prohibited.
- `FORBID`, `ASK`, and `ALLOW` control whether a tool may be invoked. The R0-R3 contract separately controls whether the concrete operation may produce side effects. `ALLOW` cannot bypass R2 or R3 confirmation.
- When `ASK` and an R2 or R3 confirmation apply to the same command, Kiyori presents one combined authorization decision rather than consecutive prompts.

## Distribution contract

- This private development build does not check the Operit release channel or receive Operit remote announcements.
- User-facing project, help, issue, and release links must use the Kiyori repository unless a feature explicitly documents a third-party service.
- Updates are distributed deliberately from the Kiyori repository. The application does not perform automatic or manual in-app update checks at this stage.

## Compatibility boundary

The following names are implementation or interoperability identifiers, not the Kiyori brand, and are not renamed by a visual-brand migration:

- the source namespace `com.ai.assistance.operit`;
- the `operit://` OAuth callback and other externally consumed URI schemes;
- persisted database, preference, backup, workspace, plugin, ToolPkg, MCP, Intent action, and file-format identifiers;
- `.operit/config.json`, `com.operit.*` ToolPkg IDs, `remote_operit`, `operit-pc-agent` filenames and directories, and `OPERIT_*` environment variables;
- attribution, license history, source references, and upstream documentation that must continue to identify Operit accurately.

Changing one of these identifiers requires a separate compatibility design and migration plan.

Kiyori's user-visible `versionName` and the **Operit Market Compatibility Version** are independent version axes. `versionName` identifies the Kiyori product release. `BuildConfig.OPERIT_MARKET_COMPAT_VERSION` is a non-user-facing semantic version that identifies the inherited Operit script and ToolPkg runtime contract used for market `minAppVer` and `maxAppVer` checks. Its current value is `1.12.0+9`; this contract includes persisted-message Hooks, shared pre-send Hook deadlines, character-card SoftwareSettings operations, address-aware location queries, the market revision workflow, and the bundled extra-info injection deadline. It changes only when Kiyori adopts and verifies a different market runtime contract. GitHub Release assets are durable publisher artifacts: a later Operit Market registration failure reports `RegistrationFailed` but must not delete the created Release or uploaded asset, so the publisher can inspect or retry the registration without rebuilding the artifact.

## Development identity and migration

- The continuous-development branch is `main`; parent and `terminal` remotes publish Kiyori changes only to their respective `main` branches.
- The `terminal` directory is the `KiyoriTerminalCore` submodule and is pinned by a parent gitlink.
- `com.kiyori` is a new Android application identity. It cannot in-place upgrade `com.ai.assistance.operit`; users must export and import supported backups.
- Kiyori-owned public downloads and exports use domain-specific directories under `Download/Kiyori`; public images use `Pictures/Kiyori`. The host does not automatically scan, read, merge, copy, migrate, or delete `Download/Operit`. Old public ToolPkg data enters a private generation only after the user selects a SAF tree and a package-specific migrator validates the declared input.
- Current-app sandbox references use `/data/data/com.kiyori`; source namespaces and external action or provider contracts continue using their established compatibility identifiers.
- Terminal user-visible surfaces use Kiyori, including the Ubuntu welcome banner, file-provider title, and generated package-source comments. Terminal keeps `installed-rootfs/ubuntu`, `.operit_installed_ok`, its existing internal mount variables, and native filenames.
- The terminal Node.js environment is ready only when `node`, `pnpm`, and `tsc` all execute successfully. Setup installs both CLIs into npm's existing global bin, and readiness resolves `npm prefix -g` explicitly so visible and hidden Ubuntu sessions do not depend on profile-specific `PATH` state.
- Inherited backup formats, market wire types, plugin IDs, ToolPkg IDs, MCP IDs, namespaces, and protocol identifiers remain interoperability boundaries even when visible text says Kiyori.
- Root JavaScript metadata is private development tooling (`kiyori-tooling`), not a runtime package or public npm contract.

## 2026-08-23 网络代理异常自然退出恢复

- 现场日志已确认一个真实用户可见的故障窗口：主进程 Mihomo generation 1 在 `15:26:45` 输出
  `Mihomo shutting down` 并以退出码 `0` 结束，直到 `15:28:12` 下一次协调才启动 generation 2；该窗口
  足以导致 Browser 所有站点 `net::ERR_PROXY_CONNECTION_FAILED`。历史日志中的上游节点 `context deadline
  exceeded`、拨号 `i/o timeout`、播放器 TLS 对端中止和 Range `partial file` 仍是独立的连接质量证据，
  不由 runtime 重启冒充解决。
- `KiyoriMihomoRuntimeState.failureKind` 区分 `UNEXPECTED_PROCESS_EXIT`、`HEALTH_CHECK_FAILED` 和
  `START_FAILED`；未被 `stopLocked()` 标记的自然退出，以及连续两次本地 Controller/mixed-port 健康失败，
  进入同一个自动恢复条件。按请求关闭、VPN 冲突、配置替换和启动失败不复用该触发器。
- `KiyoriNetworkProxyManager` 是恢复协调的唯一 owner：监听本进程 runtime state，在现有 `mutationMutex`
  内重新读取当前配置和 VPN 状态并执行协调；每个失败 generation 一次、五分钟最多两次，超过后保持
  `ERROR` 并记录限额。恢复不换节点、不静默直连、不吞错，也不新增用户设置项。
- Browser WebView 会在恢复协调时安装新 mixed-port；`:player` 的既有 mpv 线程会在新 generation 运行后
  刷新 `http-proxy`。下载任务继续使用创建时冻结的端点快照，避免恢复过程中无日志切换正在执行的传输。
-  runtime 诊断和播放器报告新增 `failure=...`，恢复日志包含失败 generation、尝试次数、恢复结果、新
  generation、端口和健康状态。主文档 WebView 错误、动态 OkHttp 连接错误和浏览器下载重试/耗尽错误也
  进入同一脱敏日志窗口；WebView ProxyController 安装/清理回调有 5 秒超时，避免协调锁永久等待。
- 多段下载对分段请求严格要求 HTTP `206`；服务端忽略 Range 返回 `200` 时明确失败，禁止把完整响应写入
  分段文件造成静默数据错位。
- 当前实现只完成本地代码、JVM 测试和 Debug 构建证据；设备上的真实订阅、自然退出触发源、Browser/
  player/downloader 现场行为仍为 `verification_pending`。
