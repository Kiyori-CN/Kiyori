# Kiyori 开发协作准则

本文约束开发者与 Agent 的工作方式。产品语义见 [CONTEXT](CONTEXT.md)，用户说明见 [README](README.md)，执行规则不复制到产品文档。

## 1. 项目边界

- Kiyori 始终处于未发布的开发阶段，无需反复询问发布状态。替换内部方案时完整清理失效实现，保留仍有实际用途的部分。
- 保持已有用户接口、数据与外部互操作契约。包名、协议、持久化格式、AIDL/JNI 等稳定标识的变化必须先有明确兼容设计；内部迭代不等同于破坏用户接口。
- 优先定位根因，复用唯一状态所有者。未经用户明确要求，禁止新增回退、降级、静默替代或兜底逻辑。
- 不进行无关重构、依赖升级或顺手清理。保留现有用户改动，禁止覆盖来源不明的文件。

## 2. 开始工作

1. 阅读本文件与精简的 [CONTEXT](CONTEXT.md)，再按任务读取相关正式文档；不要默认加载整个 docs 或历史记录。
2. 检查 `git status --short --branch`、当前分支、HEAD 及相关差异，区分本轮改动与既有工作。
3. 持续开发前阅读 [正式开发准备](docs/TODO/formal_development_readiness/index.md) 及其五份分项清单。
4. 明确目标、范围、非目标、风险和验收方式；多步骤任务在 [开发任务索引](docs/TODO/README.md) 对应的专项 `index.md` 中细化计划。
5. 只有会实质改变行为、数据、兼容性或授权边界的歧义才集中澄清；其余通过源码、文档和低风险验证主动解决。

## 3. 实现与工具约束

| 场景 | 要求 |
| --- | --- |
| 文件与文本搜索 | 使用 `rg`，禁用 Search files 工具 |
| 修改文件 | 使用补丁或明确 UTF-8 的编辑工具；严禁使用 PowerShell 编辑代码文件 |
| 仓库 Python | 使用项目 `.venv`；迁移至 pixi 前保持现有入口 |
| 全局 skill 与外部工具 | 使用对应工具记录或返回的绝对运行时，不套用项目虚拟环境 |
| 调试修复 | 为关键、非直观逻辑说明修改意图、约束及不这样处理的后果 |
| 子模块 | `terminal` 独立审查与交付，父仓库只记录固定 gitlink；不混入未完成子模块改动 |

TypeScript 额外规则：

- 尊重 Hook 与宿主返回的确定类型，不扩大为 `any`、`unknown` 或不必要的可空联合类型。
- 返回类型为 `string | undefined` 时，用显式分支或 `?? ""` 处理，不直接 `as string`。
- 不用 `String(...)` 掩盖类型错误；`catch` 中必须记录可诊断、已脱敏的错误。

## 4. 文档维护

- 遵循 [文档维护规范](docs/doc-src/before_docing.md)，使用中文说明和稳定技术标识。
- README 负责用户上手；CONTEXT 只保存高频术语、所有权和不变量；详细契约按领域放入正式文档；阶段证据留在专项 TODO。
- 修改行为、接口、配置或验证方式时，同步对应权威文档，不重复维护多份事实。
- 历史命令、测试数量和 APK 哈希保留观察日期；未验收项目不能被整理成“已完成”。

## 5. 验证与收尾

- 每轮完成仓库修改后，默认在收尾阶段**串行**执行以下命令，并核验实际 Debug APK；用户当前明确要求跳过构建时除外：

  ```bash
  ./gradlew :app:assembleDebug --no-daemon --console=plain
  ```

- Windows 可使用对应的 `./gradlew.bat` 入口；标准产物为 `app/build/outputs/apk/debug/app-debug.apk`。
- 除生成 Debug APK 所需任务外，单元测试、Lint、Release 构建或专项检查仅在用户明确要求或当前改动风险确有必要时执行。
- 正式准备检查：`python -B ci/script/check_formal_readiness.py --repository . --require-main`。
- 新鲜克隆检查：`python -B ci/script/check_fresh_clone.py --repository .`；该命令验证已形成的候选提交，不验证未提交工作区。
- 文档整理执行 [文档维护规范](docs/doc-src/before_docing.md) 中的相关检查。
- 明确区分静态、单元、构建、设备与远端证据；必要现场验收未完成时保留 `verification_pending`。

## 6. Git 与沟通

- 持续开发以 `main` 为唯一目标。默认不提交、不推送；只有用户在当前任务中明确要求时才执行。
- 提交前审计精确文件清单，排除凭据、私密配置、构建产物及 `work/` 检查点；提交推送后核对本地、跟踪与远端 ref。
- 用户表达愤怒时先停止工作，道歉并反思，确认需求后再继续实施。
- 使用中文汇报结果、关键变更、验证证据、剩余问题和 Git 状态；路径、API、命令与错误文本保留原样。
